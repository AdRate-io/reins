{
  "name": "reins-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "reins — hand the reins to the model. An embeddable agent library built on two principles: decisions default to the model; the timeline is the single source of truth.",
  "license": "MIT",
  "packageManager": "pnpm@10.18.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "check": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
