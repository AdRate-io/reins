# 技术方案 v1.0

> 日期：2026-09-08
> 作者：技术合伙人（架构师视角）
> 对应：`docs/PRD.md`
> 状态：M0 已按本方案落地并回填实测结论（2026-09-08，T16 复盘）；接口以代码为准，本文同步描述
> 继承：v0.1 稿的事件模型、EventLog、Projection、Lowering、schema 演进约定（§4、§5、§8）在此更新后沿用

---

## 1. 架构原则（由宪法推出的工程约束）

| # | 原则 | 工程含义 |
| --- | --- | --- |
| P1 | 决策权默认在模型 | 任何"框架替模型做决定"的逻辑必须同时提供"模型自决"路径，且后者为默认；框架动作降为兜底 |
| P2 | 时间线是唯一真源 | 内部只有一种数据：带发起者的事件；角色格式只在降级层出现；不存在第二份"消息数组"真源 |
| P3 | 不拥有循环 | 循环是一个导出的普通异步生成器函数，几百行，无私有状态；用户可复制修改后仍能接入所有模块 |
| P4 | 一切可拆 | 每个脑子模块是独立包中的独立对象，通过统一插座挂到循环上；移除任一模块不破坏其余 |
| P5 | 运行时无关 | 核心包零 Node 内置依赖，只用 Web 标准 API；子进程、`node:vm`、真实文件系统只出现在可选包 |
| P6 | 进程可随时死亡 | run 状态可序列化；暂停是显式返回值；任何事件边界可重入 |
| P7 | 有损必声明 | 降级层对每种事件声明在每家 API 上是否有损；禁止静默丢弃 |
| P8 | 无 eval 不默认开 | 模型自决类机制附 eval 并达标后才默认开启，否则默认关闭 |
| P9 | schema 有版本 | 事件 type 各自带 schemaVersion；读时升级；查不到升级函数即拒绝读取 |

---

## 2. 总体结构

```
宿主应用（TanStack Start / Hono / Next / 任意 fetch 服务端）
   │  (Request) => Response            ← @reins/server：Web 标准 handler，SSE 由宿主注入
   ▼
┌───────────────────────── 循环 Loop（@reins/core，可复制）─────────────────────────┐
│  for each turn:                                                                │
│    events   = eventLog.read(session)                                           │
│    ctx      = projection(events, budget)        ← 过滤 → 折叠 → 注入 → 预算     │
│    request  = lowering.toRequest(ctx, tools)    ← 事件 → 某家 API 请求          │
│    for e of lowering.stream(request): eventLog.append(e)                       │
│    for call in toolCalls: policy → execute/spill → eventLog.append(result)      │
│    decide: continue | paused(serializable) | done | handoff                     │
└────────────────────────────────────────────────────────────────────────────────┘
   ▲ 插座 Socket：beforeModel / afterModel / beforeTool / afterTool / onTurnEnd
   │
┌──┴──────────── 脑子 Brain（@reins/brain，每个模块独立可拆）────────────────────────┐
│ perception  compact  pins(幸存契约)  spill  handoff  memory  approval  budget  │
└────────────────────────────────────────────────────────────────────────────────┘
   │
┌──┴─────────────── 存储接口 Stores（接口在 core，实现在可选包）─────────────────┐
│ EventLog（append-only）   BlobStore（外溢与附件）   MemoryStore（记忆文件）      │
└────────────────────────────────────────────────────────────────────────────────┘
   │
┌──┴─────────────── 降级 Lowering（@reins/lowering-pi，包裹 pi-ai）──────────────────┐
│ 事件 → Anthropic / OpenAI Responses / 其余 10 种线协议；流式响应 → 事件          │
└────────────────────────────────────────────────────────────────────────────────┘

投影方向（同一条时间线的多种视图）：
  → 模型上下文（projection）   → 前端（@reins/ui-agui）   → 审计回放（原样 + replayTurns 逐轮重算）   → eval fixture（@reins/eval）
```

**同一插座的三种宿主：** 我们的默认循环；TanStack AI 的 middleware（S2 核实，0.53.0：`onConfig` 对应 beforeModel，可同时返回 `providerMessages` 与 `systemPrompts`；`onBeforeToolCall`/`onAfterToolCall` 对应工具钩子；`onInterruptBoundary` 对应审批暂停；`onShouldContinue`/`onUsage` 对应预算；`onChunk` 对应模型事件入日志）；pi-agent-core 的 `transformContext`/`beforeToolCall`/`afterToolCall`。脑子只依赖插座，不依赖循环实现。TanStack 的消息角色无 system，`system_note` 在该路径有损（见 §11）。**B10 落地**：`@reins/adapter-tanstack-ai` 的 `reinsMiddleware()`，日志仍是唯一真源、每轮 `onConfig` 用投影产出 `providerMessages`；动态审批以通用中断 `reinsApprovalInterrupt` 表达；与默认循环的差异见 DECISIONS 2026-09-08 B10 三条。

---

## 3. 包结构（pnpm monorepo）

| 包 | 内容 | 依赖约束 | 期 |
| --- | --- | --- | --- |
| `@reins/core` | 事件类型、Store 接口与内存实现、Projection、Loop、RunState、Socket、Tool 抽象、Policy 接口、replay | 零运行时依赖，Web 标准 | 1 |
| `@reins/brain` | perception、compact、pins、spill、handoff、memory、approval、budget | 仅 core | 1 |
| `@reins/lowering-pi` | pi-ai 包裹；能力声明；有损矩阵 | pin `@earendil-works/pi-ai@0.85.1`；只从 `api/*` 子路径导入 | 1 |
| `@reins/store-sqlite` `@reins/store-pg`（内存实现在 core：`memoryStore()`） | SQLite 与 Postgres 实现；一致性测试套件在 `@reins/core/testing` | sqlite 驱动 `node:sqlite` / `bun:sqlite`（S3），可选包；pg 与 dogfood 宿主同库；Workers 用 Durable Objects SQLite 另起 `@reins/store-do`（第二期） | 1（M1 B9） |
| `@reins/server` | `createAgentHandler()`：Web 标准 handler，SSE，`lastSeq` 补发；`./node` 子路径 `nodeListener`（node:http 适配） | 仅 core | 1 |
| `@reins/ui-agui` | 时间线 → AG-UI 映射与流式编码器；`demo/index.html` 最小页面 | core、server（仅类型） | 1 |
| `reins` | 总包：`createAgent()` + 原样再导出 core / server / ui-agui | 三者；不含 lowering-pi | 1 |
| `@reins/adapter-tanstack-ai` | 脑子作为 TanStack AI middleware | `@tanstack/ai` 隔离在此 | 1 |
| `@reins/eval` | fixture（种子日志 + 录像回放工具 + 预埋事实 / 约束）、指标、对照运行器、门禁 | 仅 core | 1（M2 E1） |
| `@reins/tools-mcp` `@reins/tools-openapi` | 工具来源适配 | `@modelcontextprotocol/client@2` | 1 / 2 |
| `@reins/adapter-pi` `@reins/adapter-ai-sdk` | 其余底盘 | | 2 |
| `@reins/governance` | 信任分层、principal、指令加载、验证 gate、hooks.json 桥接 | | 2 |
| `@reins/skills` | 技能生命周期 | | 3 |
| `@reins/code-mode` | 代码编排工具：QuickJS-wasm 为 L1，托管沙箱为 L2；或直接适配 `@tanstack/ai-code-mode` | | 3 |

scope `@reins`；若 npm 组织名不可用则退为 `@reinsjs`。

---

## 4. 核心数据模型：事件时间线

沿用 v0.1 §4.1，补充信任与来源字段。

```ts
interface EventBase {
  id: string                      // uuidv7，时间有序
  sessionId: string
  seq: number                     // 会话内单调递增
  at: number                      // Unix ms
  type: string                    // "core.user_message" | "core.tool_result" | "ext.*"
  schemaVersion: number
  actor: "user" | "model" | "tool" | "system" | "host"
  parentId?: string               // 因果链 / 分叉点
  trust: "principal" | "system" | "model" | "untrusted"   // 工具输出与外部内容默认 untrusted
  provenance?: { source: string; ref?: string }           // 哪个工具、哪个 URL、哪个用户
  replay?: Record<string, unknown>                        // thinking signature / encrypted reasoning 等厂商回放数据
}
type Event<T extends string, P> = EventBase & { type: T; payload: P }   // 载荷统一放 payload，壳永远稳定
type ContentPart = { type: "text"; text: string } | { type: "image"; mime: string; data: string /* base64 */ }
```

实现（T3，`packages/core/src/events/`）：载荷放在 `payload` 字段下而非平铺，升级函数只碰 payload；`EventSchemaRegistry` 登记 type → 当前版本 + 逐级 upcaster，`read()` 对未知 type、未来版本、缺升级步骤、壳字段畸形一律抛 `SchemaError`（fail-closed）；登记时即校验升级链完整，宁可启动报错；`createEvent` 从注册表取版本号，未登记的 type 写不进日志；`trust` 缺省按 actor：user→principal、system/host→system、model→model、tool→untrusted；id 为自实现的 uuidv7（仅 `crypto.getRandomValues`）。

核心事件类型（`core.*`）：

| type | 载荷要点 | 备注 |
| --- | --- | --- |
| `user_message` | content parts | 中途插话就是再 append 一条 |
| `model_text` / `model_thinking` | text；thinking 带 replay | |
| `tool_call` / `tool_result` | toolCallId、name、args / content parts、isError、`spilled?: {blobId, summary}` | 结果外溢时 content 只留指引 |
| `system_note` | kind: "perception" \| "pin" \| "budget" \| "host"，text，`meta?`（不进模型上下文），`supersedes?: id[]`（取代的旧说明，B3） | 脑子注入的一切都是可见事件 |
| `approval_request` / `approval_decision` | toolCallId、policyId、摘要 / approved、by | 审批是事件级，可跨进程 |
| `compaction` | coversSeq [from,to]、summary、decidedBy: "model" \| "threshold"、pinsKept[] | 不删原事件，只追加 |
| `handoff` | toSessionId、summary、triggerMessage、reason | |
| `memory_op` | op、path、`toPath?`（rename 目标，B6 可选字段）、bytes | 模型读写记忆留痕；只记成功的操作，模型不可见 |
| `budget_usage` | tokens、toolCalls、wallMs、remaining | 用量是事件，宿主可聚合配额 |
| `run_paused` / `run_resumed` | reason、stateRef | |
| `error` | 分类、message、retryable | |

不变量：EventLog 只 append；压缩、外溢、交接都以追加事件表达；模型可见的一切都在日志里（"model-visible ⟺ logged"）。

Schema 演进：沿用 v0.1 §8（只加可选字段不升版本；改义升版本；读时 upcast；查不到拒绝）。**读时升级发生在 `readTimeline` / `readEvents`（`@reins/core` store 层，T16）**：存储实现只原样存取、不认识 schema；循环每轮、run 起步、server 补发与预校验、回放全部经它读，逐条 `registry.read`，未登记的 ext.* 或未来版本在写任何东西之前抛 `SchemaError`。宿主有 ext.* 事件时通过 `LoopConfig.registry` 给自己的注册表。

---

## 5. Store 接口

```ts
interface EventLog {
  append(events: Event[]): Promise<void>                       // 同会话原子
  read(sessionId: string, opts?: { fromSeq?: number; toSeq?: number }): AsyncIterable<Event>
  tail(sessionId: string, n: number): Promise<Event[]>
  fork(fromSessionId: string, atSeq: number, toSessionId: string): Promise<void>
}
interface BlobStore {   // 外溢结果、附件、代码执行文件
  put(bytes: Uint8Array | string, meta: { mime: string; sessionId: string }): Promise<{ id: string }>
  get(id: string): Promise<{ bytes: Uint8Array; meta: BlobMeta }>
  slice?(id: string, range: { start: number; end: number }): Promise<Uint8Array>   // 可选：模型分段取回
}
interface MemoryStore {  // 记忆工具后端，路径限定在 /memories 下
  list(prefix: string): Promise<string[]>
  read(path: string): Promise<string | null>
  write(path: string, content: string): Promise<void>
  delete(path: string): Promise<void>
}
```

- 契约细则（T4 落地，`packages/core/src/store/`）：`append` 由调用方分配 seq，日志只校验同会话且从末尾 +1 连续（首条为 1），整批先校验再写；不连续抛 `StoreError("seq_conflict")`，这就是乐观并发控制。`fork` 原样复制 [1, atSeq]（保留 id 与 seq，只换 sessionId）以维持 parentId / pinsKept 引用，目标会话必须为空。`read` 区间闭合，`tail` 升序。BlobStore 字符串按 UTF-8 存，`get` 缺失抛 `not_found`；MemoryStore `delete` 幂等，路径限定由 memory 工具层负责。
- 允许部分实现：只实现 EventLog 即可跑；缺 BlobStore 则 spill 模块自动关闭并告警；缺 MemoryStore 则 memory 工具不注册。
- 每个接口附一致性测试套件，入口 `@reins/core/testing`（T5）：只要求宿主传入 `{ describe, it }`，断言自带，vitest / node:test / bun:test 皆可；内存实现是参考实现，必须全绿。
- 默认实现：内存（零依赖）、SQLite（可选包）、Postgres（可选包，dogfood 宿主投放工具用 pg，与 SQLite 同在 M1 B9）。
- **实现（B9）**：`@reins/store-sqlite` 与 `@reins/store-pg`，两包都跑 `@reins/core/testing` 的三份一致性套件（内存实现为基准）。事件整条存 `data` 列、壳字段（session_id / seq / id / type / at）抽列，主键 `(session_id, seq)` 的唯一约束即乐观并发闸。**SQLite**：只认 `{ exec, prepare }` 形状（`node:sqlite` DatabaseSync 与 `bun:sqlite` Database 都满足），`append` 在 `BEGIN IMMEDIATE` 事务里先查末尾再插；`slice` 用 `substr` 在库内切 BLOB；`@reins/store-sqlite/node` 的 `openSqlite` 用内置 `node:sqlite` 打开、文件库缺省 WAL + busy_timeout；主入口零 `node:*`。**Postgres**：只认 `query(text, params) → { rows }`（pg 的 Pool/Client、PGlite 都满足），每个写操作都是**单条语句**（`append` 用 `INSERT … SELECT unnest(...) WHERE 当前末尾 = 首 seq−1 RETURNING`：插到 0 行即 seq_conflict，并发双写撞主键也是 seq_conflict），不开事务、不借连接，传连接池即正确；**`data` 用 `json` 而非 `jsonb`**——jsonb 重排键序会让 T10 的 pendingDigest / configHash（按 `JSON.stringify` 算）在续跑时误判篡改，json 按文本原样存取，各后端逐字节一致；`fork` 因 json 无 `jsonb_set`，在 JS 里改 sessionId 再整批插回。DDL 逐条执行（扩展协议客户端不接受一次多条语句）。测试缺省用 PGlite（进程内 WASM Postgres），设 `REINS_PG_URL` 再对真库跑一遍。

---

## 6. Run 状态与暂停

```ts
type RunResult =
  | { status: "done"; sessionId: string; lastSeq: number }
  | { status: "paused"; sessionId; lastSeq; reason: "approval" | "budget" | "host"; interruptions: Interruption[]; state: SerializedRunState }
  | { status: "handoff"; sessionId; lastSeq; toSessionId: string }
  | { status: "error"; sessionId; lastSeq; error: ErrorEvent }

type Interruption =
  | { kind: "approval"; toolCallId; request: ApprovalRequestPayload; call: ToolCallPayload }
  | { kind: "client_tool"; toolCallId; call: ToolCallPayload }     // 客户端工具，等宿主回填 tool_result
  | { kind: "budget" | "host"; note: string }

// 恢复：任何进程、任何实例。同一 sessionId 再跑一次即续跑；带 resume 多一层校验
runLoop({ sessionId, log, lowering, model, tools, secret,
          resume: JSON.parse(stateFromUrl), decisions: [{ toolCallId, approved: true, by: "boss" }] })
```

- `SerializedRunState` 只含 sessionId、lastSeq、pending 工具调用 ID、配置摘要（SHA-256 of 模型 + 工具名 + 系统提示）、pending 入参摘要（SHA-256 of 各调用的 id/名/入参），配了密钥再加 HMAC-SHA256 签名；内容全部从 EventLog 重读。实测不到 400 字节，能放 URL 参数或 KV。
- **恢复校验（T10）fail-closed，全部通过才写日志**，顺序：形状 → 会话一致 → 签名（配了密钥而无签名或签名不符都拒）→ 配置摘要一致（可 `allowConfigDrift` 放行）→ 日志不短于状态 → 日志里的 pending 调用与状态的 ID 列表、入参摘要一致 → `decisions` 都指向 pending 调用。不通过抛 `RunStateError`（8 种 code）。通过后 append `run_resumed(by)`、逐条 `approval_decision`，然后走"补齐 pending 调用"。不配密钥则不签名不验签，只适合可信环境。
- **续跑的统一机制（T9）**：每轮开始先扫时间线，凡是没有 `tool_result` 的 `tool_call` 都算 pending，先补齐再问模型。审批暂停、宿主中止、进程死亡、客户端工具回填，恢复时走的都是这一条路。已有 `approval_decision` 的按决定办（拒绝 → `tool_result(isError)`），已发过 `approval_request` 还没决定的不重复发、再次暂停。
- 嵌套（子代理）的审批冒泡到最外层 run（借 OpenAI Agents SDK）。

---

## 7. 循环与插座

```ts
export async function* runLoop(cfg: LoopConfig): AsyncGenerator<Event, RunResult> {
  // 约 600 行（M0 收口实测 578 行含注释），无私有状态，用户可整个复制。yield 每一条刚 append 的事件
}

interface Socket {                       // 脑子与底盘之间唯一的契约；钩子可不返回 = 无意见
  tools?: StaticContribution<readonly Tool[]>   // 静态贡献（B2）：模块带给模型的工具，整个 run 不变，续跑补齐 pending 时也在场，计入 configHash
  systemPrompt?: StaticContribution<string>     // 静态贡献（B2）：规则提示片段，追加在宿主系统提示之后（空行分隔），整个 run 不变
  // StaticContribution<T> = T | ((setup: SocketSetup) => T | undefined)（B6）：常量，或按运行环境算一次的函数；
  // SocketSetup = { log, blobs?, memory?, model, principal?, hostTools }，只含整个 run 不变的东西
  beforeModel?(ctx): BeforeModelPatch | undefined        // { events?, tools?, systemPrompt? } 顺序合并（后一个 Socket 在 ctx 里看到前一个改过的）；期间 emit 的事件本轮即可见
  afterModel?(ctx, events: Event[]): void
  beforeTool?(ctx, call: ToolCallEvent, tool: Tool | undefined): "proceed" | { block: string } | { defer: { policyId, summary } } | { rewrite: unknown } | undefined
  afterTool?(ctx, call, result: ToolResultDraft): ToolResultDraft | undefined   // 结果此时尚未 append，返回即替换（外溢、截断）
  onTurnEnd?(ctx): "continue" | "stop" | { handoff: HandoffIntent } | { pause: { reason: "budget" | "host"; note? } } | undefined
}
interface TurnContext {
  session: { id; turn }; principal?: Principal
  events: Event[]                        // 当前投影结果（可读）
  timeline: readonly Event[]             // 完整时间线快照（回看被折叠的事件）
  log: EventLog; blobs?: BlobStore; memory?: MemoryStore
  tools: readonly Tool[]; model: ModelRef
  budget: { contextLimit; used; tokensSpent; turns; toolCalls; wallMs }
  capabilities: LoweringCapabilities     // 本模型支持什么（中途 system、thinking 回放、并行工具）
  signal?: AbortSignal
  emit(e: EventDraft): void              // 草稿：无 id / seq / at / sessionId，循环补齐后 append
}
```

- 多个 Socket 按注册顺序执行。`beforeTool` 任一 `block` / `defer` 即定，`rewrite` 替换入参后继续问下一个（B7：后面的钩子在 `call.payload.args` 里看到改写后的入参，日志里的 tool_call 原样；宿主已批准的调用再遇到 `defer` 只是略过、继续问后面的钩子 —— 批准解决的是"要不要问人"，排在后面的审批策略仍可 block）；`onTurnEnd` 第一个给出意见的定，都无意见时"本轮有工具调用则继续，否则结束"。
- 静态贡献与动态补丁分两条路（B2）：模块给模型的工具与规则提示走 `Socket.tools` / `Socket.systemPrompt`，循环起步并入（宿主同名工具优先），整个 run 逐字不变——这是 §9.1 约束 3（系统提示与工具表每轮稳定）的落点，也让续跑补齐 pending 调用时模块工具在场；`beforeModel` 补丁留给真正的动态改动（宿主换场景、按 principal 增删工具），每改一次缓存前缀重算。**静态贡献可以是函数**（B6）：run 起步时拿 `SocketSetup`（有哪些存储、给谁跑）算一次，返回 undefined 即这次不贡献 —— §5"缺 MemoryStore 则 memory 工具不注册"就靠它落地，而不是注册一个一用就报错的工具。解析算法在 `resolveSocketContributions`（core 导出），循环起步与 server 的恢复预校验共用，configHash 才对得上（B6 顺带修：此前 server 预校验只用宿主工具算 hash，装了任何带静态贡献的模块后续跑都会误判为配置漂移）。
- 循环只有两处兜底（P1）：工具声明 `needsApproval` 且没有 Socket 做主时转审批暂停（policyId `tool.needsApproval`）；轮数达到 `maxTurns`（缺省 100）时 `paused(budget)`。细粒度预算在 brain budget 模块（§9.8，B8）。
- 每轮模型调用后循环 append 一条 `budget_usage`（用量、本轮工具调用数、耗时、该请求的投影估算 `contextEstimate`）；降级层 `stopReason=error` 或抛异常 → append `core.error` 并返回 `error` 态，已产出的内容块仍在日志；`aborted` → `paused(host)`，未回答的 tool_call 记为 pending。
- 工具执行：未知工具、`validate` 抛错、`execute` 抛错一律以 `tool_result(isError)` 告知模型，循环不崩；钩子与工具经 `ctx.emit` 留的痕在**每条**结果路径（正常、拦截、未知、入参不合法、审批请求）都先于结果落日志（B7）；`execute` 返回值按 `normalizeToolOutput` 归一（string → 文本；ContentPart[] 原样；`{content,isError}` 原样；其余 JSON）；工具经 `ctx.emit` 留的痕（如 memory_op）排在结果之前。`side: "client"` 或无 `execute` 的工具 → `paused(host)` + `client_tool` 中断，宿主回填 `tool_result` 后续跑。
- 交接：`onTurnEnd` 返回 handoff → 旧会话 append `core.handoff`，新会话 seq 1 为 `system_note(kind=host, summary)`、随后是 `HandoffIntent.opening` 里的开场草稿（B5：脑子带过去的 pin，循环只补壳字段不解释内容）、最后 `user_message(triggerMessage)`（Anthropic 的 system 不能为首条由降级层归位解决），调用 `cfg.onHandoff(from, to)`，返回 `handoff` 态；续跑新会话由宿主再起一次 runLoop。
- 测试用 `@reins/core/testing` 的 `ScriptedLowering`：按剧本逐轮吐草稿，记录每轮 `toRequest` 输入，脑子模块与适配器的单测都靠它。
- TanStack AI 与 pi 的适配器只是把各自钩子翻译成这五个方法。

---

## 8. 投影（Projection）

策略链，输入完整时间线，输出模型本轮应看到的事件：

1. **过滤**：按可见性剔除运维事件（approval_*、run_paused/resumed、budget_usage、memory_op、handoff、error 默认不给模型看），宿主可加 principal 可见性判定。trust 标注（不可信内容加显式标记，不删除）**由降级层做**：事件已带 trust 字段，翻译成文本时按它包裹，投影不复制事件、不篡改 payload
2. **折叠**：用 `compaction` 事件替代其覆盖的 seq 区间；被折叠的原事件仍在日志。多条 compaction 的隐藏范围取并集，被盖住的旧摘要不再可见但其范围仍生效。摘要放在**被覆盖区间的位置**而非自身 seq 位置，所以投影输出顺序可以与 seq 不一致（视图给模型看，日志才按 seq）
3. **钉住**：幸存契约。被覆盖的事件只有在**每一条**覆盖它的 compaction 都保留时才幸存：出现在 `pinsKept`，或它本身是 `system_note(kind=pin)`（自动幸存，默认开，模型 pin 过的东西默认穿越折叠）。**被后来某条 `system_note.supersedes` 指到的事件一旦被覆盖就不再幸存**，不论谁保留过它（B3：append-only 日志里撤销一条钉住的唯一表达；取代者在完整时间线里找，自己被折叠了也算）。幸存者重排到覆盖它的最新摘要之后
4. ~~感知注入~~（B1 后移出投影链）：感知说明是 `@reins/brain` 的 Socket 在 `beforeModel` 里 emit 的**真实事件**，由循环追加到时间线末尾，模型本轮即见；投影只负责"看历史"，不负责"说话"。这样才满足 §9.1 的 prompt cache 约束（只追加、不改前缀），回放时它也和其他事件一样凭日志重现。默认链因此是四步
5. **预算裁剪**：仍超限时的最后兜底。按"轮"从最旧裁掉一个前缀，新造 `compaction(decidedBy=threshold)` 放最前、幸存 pin 紧随。切点三条硬规则：只在轮边界切（assistant 轮 = 连续 thinking/text/tool_call）；保留部分首轮不含 tool_result（不留孤儿结果）；seq 封闭（保留部分 seq 全大于被裁部分，保证新 coversSeq 是干净区间）。被裁的旧摘要原文并入新摘要。裁到只剩最后一轮仍超限则 `stats.overBudget=true`，交上层告警

投影是纯函数（T6，`packages/core/src/projection/`）：`project({ timeline, budget, strategies?, estimate?, registry?, now?, newId? }) → { events, emitted, stats }`。随机与时间全部从参数注入，同一输入逐字同一输出。策略新造的事件（目前只有阈值 compaction）通过 `emitted` 交给循环 append 进日志，seq 按时间线末尾预分配，冲突则重跑投影 —— 模型看到的每一条都要落日志。token 估算默认粗估（ASCII 4 字一 token、非 ASCII 一字一 token、图片 1600），宿主可注入精确 tokenizer；`reserveTokens` 缺省为窗口的 15%。

回放（T15，`packages/core/src/replay/`）：`replayTurns(timeline, { budget, strategies?, estimate?, registry? }) → { turns, preamble }`。因为投影是纯函数、循环又在每次请求前把模型将看到的一切先写进日志，所以"第 n 轮模型看到的" = project(该轮第一条模型输出之前的全部事件)，不需要额外的"请求开始"事件。每个 turn 给出 `requestAtSeq`、`visible`、`stats`、`output`（连续的 model 事件）、`aftermath`（到下一轮之前的工具结果 / 用量 / 暂停等）、`usage`（该轮 budget_usage）；重算若新造事件则 `diverged=true`（现在的策略或估算与当时不同，visible 不再是当时的事实）。边界：Socket 用 `patch.events` 整体替换投影的部分不可重算；一条模型输出都没回来的失败轮识别不出请求边界。示例 `examples/minimal/replay.ts` 在此之上叠加降级层 `toRequest` 的落点，生成终端时间线与静态页面。

---

## 9. 脑子模块规格

每个模块实现 `Socket` 的若干方法，注册若干工具，附开关、按模型族配置与 eval。

### 9.1 perception（感知）

- `beforeModel`：注入一条 `system_note`，含上下文使用档位（如 <50% / 50~70% / 70~85% / 85%+）、最旧未折叠段的年龄、本会话已用 token 与预算余量、上次整理位置、工具结果外溢数量。数值按档位而非精确值，减少 cache 抖动；每档只变一次。
- **prompt cache 约束（2026-09-08 与 Boss 达成共识，B1 实现时必须遵守）**：
  1. 感知说明**只追加在时间线末尾**（作为新事件 append，模型看到的顺序按 seq），天然落在最新 user 消息之后、模型回答之前；前面的历史一字不改，缓存前缀不受影响。Anthropic 中途 system 的摆放规则（S1：紧跟 user 轮）与此一致。**禁止**放在系统提示里或对话开头。
  2. **旧的感知说明不删、不隐藏**。隐藏一条曾经可见的事件等于改前缀，比多留几十个 token 更贵；每换一档才追加一条，一段会话通常三四条，整理时随历史一起折叠。这是宪法二"只追加"的自然结果。
  3. **系统提示与工具表每轮必须稳定**。`beforeModel` 的 `systemPrompt` / `tools` 补丁是给宿主换场景用的，感知不走这条路；改一次 = 整段前缀重算。
  4. 缓存本身不自造：pi-ai 已在系统提示、工具表末尾、最后一条 user 消息打 `cache_control` 断点，OpenAI Responses 用会话 id 作 `prompt_cache_key`；reins 只负责不破坏它，并用 `budget_usage.tokens.cacheRead / cacheWrite` 量出来。
  5. 唯一允许打掉缓存的是 compaction：折叠替换历史中段，前缀必变，所以只在轮次边界、由模型自决地做，把"重算一次缓存"当成有意识的花费。
- eval：与不注入对比，模型自决整理的时机质量（过早/过晚率）；**注入前后 cacheRead 占输入的比例不得下降**（§17 待核实项在 B1 验收时一并关闭）。
- 可选桥接 Anthropic task budget（模型支持时由服务端注入倒计时，我们不重复）。
- **实现（B1，`packages/brain/src/perception/`）**：`perception(options): Socket`。`beforeModel` 用 `readPerception(ctx)` 算读数 → `render` 成文字 → 与模型**当前可见**的最后一条感知说明逐字比较，相同则什么都不做，否则 `ctx.emit` 一条 `system_note(kind=perception, meta.reading=读数)`；永远返回 `undefined`（不碰 systemPrompt / tools）。读数：上下文使用率（估算 / contextLimit，缺省档 [0.5, 0.7, 0.85]）、阈值兜底触发点（`targetTokens / contextLimit`，新增到 `TurnContext.budget.targetTokens`）、可见的未折叠模型轮数（[5, 15, 40]）、会话整理次数（精确，只在整理时变）、会话累计 token（按 budget_usage 累加含缓存读写，[1e4, 5e4, 2e5, 1e6]）、可见外溢结果数（0 单列，[3, 10]）、配置 `limits` 时最紧一维的余量（[0.05, 0.2, 0.5]）。判重按文字而非读数：宿主自定义 `render` 后，模型看到的没变就不追加。**上下文使用率经校准（B8）**：投影估算只数可见事件正文，系统提示、工具表、thinking 等不在内，真实 input 可高出一倍（B2 实测）；`contextOverheadOf(timeline)` 取日志最后一条带 `contextEstimate` 的 `budget_usage`，算"真实上下文（input + cacheRead + cacheWrite）− 当时估算"为固定开销，本轮读数 = 估算 + 开销。用**加法**而非比例：多出来的部分在一个 run 里基本是常量，比例会随历史变长把误差放大。首轮没有可对照请求时开销为 0；`calibrate: false` 关闭；`limits.contextTokens` 维的余量也用校准值；开销精确值进 `meta.reading.contextOverhead`。折叠把旧说明盖掉后视图里没有说明，会重新注入一条（追加在末尾，不改前缀）。`system_note.meta` 是 T3 之后新增的可选字段（不升版本），降级层不翻译它。
- **缓存断点（B1 修的降级层问题）**：pi-ai 把 Anthropic 的 `cache_control` 打在"最后一条 user 消息"上；感知说明追加在末尾时，进 pi-ai 时它正是那条伪装成 user 的标记消息，`onPayload` 改写成 system 后断点就没了，而官方规则是"最后一个断点之后的内容一律不缓存"（`input_tokens` 的定义），整段历史都会按原价计费。处置做成 `PiAiLowering.midSystemCacheBreakpoint`，**缺省 `automatic`**：去掉该块级断点、在请求顶层补 `cache_control`（Anthropic 自动缓存：断点自动落在最后一个可缓存块上，与显式断点兼容、占 4 个槽位之一，满了就放弃不让请求 400）。实测（`spikes/b1-perception-cache`）：`automatic` 与不注入持平；`previous-user`（搬到前一条 user）低 3~6 个点；留在 system 消息本身上会让整段提示每次重写、命中率崩到 20% 以下，故不提供。第二家 Anthropic 协议上游 DeepSeek（`spikes/deepseek-anthropic-check`）接受顶层字段、注入亦不降命中，但其缓存全自动、分辨不出断点处置，直连官方 Anthropic 的对照仍未做。第三方上游是否接受中途 system 由 `ModelDefinition.midConversationSystem` 声明，缺省不支持。OpenAI Responses 用 `prompt_cache_key`，无此问题。

### 9.2 compact（自主整理）

- 工具 `compact({ keep: string[]; summary: string; coverUntilSeq?: number })`：模型给出保留要点与总结；库生成 `compaction` 事件；投影层折叠。
- 规则提示（一等交付物）：子任务完成、轨迹收敛时整理；推导中途、卡住时不整理；整理时逐条确认 pins 已包含。
- 兜底：使用率超硬阈值仍未整理 → `threshold` 整理并记事件；连续整理次数上限（默认 3）→ 暂停并告警。
- 只在轮次边界执行；产物追加不改写。
- eval：对照纯阈值，指标 token、完成率、关键信息召回、cache 命中率、死循环计数。
- **实现（B2，`packages/brain/src/compact/`）**：`compact(options): Socket`，工具与规则提示走 Socket 的静态贡献（§7）。工具入参 `{ summary, keep: string[], keepRecentTurns?: number }`——方案里的 `coverUntilSeq` 换成 `keepRecentTurns`，因为模型看不到 seq，能感知的是"最近几个模型轮"。真正的工作在 `afterTool`（需要 TurnContext 里的视图与完整时间线，ToolContext 没有；工具自己 readTimeline 会绕开宿主注册表）：按视图算出 `compaction(decidedBy=model, actor=model, parentId=tool_call)` 经 `ctx.emit` 入日志（顺序 tool_call → compaction → 回执 tool_result），再把结果换成回执。切点规则与投影裁剪同口径：只在模型轮边界切、发起调用所在的模型轮永不折（续跑 pending 时它在视图里，Anthropic 要求 thinking + tool_use 一起回放）、seq 封闭（旧 compaction 的 seq 不小于保留部分最小 seq 时不吸收、留在视图里继续可见；全部折叠时才吸收）。幸存者进 `pinsKept`：kind=pin 的说明、旧 compaction 已保留的事件，以及**被折叠范围内最近的一条用户消息**（真模型实测里模型把"先整理，然后做 X"这条指令折了进去、整理完反问 X 是什么；模型无法复述它还没处理的指令，所以由库保住，长任务里这就是原始任务陈述）。**阈值兜底**不重复实现：core 投影链的 `budgetTruncate` 在估算超过 `contextLimit − reserveTokens` 时机械折叠并记 `compaction(decidedBy=threshold)`，本模块只把它算进连续计数。**连续上限** `maxConsecutive`（缺省 3）：把完整时间线按轮切段（一轮 = 开轮时追加的兜底 compaction / 系统说明 + 模型输出 + 之后到下一轮之前的一切），从最近一轮往前数连续含 compaction 的轮里 compaction 的总数（模型自决 + 阈值兜底合计），加上本轮的，达到上限且本轮仍有工具调用（模型没收尾）→ `onTurnEnd` 返回 `pause(budget)` 交宿主处理。规则提示 `COMPACT_RULES`（英文，只讲经验不下指令）可整体替换或 `rules: false` 由宿主自放。真模型核实见 `spikes/b2-compact-live/`。
- **取回（E3c，`packages/brain/src/compact/recall.ts` + core `projection/manifest.ts`）**：整理丢细节是机制本身的代价（两个模型族都把"复核过的字段值"整理成"状态正常"一句结论，被问到时只能说"没留，可以重查"），与其教模型该留什么，不如让它知道**什么还能拿回来**。core 新增纯函数 `foldedToolResults(removed, { lookup, exclude })` / `renderFoldedToolResults(items, { heading, maxItems=80 })`：把被折叠范围内的每条工具结果列成 `seq N tool(键排序后的入参) — 大小`（外溢过的标 blob id，改用 fetch_blob 读）。compact 的 `planCompaction` 把这份清单接在摘要与要点之后（`compaction.summary` 里，回放逐字一致），范围 = 本次折叠的可见事件 + 被吸收旧摘要盖住的原件（已不在视图里，从完整时间线找，否则一吸收旧摘要清单就丢）；脑子自己的回执（compact / pin / recall / fetch_blob）不列。core 的阈值兜底摘要 `defaultThresholdSummary` 也列同样的清单（措辞不提取回工具，core 不知道它叫什么）。**工具** `recall({ seq })`（随 compact Socket 静态贡献，`recall: false` 可关）读**本会话**日志按 seq 找 tool_result，原样返回内容并加一行说明头；seq 指向非结果事件 / 不存在 / 别的会话一律 isError 说明；原件已外溢的不复述预览而指向 blob。fork 出的会话保留 seq（eval 探针正是 fork），所以摘要里的 seq 在子会话有效。取回的结果是普通 tool_result，超限时由 spill 照常外溢。`manifest: false | { maxItems, exclude }` 调清单，关掉清单时应一并换 `rules`。规则提示与工具说明相应改写：折叠的东西"不是没了，是列在摘要下面、按 seq 能拿回"。pins 顺带把单条上限写进工具说明与 schema `maxLength`（`pinToolDescription(max)` / `pinInputSchema(max)`），模型下笔前就知道 500 字符与分条办法（E3b 里 Sonnet 5/9 格撞上限后重试）；校验按声明值加两成容差（`overshootTolerance` 缺省 0.2）——写明上限后 Sonnet 仍写出 502 / 545 / 558 字符，模型数不准自己的字数，差几个字不该白费一轮。

### 9.3 pins（幸存契约）

- 宿主声明 `pins: PinSpec[]`（静态文本或从事件中抽取的函数，如"最近一条用户明确约束"）；模型也可通过 `pin({ text })` 工具自行钉住。
- 折叠后由投影重注入；`compaction` 事件记录 `pinsKept`。
- eval：治理衰减测试（压缩前后约束违规率），目标 0% 不上升。
- **实现（B3，`packages/brain/src/pins/`）**：幸存契约本身在 core 投影链（T6：kind=pin 自动幸存、`pinsKept`、重排到摘要之后），本模块 `pins(options): Socket` 只让契约有人用。**宿主声明** `pins: PinSpec[]`——字符串（静态文本，名字即文本）、`{ name, text }`（命名静态文本）、`{ name, extract(ctx) }`（每轮从 TurnContext 抽取，返回 undefined 表示"没新说法、上一条继续生效"）；`beforeModel` 算出每条文字，与模型**当前可见**的同名（`meta.pin.spec`）pin 逐字比对，相同不动，不同（首轮 / 内容变了 / 被折掉了）就 emit 一条 `system_note(kind=pin, actor=system)` 追加在末尾，`supersedes` 指向该 spec 在完整时间线里的上一条；永不返回补丁（§9.1 只追加不改前缀）。**模型工具** `pin({ text, replaces? })`（静态贡献 + 规则提示 `PIN_RULES`，可 `tool: false` 只留宿主 pin、`rules` 替换或关闭）：工作在 `afterTool`（要视图判重、找被替换者），emit `system_note(kind=pin, actor=model, parentId=tool_call)` 再换回执；同文已钉不重复；`replaces` 须逐字匹配**模型自己钉的**（`meta.pin.source="model"`），指向宿主 pin 报错——宿主的约束模型动不了；单条缺省 ≤500 字。**取代**：core 新增 `SystemNotePayload.supersedes?: string[]`（可选，不升版本），fold / truncate / compact 规划三处幸存判定一致地排除被取代者（`supersededIds(timeline)`），旧值到下一次折叠才消失、折叠前照常可见。没有 unpin：撤销就是钉一条当前有效的说法去替换旧的。13 个 brain 用例 + 4 个 core 用例。

### 9.4 spill（结果外溢）

- `afterTool`：结果超 `maxResultTokens`（默认 16k，E3 由 8k 调高：6k/8k 会把模型必须整读的列表外溢出去再分段取回，平白多两三轮；可按工具配置）→ 写 BlobStore，结果替换为 `{ 摘要（首尾各 N 行）、blobId、取回指引 }`；追加 `tool_result.spilled`。
- 工具 `fetch_blob({ id, range? })` 让模型分段取回。
- 同一 BlobStore 将来挂进代码执行沙箱的文件系统。
- **实现（B4，`packages/brain/src/spill/`）**：`spill(options): Socket`。**外溢在 `afterTool`**：把结果里全部文本片段拼成正文（图片片段不度量、不外溢，原样跟在预览之后），估算超过上限（`Tool.resultPolicy.maxTokens` 优先，否则模块 `maxResultTokens` 缺省 16000，E3 定）→ 全文以 `text/plain; charset=utf-8` 写入 BlobStore，结果替换为一段文本 `[说明：大小、上限、blob id、第一段 fetch 的具体调用] + 首 N 行 + [... 省略量 ...] + 尾 N 行`（`previewLines` 缺省 20，每侧 `previewChars` 缺省 min(2000, 上限/4) 防单行超长），`tool_result.spilled = { blobId, summary }`；日志里仍只有一条 tool_result（afterTool 改草稿，T9）。`resultPolicy.overflow = "truncate"` 的工具不存 blob、只留预览并向模型明说中段不可恢复 —— 这是宿主对该工具的显式选择，不是缺省。**没有 BlobStore 则外溢关闭**：结果原样通过，每个 `spill()` 实例经 `warn`（缺省 console.warn）告警一次；声明 truncate 的工具照常截断。**取回工具** `fetch_blob({ id, start?, end? })` 走静态贡献 + 规则提示 `SPILL_RULES`：范围是**字符**偏移（方案里的 `range?` 落成 start/end 两个字段；不用 `BlobStore.slice`，它按字节切会切坏 UTF-8 多字节字符），单次返回按同一 token 上限裁，头部写明 `characters a–b of total (n lines total)` 与下一段起点；只能读**本会话**的 blob（别的会话的当不存在，不泄露 id 是否存在）、只读文本类 mime；它的输出不再外溢。perception 的"可见外溢结果数"直接数 `spilled` 字段，无需对接。23 个 brain 用例。真模型核实待 B11 dogfood 一并做（工具结果形状与 compact / pins 相同，都是 tool_result 文本，网关实测已覆盖）。

### 9.5 handoff（会话交接）

- 工具 `handoff({ summary, nextSteps, triggerMessage })`：库建新会话、写 `handoff` 事件到旧会话、在新会话首条 append `system_note(kind=host)` 携带 summary 与 pins、返回 `RunResult.handoff`。
- 宿主实现 `onHandoff(from, to)` 重绑对话锚点（聊天窗口、频道等）。
- 通用化 anima switch_session v3：事务性、旧循环优雅退出、新会话自动续接。
- **实现（B5，`packages/brain/src/handoff/`）**：机械部分在 core 循环（T9，见 §7），`handoff(options): Socket` 只让模型能用它。工具 `handoff({ summary, nextSteps, triggerMessage?, reason? })` 走静态贡献 + 规则提示 `HANDOFF_RULES`；工作在 `afterTool`：解析入参、把 `HandoffIntent` 记在本轮（WeakMap<TurnContext>）、结果换成回执（写明带了几条下一步、几条 pin、有没有触发消息）；同轮其余工具照常执行，`onTurnEnd` 才返回 `{ handoff }` —— 交接只在轮次边界发生；同轮第二次调用报错。**开场说明**由模块排版（`composeHandoffNote`：固定引言 + `## Summary` + 编号 `## Next steps`），同一段文字既是旧会话 `handoff.summary` 也是新会话 seq 1。**pin 跟着走**（`carryPins` 缺省 true）：模型当前可见、未被 `supersedes` 取代的 `system_note(kind=pin)`（宿主的与模型的）原样复制成 `HandoffIntent.opening`，保留 actor 与 `meta.pin`、provenance 指回旧事件、不带 supersedes；在新会话里仍是 pin（穿越那边的折叠），pins() 见同名同文的宿主 pin 不再注入。方案里"首条 system_note 携带 summary 与 pins"落成"摘要一条 + pin 各一条"：pin 塞进摘要正文就不再是 pin。**触发消息**缺省取可见的最后一条用户消息（长任务里即原始任务陈述）；`reason` 缺省 `model_decision`。注册顺序：本模块应排在 compact 等可能返回 pause 的 Socket 之前（onTurnEnd 第一个给意见的定）。不带走：外溢 blob（见 §17）、其余历史。10 个 brain 用例 + 1 个 core 用例。

### 9.6 memory（记忆）

- 工具形状对齐 `memory_20250818`：view / create / str_replace / insert / delete / rename，路径限定 `/memories`，防路径穿越。
- 后端 MemoryStore 由宿主实现。
- 提示片段：任务开始先查记忆；只记未来有用的；不记能从代码或文档推出的。
- 模型每次读写留 `memory_op` 事件。
- **实现（B6，`packages/brain/src/memory/`）**：`memory(options): Socket`，没有钩子，只有静态贡献 —— 工具 `memory` 与规则提示 `MEMORY_RULES` 都是按 `SocketSetup.memory` 有无算一次的函数，缺 MemoryStore 时两者都不出现并告警一次。工具是**自定义工具**而非 Anthropic 原生 `memory_20250818` 类型（pi-ai 不支持 provider-defined 工具，且要跨模型族），入参 JSON Schema 与六个 command 的字段同名同义（`command / path / view_range / file_text / old_str / new_str / insert_line / insert_text / old_path / new_path`），回执文案对齐官方参考实现，模型已有的习惯直接迁移。三处有意的差异：`create` 对已存在文件是覆盖（工具说明本就写 "creates or overwrites"）并在回执里说明；`view` 超过 `maxViewChars`（缺省 16000）按行截断并提示用 `view_range` 续读；写类操作有单文件上限 `maxFileBytes`（缺省 64 KiB）。**路径限定**（`resolveMemoryPath`）：必须是 `/memories` 或其下，拒绝 `.` / `..` 段、反斜杠、百分号编码、控制字符、段首尾空白，折叠重复斜杠与末尾斜杠得到唯一规范形态；存储层只见规范路径。**目录**在 KV 式 MemoryStore 里是隐含的（有以它为前缀的文件即存在）：列表最多两层深、更深的归并到所在目录并合计大小；`delete` / `rename` 目录递归处理（先写后删，非原子，失败最多留重复不丢）；根目录不能删、不能改名、不能当文件建。**命名空间** `namespace(ctx) → 前缀`：存储键 = 前缀 + 模型可见路径，多用户宿主按 principal 隔离，模型看到的永远是 `/memories`。**留痕**：每次成功操作在 execute 里 `ctx.emit` 一条 `memory_op(actor=model, provenance.ref=toolCallId)`，循环排在 tool_result 之前，投影过滤掉不给模型看；失败（含被拒的穿越）不留，tool_call 入参 + tool_result(isError) 已是审计痕。52 个 brain 用例 + core 1 个 + server 1 个。

### 9.7 approval（审批与权限）

- 工具声明 `needsApproval: boolean | (input, ctx) => Promise<boolean>`；`risk: low|medium|high`。
- Policy 管线：deny → ask → allow，首匹配即定；deny 不可被 allow 覆盖；决策本身是 `approval_*` 事件。
- `ask` → `run_paused` + `RunResult.paused`；恢复见 §6。
- fail-closed：策略求值异常视为 deny。
- 默认：只读工具 allow，写与外部副作用 ask，未知工具 deny。
- **实现（B7，`packages/brain/src/approval/`）**：`approval(options): Socket`，管线在 `beforeTool` 里跑，顺序 **未知工具 → deny 段 → ask 段（规则，再看工具自己的 `needsApproval`）→ allow 段 → `unmatched` 兜底**，每段首匹配即定，段序就是"deny 不可被 allow 覆盖"的落点。规则 `{ id, match(call, ctx), summary?, reason? }`，字符串简写为工具名 glob（id `name:<pattern>`）；`match` 拿到 `TurnContext`，可按 principal 判。`unmatched` 缺省 `"byRisk"`：`risk: low` → allow，medium / high / 未声明 → ask（policyId `approval.risk.<level|undeclared>`），也可一律 ask / deny。三种结论落时间线：**deny** 先 `emit` 一条 `approval_decision(actor=system, approved=false, by=规则 id, reason)` 再返回 `{ block }`，循环把它排在 `tool_result(isError)` 之前，模型看到带策略名的错误；**ask** 返回 `{ defer: { policyId, summary } }`，走 T9/T10 的审批暂停与续跑；**allow** 返回 `"proceed"` 不留事件（tool_call → tool_result 已是完整记录，每次放行都记一条只会撑大日志）。fail-closed：任一规则或 `needsApproval` 函数抛错立即 deny（by 为出错规则、reason 带错误信息、经 `warn` 告警），后面的规则不再看。**位置**：由 Socket 注册顺序决定，建议放在 sockets **末尾**（至少在会 `rewrite` 入参的钩子之后），这样判定的是真正要执行的入参；前面的钩子只可能收紧（block / defer），没有"钩子放行绕过策略"的路径。为此 core 顺带改了 `beforeTool` 链的两条语义（见 §7）并保证每条结果路径先 flush 留痕。HMAC：审批的防伪造已由 T10 的状态签名 + `pendingDigest` 覆盖（客户端只回传 toolCallId + approved，批的必须是暂停时看到的那次调用），本模块不再加第二套签名。规则提示 `APPROVAL_RULES`（run 会暂停等人批、被拒的调用别换个写法再试）。23 个 brain 用例 + core 3 个。

### 9.8 budget（预算）

- 上限：contextTokens、totalTokens、turns、toolCalls、wallMs；任一触顶 → `run_paused(reason=budget)`，模型在触顶前收到 perception 提醒。
- 每轮 append `budget_usage`；宿主可聚合做 per-user 配额（库不做配额）。
- **实现（B8，`packages/brain/src/budget/`）**：`budget({ limits, note? }): Socket`。`onTurnEnd` 对照 `ctx.budget` 查五维（`checkBudget` 纯函数导出）：contextTokens = 最近一次请求真实上下文 `contextTokensOf(lastUsage)`（input + cacheRead + cacheWrite，Anthropic 语义下 input 不含缓存），totalTokens = `tokensSpent`（输入 + 输出），turns、toolCalls、wallMs 直接读；**用量 ≥ 上限**即 `{ pause: { reason: "budget", note } }`，note 列出全部触顶维度进 `Interruption`。**只在模型本轮还要继续（afterModel 看到 tool_call）时拦**：收尾作答的轮循环本来就停，改成暂停只会让宿主续跑一个没事可做的会话。上限是**每次 run** 的：续跑即再批一份，`ctx.budget` 从零计；会话级配额宿主聚合 `budget_usage` 自己做。上限须为正的有限数，构造期校验。注册顺序放 handoff 之后（模型已决定交接就交接）。**core 顺带**：`budget_usage.contextEstimate`（该请求的投影估算，可选字段不升版本）、`TurnContext.budget.lastUsage`（beforeModel 时为上次请求的，从日志最后一条 budget_usage 读，含上次 run；模型调用后为本轮的）、模型调用后更新 `ctx.budget.wallMs`、导出 `contextTokensOf`。与 perception 共用同一个 `BudgetLimits` 形状，宿主传同一份。9 个 brain 用例 + core 1 个。

---

## 10. 工具模型

四个正交维度（不是四种工具类型）：

```ts
interface Tool {
  name: string; description: string; inputSchema: JSONSchema        // 原样给模型；不绑 Zod
  validate?: (input: unknown) => TInput                             // 可选校验/转换，抛错 = 入参不合法（T9：不引 StandardSchema，宿主用任何校验库包一层即可）
  side: "server" | "client" | "sandbox" | "provider"                // 执行位置
  execute?: (input, ctx: ToolContext) => Promise<unknown>          // 缺省 = 转客户端；返回值经 normalizeToolOutput 归一
  lazy?: boolean                                                    // 暴露策略：延迟加载/发现
  allowedCallers?: ("direct" | "code")[]                            // 可被代码模式调用
  toModelOutput?: (out) => ContentPart[]                            // 结果处置：多模态与压缩
  resultPolicy?: { maxTokens?: number; overflow: "truncate" | "spill" }
  needsApproval?: boolean | ((input, ctx) => Promise<boolean>)      // 人机交互
  risk?: "low" | "medium" | "high"
  ui?: { resourceUri: string }                                      // MCP Apps 形态，第三期
}
type ToolSource = inProcess(tools) | mcp({ transport: "stdio" | "http", ... }) | openapi({ spec, operations })
```

- MCP 客户端用 `@modelcontextprotocol/client@2`；stdio 传输只在 Node 可选包中提供（P5）。
- 代码模式是同一批工具的另一种投影（类型 stub + 沙箱绑定），第三期。
- 模型自建能力走进程内注册，不走 MCP（规范禁止工具集按连接变化）。

---

## 11. 降级层（Lowering）

- 接口在 `@reins/core/lowering`（T7）：`capabilities(model)`、`toRequest({ events, tools?, model, systemPrompt? }) → LoweredRequest`、`stream(req, { signal?, onDelta? }) → AsyncGenerator<EventDraft, LoweringOutcome>`。草稿只在内容块完整时产出（签名类回放数据此时才齐），流式增量只经 `onDelta` 给 UI；返回值带停止原因、用量、成本，由循环记成 `budget_usage` / `error`。
- `LoweredRequest.landings` 对每条输入事件记一条落点 `{ kind: exact | lossy | dropped, landing, note }`；`lossesOf(req)` 取非 exact 项。实现导出 `LossMatrix`（api → 事件 type → 可能落点），T8 测试断言实际落点必在声明内、矩阵无死条目。
- 第一版 `PiAiLowering`（`@reins/lowering-pi`）：事件 → pi-ai 三角色 Message[] → 线协议；连续 model 事件合成一条 assistant 消息，来源（replay 的 provider/api/model）不同则分开；pi-ai 类型不出此包，宿主自定义模型用 `ModelDefinition` 描述。
- `system_note` 落点（S1 核实）：Anthropic 支持中途 system 的模型族（Fable 5.x / Mythos 5.x / Opus 5 / Opus 4.8）与全部 OpenAI Responses（developer / system 角色）为 exact；做法是先以带内部标记的 user 消息进 Context，在 pi-ai 公开的 `onPayload` 钩子里改写并归位（Anthropic 规则：紧跟 user、后接 assistant 或收尾、不可为首条）。不支持的模型以 `<system_note kind=…>` 标签包住走 user，记 lossy。`compaction` 一律 user 文本，记 lossy。
- thinking 回放：事件 `replay` 记 `{ provider, api, model, thinkingSignature, redacted? }`（Anthropic 为 signature，OpenAI 为整个 reasoning item 的 JSON）。无签名或来源不同记 lossy。OpenAI Responses 只在请求带 `reasoningEffort`/`reasoningSummary` 时开 reasoning 并返回 `encrypted_content`，`capabilities.thinkingReplay` 按实例的 requestOptions 如实声明。
- `capabilities`：`midConversationSystem`（按模型族）、`thinkingReplay`、`parallelTools`、`taskBudget`（Opus 5 / Fable 5.1）、`images`、`contextWindow`、`maxOutputTokens`。
- **TanStack 路径的翻译**（B10，`@reins/adapter-tanstack-ai` 的 `toModelMessages`，纯函数）：事件 → TanStack `ModelMessage[]`，只有 user / assistant / tool 三角色。同一响应的 thinking + text + tool_call 合成一条 assistant；system_note 以 `<system_note kind=…>` 标签走 user（lossy user-role）、compaction 走 user 文本（lossy）、多段正文合并（lossy merged-text）、thinking 只在签名来自同一 provider/model 时回放否则不下发（dropped）、isError 只落 `ModelMessage.error`（lossy）；落在 tool_call 与 tool_result 之间的说明后移到同批结果之后。矩阵 `TANSTACK_LOSS_MATRIX`，测试断言实际落点必在其中且无死条目。流入方向 `BlockAssembler` 把 AG-UI chunk 拼成完整块，replay 记 `{ provider, api: "tanstack-ai", model, thinkingSignature? }`（与 pi 路径同字段）。
- pi-ai pin `@earendil-works/pi-ai@0.85.1`（原 `@mariozechner/pi-ai` 已 deprecated）；只从 `api/<api>` 与 `providers/<name>.models` 子路径导入，不用 `compat` 入口与 `providers/all`；升级走契约测试（假 fetch 断言请求体与 SSE 译回）。

---

## 12. 服务端与前端

- `@reins/server`（T12）：`createAgentHandler(agent, options?)` 返回 Web 标准 `(Request, ctx?) => Promise<Response>`，Node / Bun / Workers / Deno 同一份代码（Node 与 miniflare/workerd 实测）。
  - **POST** `{ sessionId?, input?, lastSeq?, resume?, decisions? }`：先补发 `(lastSeq, 末尾]`，再起 `runLoop`，实时推每条刚 append 的事件，结束给 `result` 帧（RunResult 四态，paused 含可回传的 state）。缺省新建会话，id 回在 `X-Reins-Session` 头与 `start` 帧。审批续跑 = 同一接口回传 `resume` + `decisions`。
  - **GET** `?sessionId=&lastSeq=`（或 `Last-Event-ID` 头，EventSource 重连自动带）：从 EventLog 补发；本进程正有该会话的 run 在跑则接着实时推到 `result`，否则 `end` 帧。补发不依赖 Redis：SSE 的 `id:` 就是 seq。补发与 resume 预校验读日志都经 `readEvents` / `readTimeline` 升级（用 `definition.registry`，缺省 core 注册表）；预校验的 configHash 用 core 的 `resolveSocketContributions` 并入 Socket 静态贡献后再算，与循环一致（B6）。
  - 流的形状 `start → (event | delta)* → (result | end | error)`。缺省编码器原样推时间线事件：事件帧不带 event 名（落 `onmessage`）、`id` = seq；控制帧带 event 名。`encode` 是按流的工厂 `() => (StreamItem) => SseFrame[]`（编码器可有状态，多流并发按流隔离）。`delta` 只给 UI 用，日志里只有完整内容块。客户端报的 lastSeq 超过日志末尾时钳到末尾，不丢实时事件。
  - 同会话同时只允许一个 run（进程内登记 → 409 `run_in_progress`；跨进程靠 EventLog 的 seq_conflict 兜底）。发起者断开缺省 run 继续跑完（`onDisconnect: "continue"`），可选 `"abort"` → paused(host)。Workers 传 `ctx.waitUntil` 让后台 run 不被回收。
  - `resume` / `decisions` 在开流前先用 core 的 `validateResume` 校验一遍，不过给 409 + 错误码（循环内部照旧再校验，fail-closed 不靠这里）。`principal(request)` 钩子做鉴权（抛 Response 即原样返回）；路由、CORS、node:http 适配交给宿主框架。
- `@reins/ui-agui`（T13）：`mapEvent` 映射表 —— user_message / model_text → `TEXT_MESSAGE_START/CONTENT/END`，model_thinking → `REASONING_START/MESSAGE_START/CONTENT/MESSAGE_END/END`（无正文只有首尾），tool_call → `TOOL_CALL_START/ARGS/END`，tool_result → `TOOL_CALL_RESULT`（isError / spilled 进 metadata），其余 core.* 与 ext.* → `CUSTOM`（name = 事件 type，value = 事件；审批弹窗靠 `core.approval_request`）。每条带 `metadata.reins { seq, eventId, type, dropped? }`。`createAguiEncoder` 把 server 的流翻成 AG-UI：start → `RUN_STARTED`（threadId = 会话）、增量先开消息完整事件补 END、result → `RUN_FINISHED`（paused 为 interrupt outcome，RunResult 含 state 放 result）/ `RUN_ERROR`；`aguiEncoding()` 直接作 `encode`。运行时零依赖，测试用 `@ag-ui/core` 官方 schema 校验。最小页面 `packages/ui-agui/demo/`（无依赖的 HTML + node:http 脚本）。AI SDK UI stream adapter 第二期。
- `reins`（T14）：`createAgent({ model, store, tools?, sockets?, systemPrompt?, secret?, handler? })` → `{ definition, handler, run }`。`model` 是 `BoundModel`（`anthropic(id, { apiKey, baseUrl? })` / `openai(...)` 的返回，或自组 `{ model, lowering }`），`store` 是 `Stores`（`memoryStore()` 或自实现）。`handler` 缺省 AG-UI 编码；`run()` 不经 HTTP 直接跑 runLoop。示例 `examples/minimal/`。
- 长任务：一次请求跑到 `paused | done | handoff` 或宿主时长上限前主动 `paused(reason=host)` 返回 state；宿主用队列或 Workflow 续跑。库不自带调度器，只暴露 `runLoop({ resume })`。

---

## 13. eval harness

- fixture = 一段事件日志 + 任务目标 + 评分器（确定性或 LLM judge，judge 与执行分离）。
- 对照组：无脑子、纯阈值压缩、模型自决。
- 指标：任务完成率、总 token、关键信息召回（预埋事实问答）、治理衰减（预埋约束的违规率）、连续整理次数、cache 命中率（从 provider usage 读）、墙钟。
- CI 门禁：P8 规定的默认开启条件。
- 第一批任务来自投放工具真实长任务脱敏版。
- **实现（E1，`packages/eval/`）**：`@reins/eval` 只依赖 core。`EvalFixture = { task: { input, systemPrompt?, seed? }, tools, facts?, constraints?, completion, approve?, contextWindow?, maxTurns?, maxResumes? }`；`EvalArm = { name, sockets?, projection?, systemPrompt? }` 只是 LoopConfig 的一部分，内置 `noneArm()`（visibilityFilter + fold + pins，无 budgetTruncate）与 `thresholdArm()`（core 缺省链），"模型自决"臂由调用方用 @reins/brain 组。`recordedTools(recording, { specs, fallback, sequence, only })` 把录像里配对的 tool_call → tool_result 变成确定性工具（同名 + 键排序后入参逐字匹配；同参多次按序轮给；没匹配上依次试 fallback → sequence → isError 说明；外溢过的结果只剩预览，`stats.spilled` 记数）。`runEval` 每格新建存储与会话（缺省内存），审批代答（`fixture.approve`，缺省全批，决策 by=`eval`）、预算暂停按 `maxResumes` 续跑、handoff 跟到新会话、客户端工具 / host / error 即停并如实记 status；探针问答在**从主会话末尾 fork 出的会话**里做（不污染主会话、探针之间互不影响），去掉宿主工具、保留脑子工具（fetch_blob 取回外溢结果算脑子的本事），系统提示追加"只凭已知作答"，用量另记为 `probeTokens`。指标是时间线纯函数（`measureTimeline`）：轮 = 一段连续模型输出（`isModelOutput`：thinking / text / tool_call）+ 到下一段输出之前的一切，第一轮前的事件归第一轮；连续整理数 = 连续含 compaction 的轮里 compaction 的总数取最大；治理衰减以第一条 compaction 的**位置**（数组序，跨会话拼接时 seq 会重置）分前后两窗；重复调用按同名 + canonicalArgs 第二次起计。`checkGate` 四条：候选 token ≤ 基线 × 比例（缺省 1）、完成度 ≥、召回 ≥（双方都无预埋事实视为通过）、候选自身整理后违规率 ≤ 整理前。`renderReport` 出 Markdown（臂表 + 门禁表 + 可选明细）。文件读写、CLI、真模型对照脚本在 E2/E3 的 `examples/eval`。判定"轮"的口径与 core `replayTurns` 一致（E1 把后者从按 actor 改为按输出类型，见 DECISIONS）。
- **首批 fixture（E2，`examples/eval/fixtures/adrate-patrol/`）**：来源是 B11 真实录像 `examples/adrate/recordings/patrol-disable.jsonl`。`@reins/eval` 新增 `recording.ts`（纯函数）：`unspillRecording` 把 spill 外溢的结果从模型随后 `fetch_blob` 取回的分片按字符偏移拼回全文（拼不齐的原样保留并报告，不硬凑）并删掉 fetch_blob 对 —— fixture 的世界必须完整，否则无脑子臂只拿到预览加一个取不回的 id；`scrubEvents` 在每条事件的 JSON 文本层按"原文 → 别名"表逐字替换（长的先换，引号 / 反斜杠按 JSON 写法匹配），入参、结果、正文、思考里同一个值一起变，回放一致，返回每条规则的命中数（不含原文）；`aliasTable` / `matchStrings` / `jsonValuesAt` 收集待换的值。`build.ts` 把这两步接起来并自查（原值一个不剩、每条规则都命中、无邮箱 / URL 残留），产出 `recording.jsonl`（229 事件）与 `tools.json`（dogfood 里同一张工具表的声明）。`fixture.ts` 从录像里长出世界（102 条计划、102 行报表、14 条候选、14 条复核、14 条成功 Command、写限 10/min、任务原话）并装配三个 fixture 共用它：`adrate-patrol-disable`（全流程）、`adrate-patrol-audit`（只读）、`adrate-patrol-resume`（种子 = 真实第一次 run 全部历史，任务 = "工具修好了，继续"）。工具 = `recordedTools` 逐字回放 + `fallback` 补位世界（列表 / 报表按任意 page / pageSize 重分页、任意计划的 get 从列表条目合成、任意已存在计划的 status 写合成 succeeded Command、commands get / resume 按键找回；别的广告主 / 不存在的计划 / 别的日期窗口照样报错），`wait_seconds` 合成不真等；工具层 bug 那一轮的 16 个失败调用不进回放但留在录像与种子里。评分全确定性：完成度 = 0.6 × 停投到位 + 0.4 × 汇总表列全 − 0.5 × 误停（只读版：列全候选、不列非候选、动写工具 = 0）；预埋事实 7 条（候选数、计划名、报表窗口、写限、广告主 id、计划总数、复核时的 secondaryStatus）；约束：只动目标广告主、只对候选发 DISABLE、只读。缺省 `contextWindow` 64k。**顺带改 runner**：指标（token / 轮 / 动作 / 违规）与 finalText 只算本次新追加的事件（`EvalOutcomeDraft.fresh`），种子历史是上一次真实运行的账，不算在这一格头上；完成判定仍能看全链。`RecordedToolSpec` 增 `resultPolicy`，让回放的列表 / 报表在装了 spill 的臂里照样外溢、在无脑子臂里照样全文。
- **对照跑数（E3，`examples/eval/run.ts` / `report.ts`，结果 `examples/eval/results/`）**：臂 = none / threshold / brain（dogfood 同款）/ brain-lean（外溢 16k、无 memory / handoff）；`run.ts` 一格一落盘（指标 JSON、全链时间线 JSONL、探针事件 JSONL），按臂拆进程并行，`--repeat-start` 补跑失败格；`report.ts --rescore` 用落盘时间线按当前评分器重算完成度、按探针记录重判召回。指标补两项：**计费等价 token**（缓存读 0.1×，只进报告不进门禁）与探针 `answerFrom`（正文为空退回最后一段 thinking）。2026-09-09 DeepSeek v4 flash 四轮 99 格的结论：spill 缺省 16k；compact / memory / handoff 不默认开；perception / pins / budget / approval 推荐默认；阈值折叠继续兜底；门槛 2 当时未达成（召回低 3–6 点，机理是模型看到整理机制的说明就认定旧细节已折叠而拒答）。**E3c（2026-09-10）**：整理摘要附被折叠工具结果清单 + `recall` 取回（见 §9.2），新增 `compact-only` 臂；两族复测召回 100%、候选 token 比阈值基线少 7%（DeepSeek）/ 22%（Sonnet 第二轮，第一轮 +1.8% 未过：审批分批与 pin 重试），compact-only 少 29% —— **门槛 2 达成，compact 改为推荐默认**，详见 results README 与 DECISIONS。

---

## 14. 安全默认值

- 所有工具输出与外部内容 `trust=untrusted`，投影时显式标记
- 写与副作用工具默认 ask；未知工具 deny；策略异常 fail-closed
- 内容检测类（注入分类器等）为可插拔 Socket，默认 fail-open 并强制告警
- 记忆与外溢路径严格限定，拒绝穿越
- bypass 模式只在宿主显式传入且建议隔离环境
- 事件日志不可静默修改；schema 升级只读时 upcast

---

## 15. 兼容与版本策略

- 事件 schema：P9
- 公开 API：SemVer；`@reins/core` 与 `@reins/brain` 主版本联动；适配包独立版本并声明支持的上游范围
- 上游 pre-1.0 依赖（pi-ai、@tanstack/ai、@ag-ui/core、@modelcontextprotocol/client）：pin 精确版本，契约测试，破坏性变更只影响对应适配包
- 每个脑子模块声明 `models: { default: on|off, overrides: { "claude-opus-5": ..., ... } }`，模型换代时按族调整不改代码

---

## 16. 里程碑

| 里程碑 | 交付 | 验收 |
| --- | --- | --- |
| M0 骨架（2 周） | core 事件模型、EventLog/Blob/Memory 接口与内存实现、runLoop、PiAiLowering、server handler、AG-UI 映射 | Node/Bun 上一个带工具的 agent 跑通并可回放；Workers 上 handler 跑通。**2026-09-08 完成（1 天）**：Node 上带工具 + 审批的 agent 经网关用 claude-opus-5 跑通并离线回放（T14/T15）；Workers 用 miniflare/workerd 跑通 handler（T12）；**Bun 未实测**（本机未装，留到 M2 门槛 3 的四环境验证）。额外交付：总包 `reins`、`@reins/server`、`@reins/ui-agui`、fork、RunState 签名恢复。全仓 279 用例 |
| M1 脑子 v1（3 周） | perception、compact、pins、spill、handoff、memory、approval、budget；SQLite store；TanStack AI 适配器 | 投放工具接入；两条路径（自带循环 / TanStack 中间件）都能用 |
| M2 数字（2 周） | eval harness、首批 fixture、CI 门禁；文档 | PRD §7 五条门槛全部达成 → 发布 0.1 |
| M3 治理（第二期） | governance 包、pi 与 AI SDK 适配器、Postgres | |
| M4 成长（第三期） | skills、子代理与预算闸、code-mode、ACP、协议草案 | |

时间按我全职写、Boss 评审与 dogfood 反馈估算，实际以 eval 达标为准不以日历为准。

---

## 17. 待核实（进代码前第一批）

- [x] TanStack AI `onConfig` 能否同时替换 providerMessages 与注入 systemPrompts；其 `metadata` store 是否适合存我们的 state 引用 —— 可以；MetadataStore 无默认实现，不用它存状态（S2，DECISIONS 2026-09-08）
- [x] pi-ai 对 Anthropic 中途 system 消息的支持现状；不支持则 `system_note` 降级为 user 角色并在 capabilities 声明 —— API 支持、pi-ai 不支持，用 `onPayload` 改写（S1，`spikes/s1-mid-system`）
- [x] 分档 perception 注入对 Anthropic 与 OpenAI prompt cache 命中率的实测 —— B1 完成（`spikes/b1-perception-cache`，经网关，claude-opus-5 / gpt-5.5）：Anthropic 不注入 91.8%~93.3%（7 次），默认档位注入 92.9%、每轮变档 93.9%，不降；OpenAI 经该网关缓存本身不稳（基线 16.5%），注入后 32.9% / 38.2%，亦不降。前提是断点按 `automatic` 处置（§9.1）；`previous-user` 会低 3~6 个点，断点留在 system 消息上会崩到 20% 以下
- [ ] `@modelcontextprotocol/client@2` 在 Workers 上的可用性（streamable HTTP）
- [x] （B2 实测发现）perception 的上下文使用率按视图正文粗估，不含系统提示、工具表、thinking，真实 input 可高出一倍（声明 3k 窗口时估 50%–70%、实测 4.4k）—— B8 完成：`budget_usage` 记该请求的投影估算 `contextEstimate`，感知用"上次真实上下文 − 当时估算"的固定开销做加法校准（§9.1 实现段）。真模型下的校准效果待 B11 dogfood 时看 `meta.reading.contextOverhead` 与实际 input 的差
- [x] （B2 实测发现）经网关、请求以中途 system（感知说明）收尾时，模型 thinking 里出现"用户只发了一个句号" —— **已确认是网关所为**（`spikes/aireiter-gateway-check`，暗号法 + DeepSeek 直连对照）：aireiter 的 Claude 端点会改写请求，排在最后一条 user 正文之后的一切（末尾中途 system、末尾额外 user、同一条 user 里写在问题后面的文字）都丢，中途 system 在历史中段被换成 user "Continue"；末尾 system 被替换为 Assistant "OK" + Human "."。所以经该网关 perception 说明在注入当轮模型看不到、下一轮才作为历史出现；顶层 system 与历史中段的 user 说明可达。DeepSeek 直连七种落点全部到达（末尾 system 以 `<system-reminder>` 并入前一条 user）
- [x] ~~降级层增加说明落点"前置到最后一条 user 消息开头"供 会改写请求的上游~~ —— **Boss 决定不做**（2026-09-08）：aireiter 只是测试用的中转，不为它改代码；经它跑 Claude 时感知说明晚一轮可见，知道即可
- [ ] 直连官方 Anthropic API 对照（B1 断点处置、中途 system 落点）—— 缺官方 key
- [x] better-sqlite3 与 bun:sqlite 双实现的成本，或先只做 SQLite-wasm —— 选 `node:sqlite` + `bun:sqlite` 共用 SQL 层，Workers 走 Durable Objects（S3）
- [x] AG-UI 对自定义事件（审批、compaction）的前端消费约定 —— 一律 `CUSTOM`，name = 事件 type、value = 事件本身，`metadata.reins` 带 seq；审批弹窗靠 `core.approval_request`，续跑回传 `RUN_FINISHED(interrupt)` 里的 RunResult.state（T13，demo 页面已消费）
- [x] （B5 发现）外溢 blob 按会话隔离 —— B9 定：`fetch_blob` 授权从"blob.meta.sessionId 等于当前会话"改为"**本会话时间线引用过**（某条 tool_result.spilled.blobId 指向它）"，读本会话自己的日志即可判定、不看别的会话。fork 复制了含 spilled.blobId 的 tool_result（blob 归属父会话、字节不复制），改后 fork 的会话能取回；handoff 不复制 tool_result，新会话仍读不到外溢结果（规则提示已告知"把要紧的写进摘要"），符合预期；未被引用的 id 一律当不存在，不泄露。`packages/brain/src/spill/spill.ts` 的 `referencedHere`
- [x] （B11 dogfood，2026-09-08）B4 spill / B7 approval / B8 perception 校准的真模型核实：DeepSeek 直连跑 AdRate"巡检降本"239 事件 / 57 工具 / 29 审批。spill：报表与计划列表（7k / 10k token）外溢，模型用 fetch_blob 三段取回全文并正确分页；approval：29 次审批暂停 / 续跑全部按预期，拒绝与拦截对模型只是 tool_result 文本；perception 校准：固定开销（系统提示 + 32 工具表）约 35k token，前两轮 21k、之后稳定 35k，读数全程 <50% 档与真实 100k/200k 一致。**新发现**：aireiter 网关对带工具结果的多轮请求两次掐断（单轮 60k 字符能过），dogfood 改 DeepSeek；AdRate 服务端 schema 与 CLI 参数不一致（`--status` vs `--set`）—— **09-09 AdRate 已修复发布**，重新 sync 后一致；写路径冒烟对真服务 ok
- [x] （E3 发现）装了整理机制后 DeepSeek v4 flash 把仍在视野里的早期工具结果当成"已被折叠"而拒答（接续版 2/3 格），感知说明明说折叠范围只部分缓解 —— **E3b 换 claude-sonnet-5 复测**：同一条事实 6/6 丢，但 Sonnet 是真的整理了、摘要没留字段值。两族一起指向机制代价（模型自决整理丢细粒度字段），转入 E3c
- [x] （E3b 发现）Sonnet 5 频繁把长清单塞进 pin，撞 500 字符上限被拒后重试（5/9 格）—— E3c 定：上限不放宽（pin 是永久占用），写进工具说明与 schema `maxLength`，并说明分条办法
- [ ] （E3 发现）DeepSeek Anthropic 端口 99 次运行 3 次中途 "Connection error"，runLoop 无重试直接记 error —— 加有限重试（见 TASKS）
- [ ] （E3 发现）DeepSeek 短答案约 9% 整个落在 thinking 块、正文为空、输出 1 token —— 是端口把文本当 thinking 还是 pi-ai 解析问题，待直连官方 Anthropic 对照
- [ ] （B10）TanStack 路径只用脚本化适配器跑过真实 `chat()` 引擎；真适配器（`@tanstack/ai-anthropic`）+ 真模型下 `providerMessages` 的角色序列（连续 user、tool 紧跟 assistant、`thinking` 签名回放、`error` 字段是否传给模型）待 B11 dogfood 时核实；TanStack 原生审批的客户端续跑（UIMessage parts `approval-responded`）未在本仓测试
- [x] 投放工具第一批长任务的脱敏与 fixture 化方案 —— E2 完成（2026-09-09）：`examples/eval/fixtures/adrate-patrol`，见 §13 实现段（E2）

---

## 18. 待 Boss 决策

见 `docs/DECISIONS.md`。第一期节奏 M0~M2 约 7 周已采纳。
