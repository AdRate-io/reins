# 未推送 24 commit 审查报告（0.2 批次推送前）

> 日期：2026-09-15
> 审查人：AGENTS（六路分域子代理并行 + 主审过滤）
> 范围：origin/main..HEAD 共 24 个 commit（19e9820…071f53b），238 个文件，+22216 / −641 行——D1 lazy-tools、D2 onEvent、D3 approval TTL、D4 跨进程 run 登记、D5 脱敏配方、F0～F4 lowering-fetch、L1 原生延迟加载、i18n 英文化、四环境验证、TikTok MCP auth
> 标准：只报正常用户经官方支持链路可复现且有实质影响的问题；安全 / 资损 / 数据丢失 / 权限绕过即使概率低也报，但注明概率、可恢复性与现有人工控制。子审查尽量找全，主审按概率与控制能力过滤。
> 硬证据：`pnpm check` 全绿（typecheck + lint + 69 个测试文件 1224 用例）；`pnpm check:dist` 17 个入口全部可 import、`node:*` 只在 `/node` 子路径、REINS_VERSION 与包版本一致。

## 一句话结论

**代码面可以推送：五路 51 项硬约束逐条对代码核对全部成立，无一条 P1 / P2 级代码缺陷，公开性红线零发现。** 唯一"必须修复"是两行 README 残留（"untested" 与四环境验证结论自相矛盾，推送公开后即误导装机选择），另有五条执行检查项（文案 / 文档收尾漏网，均不影响行为），建议一并改完再推。

## 必须修复（1 项）

### 1. README "untested" 残留，与 spike 结论、TASKS 结论行三重矛盾

- 位置：`README.md:18`（"…Bun / Deno / Vercel Edge untested."）、`packages/core/README.md:3`（"…not yet tested."）
- 证据：根 README 第 7 行与 224-229 行已改为 "verified on … Bun, Deno and Vercel's Edge Runtime"；`docs/TASKS.md:10` 状态行明确写"根 README 不再写 'not yet tested'"。两处残留由 c16e3ea（早于四环境验证）写入，894594d / 60887e4 更新同文件其他位置及四包 Runtime 段时漏掉。core 主入口在 runtime-matrix 矩阵覆盖范围内，这两句是过期的。
- 影响：公开仓库 README 内部自相矛盾的运行时支持声明，误导装机选择；已勾任务结论行与实际不符。
- 动作：两行删改即可。注意 `packages/server/README.md:3` 的 "not yet tested" 是准确的（server 确实不在矩阵内），不要顺手改错。

## 执行检查项（5 项）

均不影响行为，建议随下一个文案 / 文档 commit 一并清掉。

| # | 事项 | 位置 | 说明 |
| --- | --- | --- | --- |
| 2 | Responses 线 `replay.phase` 写而不读 | `lowering-fetch/src/responses/from-stream.ts:209`（存）；`to-request.ts:139-154`（无读者） | fetch 版自产的 message phase 标记回放丢失，pi 版 JSON 签名反而能带回。厂商不校验、不会 400，F3 双臂真模型全通。修法二选一：`textItemId` 补读 `replay.phase`，或 from-stream 别存 |
| 3 | 英文化字符级残留：一处中文破折号 + 三处全角分号 | `anthropic/from-stream.ts:300`（refusal errorMessage 的 `——` 连接符）；`chat/to-request.ts:83`、`anthropic/to-request.ts:135`、`responses/to-request.ts:100`（note 拼接 `join("；")`） | 运行时文案硬约束的漏网：宿主 / 模型会看到中文标点混在英文句子里。纯文案，无功能影响 |
| 4 | capabilities.ts 两条中文注释被英文化误替换，语法破碎 | `lowering-fetch/src/capabilities.ts:8、:51` | "带正文的中途 system 消息支持" 等被机器式替换成中英碎片（如 "Anthropic a mid-conversation system message carrying the text支持"）。违反"注释一律中文"与 14345eb 自身声明（JSDoc 保持中文）。改回中文即可 |
| 5 | 网关配方 README 与配方测试 allow 段漂移，但双方注释都声称"逐字一致" | `brain/src/gateway-tool.recipe.test.ts:26` 注释 vs `tools-mcp/README.md` Gateway 节 | README 版 allow 段比测试多 `"tool_get"`（测试 fixture 工具表里本就没有这件，属合理省略）。要么补齐两边，要么把"逐字一致"改成"语义一致" |
| 6 | tools-mcp README "not tested directly" 滞后于 P8 补测结论 | `tools-mcp/README.md:189` | CLAUDE.md 与 TASKS 已记录"官方 Anthropic 经 CF 网关 2026-09-15 补测两变体接受（P8）"。该行来自更早的已推送 commit，非本次引入，顺手更新 |

## 知情项（10 项）

不阻断推送。均为已记录的设计取舍、低概率边界或维护备忘。

1. **lazy-tools 原生路径 `inView` 对宿主自定义折叠型 Socket 顺序敏感**（`lazy-tools.ts:250-251`）：宿主自定义 beforeModel 折叠 Socket 且排在 lazyTools 之后时，被折出视图的已取回工具会被错标 deferred；brain 自带折叠（compact / budgetTruncate）全在投影阶段、不受影响；直接调用仍会执行、重新 `tool_find` 即恢复。
2. **`McpAuth.onUnauthorized()` 丢弃 SDK 的 UnauthorizedContext**（`tools-mcp/src/http.ts:46`）：刻意不暴露 SDK 类型的结果（DECISIONS 定形），README 也明说 OAuth 流程不封装；要做高级 OAuth 挑战处理的宿主自行绕道。
3. **D3 "批准到达时刻" = 续跑 append 批准事件的 `at`，非人工点批时刻**（`run-loop.ts:209-220`）：宿主批了隔很久才 resume 时，这段延迟不计入 age。DECISIONS 已明文认可；在意就批准后尽快续跑。
4. **网关配方操作名后缀白名单不保证覆盖 TikTok 330+ 注册表操作**：结构上 fail-closed——未命中任何段落的调用落到缺省 byRisk 判 ask，最坏"多问一次"而非"放跑写操作"（配方测试第 4 例锁死）。接入时按 `tool_list` 清单核对后缀覆盖。
5. **跨实例 "result 帧后立刻 POST" 有一个 DB 往返量级的 409 窗口**（`server/src/runs.ts:122-172` + `store-pg/src/run-lease.ts:23-34`）：release 的 DELETE 在途时，接手实例 acquire 见行仍在 → 409 lease。409 本就是"稍后再试"语义；同实例因 acquire 对同一 owner 幂等而无此窗口。
6. **丢租约到 abort 的窗口内两实例可能并发写同一会话**（`leased-runs.ts:61-80`，最多差一个心跳周期 ttl/3）：DECISIONS D4 明文"丢租约 abort 是尽量少浪费，不是正确性所依赖；seq_conflict 仍是最后一道闸"。后写者 append 撞 `seq_conflict` 进 error 态，日志不坏。
7. **`InMemoryRunLease.holderOf` 挂在 core 生产类上**（`core/src/store/in-memory.ts:165-168`）：测试观察用，注释已写明，不进行为契约。
8. **`tool_reference` 指向"表内但不带 `defer_loading` 的工具"的厂商行为未实证**：只在全表延迟回退 + 历史恰有指向其中工具的引用这一串联场景下出现，lazy-tools 正常链路（菜单工具持续 defer、表整段不变）不达。
9. **构造期校验措辞轻微不统一**：部分校验无 "got …" 尾巴（`mcp-tools.ts:27`、`spill.ts:104-117`、`memory.ts:61`、`pins.ts:133`），数值符号 `>=` 与 `≥` 混用。纯措辞，不构成回潮。
10. **测试 helper 的 unreachable 断言保留中文**（如 `as-tool.test.ts:355` "期望 paused"）：读者是维护者，符合"测试保持中文"边界，无需动作。

## 硬约束核对汇总（逐条全部成立）

- **宪法与循环（core）**：EventLog 只 append；`append` 全包唯一分配 seq、循环外 append 均同步 lastSeq；`tools_bound` 起步事件计入新测试断言；被改动的旧测试均为类型收紧（`LoweringCapabilities.deferredTools` 必填字段）的正当适配，无"改到过"。
- **lazy-tools（D1 / L1）**："已取回"集合从时间线重建、无内存状态，跨 run / 换进程对得上；未取回直调被 block 指向 `tool_find` 而非"未知工具"；原生路径 beforeTool 只看本轮被藏名单；`renderToolReference` 全仓唯 core 一份（10 处引用全部 import）；菜单只收宿主工具、按 Tool 对象同一性；Socket 贡献的 lazy 工具不进菜单也不藏。
- **降级层 fetch 版（F1～F4 + L1）**：零依赖零 `node:*`（grep 全包）；`payload.body` 即请求体无第二跳（测试断言逐字相等）；DeepSeek `reasoning_content` 方言"字段必在"落实；HttpError 文案 SDK 同款且 core 瞬断判据原样适用（429 / 503 重试、400 带 timeout 字样不重试、宿主 AbortError 不重试）；`auth:"none"` 不带凭证头；三线有损全在矩阵且无死条目；L1 四条厂商规矩全落 encoder；`tool_reference` 只给 system 信任结果；缓存断点三处 + 封顶 4 + 不落 system 消息；thinking / reasoning 回放判据分别为签名与 encrypted_content，原件回放、别家与无签名一律 dropped 不降正文；`store:false` 强制、`previous_response_id` 剥掉、两个 id 的回放规则与 CLAUDE.md 逐字吻合。
- **服务端与存储（D2 / D4）**：onEvent 只观测 live（补发不调）、先广播再调不挡 run、收尾等整条观测链（result 帧 / done 排在链后）、钩子抛错只 warn 一次不进日志；RunRegistry 删项同步生效、`waitUntil(run.done)` 覆盖 release；租约三条单语句、过期只比库 `now()`、acquire 对同一 owner 幂等、release 按 (session, owner) 删不掉接手者的行；鉴权 fail-closed 未被新钩子 / 租约绕过（鉴权拒绝时根本不去 acquire）；内存版路径无回归（33 个既有用例全绿，删项只提前不推迟）。
- **审批与 MCP 安全（D3 / TikTok auth）**：过期批准以 deny + block 真落地（`executed` 为空、留痕先于 tool_result，端到端断言）；allow 不看 ttl、未批 pending 不过期、allow 仍零事件；时钟倒挂判不过期（宽松方向）；auth 口子是出站凭证供给不是访问控制边界，没配就不加头、服务器 401 如实报错，无静默放行；凭证不进任何事件 / 告警 / 日志（label 只留 origin + pathname，query 里的 token 剥掉）；模型侧名字改写 `^[A-Za-z0-9_-]{1,64}$` 未被触碰；网关服务器无 annotations → 缺省 byRisk 判 ask，反面用例真实断言。
- **i18n 与公开性**：519 处文案英文化抽验（11 个包逐行读 diff）全部符合"运行时英文、注释中文"边界；测试断言逐字同步非放宽；回潮扫描零残留（除执行检查项 3 的字符级四处）；17 个 changeset 与 9 个 feat commit 完整对齐、定级合理（新包 minor 首发、breaking 均已声明）；全量 diff 凭证模式扫描零命中——spike 全用显式假数据或 gitignore 的信息文件，TikTok 归档 advertiser_id / name 已打码，Vercel 部署凭证经 `-e` 注入不落盘；README 运行时表述与四环境结论一致（除必须修复项 1 的两处）。

## 验证记录

- `pnpm check`：typecheck + lint + 69 个测试文件 1224 用例全绿（3.81s）。
- `pnpm check:dist`：17 入口全部可 import；`node:*` 只在 brain / node（fs/promises、path）与 store-sqlite / node（sqlite）；REINS_VERSION 与包版本一致。
- 子代理分域实跑（全部通过）：approval 29/29、tools-mcp http 3/3、gateway 配方 4/4、server handler 33、leased-runs 7、跨包 PGlite 租约 1、create-agent 5、redaction 配方 3、lowering-fetch 共用层 + Chat 130、Anthropic + Responses 271、lazy-tools 相关 65。
- 需真 Postgres 的 `pg.test.ts` 未跑（无本地实例），其新增并发 / 库时间用例为常规 conformance 扩展。

## 审查方法

六路分域并行：① core + brain lazy-tools；② lowering-fetch 共用层 + Chat 线；③ Anthropic + Responses 线（含 L1）；④ server D2 / D4；⑤ approval TTL + MCP 网关 auth（安全重点）；⑥ i18n + 公开性 + 文档一致性。每路带 CLAUDE.md 对应风险清单逐条核对；主审跑全量 `pnpm check` + `check:dist`，对子代理 finding 按"正常官方路径可信复现 + 实质影响"过滤后定级。子代理排除的理论边缘场景（spill 外溢引用段、TanStack 路径失效疑虑、全表延迟 400、Socket 同名 `tool_find`、`userContent([])` 手工构造、`findBuiltin` 缺省路由等十余条）均经验证不构成问题，不入正文。
