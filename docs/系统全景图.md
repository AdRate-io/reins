# reins 系统全景图

> 系统现状的全貌与索引（2026-09-10 建立）。CLAUDE.md 放精简警示，这里放全貌；每个包的代码级细节见 `模块盘点/`。
> 活文档：包、事件类型、钩子、缺省开关变了必须同步。规格与"为什么"见 `技术方案.md` 对应 §。

## 1. 定位一句话

可嵌入的 Agent 库：装上、配自己的模型，得到一辆有驾驭经验的车。两条宪法——决策权默认在模型；时间线是唯一真源、角色只是翻译。不是框架、不是运行时、不是应用、不是平台（PRD §3）。

## 2. 包全景与依赖方向

```
                    reins（总包：createAgent + re-export）
                   /            |             \
        @reins/server    @reins/ui-agui    （用户自己的传输层）
              |                |
              +-------- @reins/core --------+---------------------------+
              |            |       |         |              |            |
       @reins/brain  @reins/lowering-pi  @reins/eval  @reins/store-sqlite  @reins/store-pg
       （八个 Socket）  （pi-ai，唯一外部依赖）            （node:sqlite / bun:sqlite）（pg / PGlite）
                                              @reins/adapter-tanstack-ai（@tanstack/ai@0.53.0，脑子装在别人的底盘上）
```

| 包 | 一句话 | 外部依赖 | 行数（src） | 盘点 |
| --- | --- | --- | --- | --- |
| `@reins/core` | 事件时间线、Store 接口与内存实现、投影、`runLoop`、Run 状态、Socket 契约、回放、一致性测试套件 | 无 | ~7.3k | `模块盘点/core.md` |
| `@reins/brain` | perception / compact / pins / spill / handoff / memory / approval / budget，每个是 `xxx(options): Socket` | 无（仅 core） | ~7.5k | `模块盘点/brain.md` |
| `@reins/lowering-pi` | 事件 ⇄ 两条协议（Anthropic Messages、OpenAI Responses），中途 system 归位、有损声明 | `@earendil-works/pi-ai`（pin 精确版本，类型不出本包） | ~2.3k | `模块盘点/lowering-pi.md` |
| `@reins/server` | Web 标准 `(Request) => Response`：POST 起 run、GET 补发、SSE、鉴权钩子、`./node` 适配 | 无 | ~2.1k | `模块盘点/server.md` |
| `@reins/ui-agui` | 时间线事件 → AG-UI 协议事件（server 的缺省编码器） | 无 | ~0.9k | `模块盘点/ui-agui.md` |
| `@reins/store-sqlite` / `@reins/store-pg` | EventLog / BlobStore / MemoryStore 的 SQL 实现，驱动由运行时给 | 无（驱动由宿主传入） | ~0.5k 各 | `模块盘点/store.md` |
| `@reins/eval` | fixture、录像回放工具、指标、臂对臂运行器、门禁 | 无（仅 core） | ~2.6k | `模块盘点/eval.md` |
| `@reins/adapter-tanstack-ai` | 脑子作为 TanStack AI ChatMiddleware，日志仍是唯一真源 | `@tanstack/ai`、`@standard-schema/spec` | ~2.9k | `模块盘点/adapter-tanstack-ai.md` |
| `reins` | `createAgent({ model, store, tools?, sockets?, systemPrompt? })` → `{ definition, handler, run }` | 三个内部包 | ~0.2k | `模块盘点/reins.md` |

硬约束：core 与 brain 零 Node 内置依赖；`node:*`、子进程、真实文件系统只在可选包（store-*、server/node、未来的 tools-mcp/node）。

规划中的包（技术方案 §10 / §16）：`@reins/tools-mcp`（0.1，P1）、`@reins/lowering-fetch`（0.1 后）、`@reins/store-do`（Workers Durable Objects，第二期）。

## 3. 数据模型：一条时间线

只有一种数据——带发起者的事件 `Event { id, sessionId, seq, type, schemaVersion, actor, payload, provenance?, trust?, meta? }`。EventLog **只 append**，seq 由调用方分配、日志校验同会话连续（首条 1），不连续抛 `seq_conflict`，这就是乐观并发。读时按注册表 upcast，查不到升级函数即拒绝。

当前 15 种核心事件（`packages/core/src/events/core.ts`）：

| 类别 | 事件 | 模型可见 | 谁写 |
| --- | --- | --- | --- |
| 对话 | `user_message` `model_thinking` `model_text` `tool_call` `tool_result` | 是 | 宿主 / 降级层 / 循环 |
| 说明 | `system_note`（kind = host / pin / …，`supersedes` 取代旧条） | 是 | 宿主 / pins / handoff / compact |
| 整理 | `compaction`（模型自决或阈值兜底；摘要后附被折叠工具结果清单） | 是 | compact / 投影兜底 |
| 运维 | `approval_request` `approval_decision` `run_paused` `run_resumed` `budget_usage` `memory_op` `handoff` `error` | **否**（投影过滤） | 循环 / 模块 |
| 规划 | `tools_bound`（每 run 一条工具表快照，P1） | 否 | 循环 |

`tool_result` 可带 `spilled.blobId`（外溢），`system_note` 可带 `meta.pin`。宿主自定义事件 `ext.*` 走自己的注册表。

## 4. 循环与插座（§7）

`runLoop(config)` 是导出的普通异步生成器：yield 每条刚 append 的事件，返回 `RunResult` 四态 `done | paused | handoff | error`。暂停是返回值不是回调，`paused` 带可序列化、HMAC 签名的 `SerializedRunState`（含 `configHash`，续跑校验，漂移 fail-closed，`allowConfigDrift` 可显式放行）。

一次 run 的骨架：

```
起步：解析静态贡献（Socket.tools / systemPrompt，整 run 不变）→ configHash → 校验 resume / decisions（一条日志不写直到全过）
每轮：beforeModel(补丁) → 投影 → 降级 → 模型（瞬断有限重试）→ afterModel
      → 对每个 tool_call：beforeTool 链（block / defer / rewrite）→ 未知工具 → validate → 审批判定 → 客户端工具转暂停 → execute → afterTool 链
      → endTurn：onTurnEnd 第一个给意见的定（continue / stop / pause / handoff），无意见则"有工具调用就继续"
续跑：补齐 pending 的 tool_call 后也走一次 endTurn（被打断的那一轮到此才算结束）
```

Socket 契约（`packages/core/src/loop/types.ts`）：静态贡献 `tools` / `systemPrompt`（常量或按 `SocketSetup` 算一次的函数，返回 undefined 即不贡献）；钩子 `beforeModel` `afterModel` `beforeTool` `afterTool` `onTurnEnd`。多个 Socket 按注册顺序执行。

投影（§8）：过滤（运维事件不给模型；trust 标注由降级层包裹）→ 折叠（compaction 之前的原件收起，pin 幸存）→ 阈值裁剪兜底。投影是纯函数，回放用同一份。

## 5. 脑子模块（§9）

| 模块 | 钩子 | 给模型的工具 | 缺省（E3c 结论） | 缺什么就自动关 |
| --- | --- | --- | --- | --- |
| perception 感知 | beforeModel | — | 推荐开 | — |
| compact 整理 | beforeModel / afterTool / onTurnEnd | `compact`、`recall({ seq })` | **推荐开**（2026-09-10 起） | — |
| pins 幸存契约 | beforeModel | `pin` | 推荐开 | — |
| spill 外溢 | afterTool | `fetch_blob` | 推荐开，上限 16k | 无 BlobStore → 关 + 告警 |
| handoff 交接 | afterTool / onTurnEnd | `handoff` | 不默认 | — |
| memory 记忆 | （只有静态贡献） | `memory`（memory_20250818 形状） | 不默认 | 无 MemoryStore → 不注册 |
| approval 审批 | beforeTool | — | 推荐开 | — |
| budget 预算 | onTurnEnd | — | 推荐开 | — |

"推荐开"由 `examples/eval` 的对照跑数决定（DeepSeek v4 flash 与 claude-sonnet-5 两族），不是拍脑袋；eval 门槛见 PRD §7、`packages/eval`。

## 6. 存储（§5）

`Stores = { log: EventLog, blobs?: BlobStore, memory?: MemoryStore }`，只有 log 必需。内存实现在 core（`memoryStore()`）；SQLite 与 Postgres 实现各一个包，三份一致性测试套件在 `@reins/core/testing`，两个实现共跑。表：`reins_events` / `reins_blobs` / `reins_memory`（表名可配置是 P2 待办）。

记忆隔离三层（§9.6）：存储实例（哪张表）→ `memory({ namespace })` 前缀（同表内逻辑分区，按角色 / 用户）→ 路径（模型自决）。

## 7. 服务端与前端（§12）

`createAgentHandler(definition, options)`：POST 起 run 或续跑（`input` / `resume` / `decisions`），GET 从 `lastSeq` 补发；SSE 编码缺省 AG-UI（`@reins/ui-agui`），可换原始事件。鉴权两层：`principal(request)` 解析"谁在问"，`authorizeSession({ sessionId, principal, method, isNew })` 决定"能碰哪条会话"，只有显式 `true` 放行，拒绝回 404。`sessionId` 必须是可打印 ASCII 无空格（`/^[\x21-\x7e]+$/`），否则 400。

另一条路径：`@reins/adapter-tanstack-ai` 让 TanStack AI 的循环跑、reins 的日志记、脑子的钩子在 TanStack 的 middleware 钩子里执行。

## 8. eval（§13）

fixture（种子日志 + 任务 + 录像工具 + 预埋事实 / 约束 + 完成判定）× 臂（LoopConfig 片段）→ `runEval` 每格独立存储与会话 → 时间线纯函数指标 → `checkGate` 四条门禁 → Markdown 报告。真实对照跑数与结论在 `examples/eval/results/`。

## 9. 示例与核实脚本

| 目录 | 内容 |
| --- | --- |
| `examples/minimal/` | 五分钟体验：一个模型、两个工具（一个要审批）、内存存储、Web 标准 handler；`replay.ts` 离线回放 |
| `examples/adrate/` | dogfood：投放工具 AdRate 经官方 CLI 接入的真实长任务（录像含真实 id，发布前待处理，见 TASKS） |
| `examples/eval/` | 对照跑数脚本 `run.ts` / `report.ts`、脱敏 fixture、结果报告 |
| `spikes/` | 进代码前与发布前的上游核实脚本，结论在 DECISIONS，上游升级时复跑 |

## 10. 模块盘点索引

| 包 | 文件 |
| --- | --- |
| core | `模块盘点/core.md` |
| brain | `模块盘点/brain.md` |
| lowering-pi | `模块盘点/lowering-pi.md` |
| server | `模块盘点/server.md` |
| ui-agui | `模块盘点/ui-agui.md` |
| store-sqlite / store-pg | `模块盘点/store.md` |
| eval（含 examples/eval） | `模块盘点/eval.md` |
| adapter-tanstack-ai | `模块盘点/adapter-tanstack-ai.md` |
| reins 总包 | `模块盘点/reins.md` |
