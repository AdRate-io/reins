# 决策日志

> 约定：技术与产品决策由技术合伙人做出并记录于此，Boss 随时可读、可否决。只有需要 Boss 本人账号、资金或业务背景的事项才会单独请示。每条含日期、决策、理由、可逆性。

| 日期 | 决策 | 理由 | 可逆性 |
| --- | --- | --- | --- |
| 2026-09-08 | 项目定位采纳 PRD v1.0 两条宪法：决策权默认在模型；时间线是唯一真源 | Boss 反复确认这是理念而非功能；调研证明此立场无库占据 | 低，是根基 |
| 2026-09-08 | 形态为"整车"：自带 pi 大小的循环 + 预装脑子 + 标准插座，脑子可脱离底盘用 | Boss 提出；脑子与底盘共设计才能落到最顺位置 | 中 |
| 2026-09-08 | 命名 **reins**，npm 包 `reins`，scope `@reins/*`（不可用则 `@reinsjs`） | 对应"把缰绳交给模型"；npm 裸名与 GitHub 组织名 2026-09-08 均空闲；备选 `annals` | 高（发布前） |
| 2026-09-08 | 开源协议 MIT | 生态一致（pi、dsh、TanStack、OpenClaw 均 MIT），采纳阻力最低 | 低（发布后） |
| 2026-09-08 | 语言 TypeScript，核心仅 Web 标准 API，运行时不限于 Node | 目标用户与复用生态均为 TS；跨语言走协议不走移植 | 低 |
| 2026-09-08 | 第一个也是第一期唯一的 dogfood 宿主：Boss 的投放工具 | 有真实长任务与真实用户；eval fixture 来源 | 高 |
| 2026-09-08 | eval 模型档位：主用 Claude Opus 5（支持 task budget，便于对照）；成本基线另用一档便宜模型；机制本身不依赖 task budget | 调研核实仅 Opus 5 / Fable 5.1 支持 task budget | 高 |
| 2026-09-08 | 降级层复用 pi-ai，pin 精确版本，隔离在 `@reins/lowering-pi` | 两万四千行协议代码不自养；v0.1 D6 | 中 |
| 2026-09-08 | 实施方式：按 TASKS.md 拆任务包（≤1 天、有验收），先做 4 项核实 spike 再写核心 | 技术方案有待核实项；Boss 需可见的小交付 | 高 |
| 2026-09-08 | 起步只建三个包：`@reins/core`、`@reins/brain`、`@reins/lowering-pi`，其余按需拆 | 避免为拆包付管理成本 | 高 |
| 2026-09-08 | 第一期节奏 M0 骨架 2 周、M1 脑子 3 周、M2 数字 2 周；以 eval 达标为准不以日历为准 | 技术方案 §16 | 高 |
| 2026-09-08 | 代码仓库先建本地 `~/Desktop/reins`，发布前再推远程公开；文档与决策日志留在 `Agent-SDK方案/`，发布时并入仓库 `docs/` | Boss 指示；避免半成品公开 | 高 |
| 2026-09-08 | 旧文档移入 `docs/归档/`，开发期只读 PRD、技术方案、本日志 | Boss 要求避免冗余信息 | 高 |
| 2026-09-08 | 全部文档并入仓库 `docs/`，原 `Agent-SDK方案/` 目录废弃 | Boss 要求不在两个目录间切换 | 高 |
| 2026-09-08 | **S4** pi-ai pin `@earendil-works/pi-ai@0.85.1`。原 `@mariozechner/pi-ai` 停在 0.73.1 并标记 deprecated，仓库迁至 earendil-works/pi。只从 `api/<api>`（流函数）与 `providers/<name>.models`（静态模型表）子路径导入；不用 `compat` 入口（上游注明为临时层、将删除），不用 `providers/all`（拉入 Bedrock 与 AWS SDK）。pi-ai 的 Message/Context/Tool/Model/AssistantMessageEvent 只在 lowering-pi 内部出现 | 主包已迁移；核心路径对 `node:*` 全部用动态可选加载，具备 Workers 可行性（T12 再实测） | 中 |
| 2026-09-08 | **S1** Anthropic 官方已支持带正文的中途 `role:"system"` 消息：Fable 5.1/5、Mythos 5.1/5、Opus 5/4.8，无需 beta 头；Sonnet 5 及更早不支持。摆放规则：不能是首条，必须紧跟 user 轮（含 tool_result 的 user），后接 assistant 或收尾，否则 400；连续 system 视为一组。pi-ai 0.85.1 的 Message 无 system 角色，仅在 `supportsMidConvoEffort` 模型上发 content 为空的 effort 专用 system 消息。**决策**：投影层把 `system_note` 落为带内部标记的 user 消息；lowering-pi 用 pi-ai 公开的 `onPayload` 钩子在支持的模型族上改写为 `role:"system"` 文本消息并按摆放规则归位（紧贴最后一条 user 之后）；不支持的模型保留 user 角色。`capabilities.midConversationSystem` 按模型族声明，有损矩阵记录两种落点 | 实测见 `spikes/s1-mid-system`；用公开钩子不 fork pi-ai | 中：上游若加入 system 角色则删除改写逻辑 |
| 2026-09-08 | **S2** TanStack AI 0.53.0：`onConfig` 返回的 Partial 可同时含 `providerMessages` 与 `systemPrompts`（浅合并；只返回 `messages` 时框架自动同步 `providerMessages`）；init 与每轮 beforeModel 都调用，`config.messages` 是含 tool 结果的完整历史。`ModelMessage.role` 只有 user/assistant/tool，无 system → TanStack 路径下 `system_note` 只能落 user 角色或追加 `systemPrompts`（改动缓存前缀），有损声明。`MetadataStore` 是需中间件在 `setup` 里 `provide` 的命名空间 KV（async get/set/delete），无默认实现 → 适配器**不**用它存 run 状态，状态引用放 EventLog。审批暂停走 `onInterruptBoundary`，预算走 `onShouldContinue`/`onUsage`，模型事件记录走 `onChunk` | 读上游 dist 类型与 compose.js 源码 | 中 |
| 2026-09-08 | **S3** `@reins/store-sqlite` 首版驱动用 `node:sqlite`（Node 22.13+ 免 flag，官方稳定性 1.2 候选发布，本机 22.20 实测可用）；Bun 用 `bun:sqlite`（同为同步 API，SQL 层共用，驱动按运行时选择）；Cloudflare 不做 SQLite 文件包，用 Durable Objects SQLite（`ctx.storage.sql.exec`）单独做 `@reins/store-do`，第二期。**不用** better-sqlite3（原生编译、装机负担）与 sqlite-wasm（Node 下仅内存、无持久化） | 零原生依赖；一份 SQL 三个驱动 | 高 |
| 2026-09-08 | lint 与格式化用 Biome 2.5.12（单一工具、零插件）；提交信息校验用零依赖的 `.githooks/commit-msg`，由 `pnpm install` 的 prepare 自动挂载；不引入 ESLint/Prettier/husky/commitlint | 工具链越少越好，三个月后仍能一眼看懂 | 高 |
| 2026-09-08 | **T3** 事件载荷统一放 `payload` 字段（不平铺到事件顶层）；`trust` 缺省按 actor 推导（tool→untrusted）；`EventSchemaRegistry` 在登记时就校验升级链完整（version=n 必须有 1..n-1 全部 upcaster）；uuidv7 自实现不引依赖；payload 形状在读取时不做运行时校验，只校验壳与版本 | 壳稳定则升级函数只碰 payload，三个月后加字段不会牵连 EventBase；启动即报错优于读到一半才发现；payload 校验交给写入方与工具 inputSchema，避免核心包绑定校验库 | 高（发布前） |
| 2026-09-08 | **T4/T5** EventLog 的 seq 由调用方分配、日志只校验连续性（乐观并发）；fork 保留原事件 id（id 唯一性范围改为"会话内"）；一致性套件不依赖任何测试框架的 expect，只接收 `{ describe, it }`，断言自带；core 增加 `./testing` 子路径导出，tsup 多入口时 DTS 关闭 composite | 存储层不做主，循环层才知道 seq 该是多少；保留 id 才能让 parentId / pinsKept 在分叉会话里继续有效；不绑 vitest 让 Bun、Node 原生测试都能跑套件 | 高（发布前） |
| 2026-09-08 | `@reins/store-pg` 从第二期提前到 M1 B9，与 SQLite 并列；pg 表结构：事件表主键 (session_id, seq)、payload jsonb，唯一约束直接产生 seq_conflict；blob 小对象 bytea、大对象存对象存储只留引用 | Boss 确认投放工具数据库为 pg，dogfood 落 pg 少一层搬运；接口极简，两个后端共用一致性套件 | 高 |
| 2026-09-08 | **T6** 投影是同步纯函数，随机（id）与时间（at）从参数注入；策略新造事件走 `emitted` 由循环 append，seq 按时间线末尾预分配。trust 标注移到降级层（投影不篡改 payload）。折叠摘要放在被覆盖区间的位置，投影顺序允许与 seq 不一致。`system_note(kind=pin)` 默认自动穿越折叠，`pinsKept` 用于钉住其它事件；被多次覆盖须全部保留才幸存。预算裁剪只在轮边界切、不留孤儿 tool_result、要求 seq 封闭；旧摘要原文并入新摘要。默认 token 估算为粗估（宁高勿低），`reserveTokens` 缺省窗口 15% | 纯函数才能单测、回放、eval 对比；"模型可见 ⟺ 已记录"要求新造事件必须落日志；摘要放原段位置读起来顺；核心包零依赖不带 tokenizer | 高（发布前） |
| 2026-09-08 | **T7/T8** 降级层接口进 core（`Lowering` / `LoweredRequest.landings` / `LossMatrix`），实现留在 `@reins/lowering-pi`。模型输出与脑子注入统一为 `EventDraft`（无 id/seq/at/sessionId），由循环补齐后 append；`Lowering.stream` 是返回 `LoweringOutcome`（停止原因、用量、成本）的异步生成器，只在内容块完整时产出草稿，增量走 `onDelta`。每次 toRequest 对每条事件记一条落点（exact / lossy / dropped），矩阵测试断言落点必在声明内。system_note：支持中途 system 的 Anthropic 模型族与全部 OpenAI Responses 落 system/developer（onPayload 改写 + 归位），其余以 `<system_note>` 标签走 user 并记 lossy。compaction 一律 user 文本、记 lossy。模型事件的 `replay` 记 `{provider, api, model, thinkingSignature/textSignature/thoughtSignature}`，来源不同的 model 事件不合并进同一条 assistant 消息。OpenAI Responses 只在请求带 `reasoningEffort`/`reasoningSummary` 时才开 reasoning 并返回 encrypted_content，因此 `capabilities.thinkingReplay` 按实例的 requestOptions 如实声明。本包不读环境变量，key 由宿主 `apiKey(provider)` 提供 | 草稿让"谁分配 seq"只有循环知道；落点逐条记录才能守住 P7；pi-ai 无 system 角色，只能在其公开钩子上改写；OpenAI 的 reasoning 默认关闭是上游行为，静默开启会改变宿主成本 | 中：上游加 system 角色则删改写逻辑 |
| 2026-09-08 | 工程：`tsc -b` 改为只出声明到各包 `.tsc/`，`dist/` 由 tsup 独占；包间引用在 `tsconfig.base.json` 用 `paths` 指向源码、配合 `references` 让 TS 自动改读被引用项目的声明；vitest 用 alias 把 `@reins/*` 指到源码；tsup 打 `.d.ts` 时清空 `paths` 以保留跨包 import | 两个工具往同一个 dist 写，tsup 的 clean 会删掉 tsc 的产物，导致类型解析读到残缺文件（T7 实际踩到） | 高 |
| 2026-09-08 | 聚合网关 aireiter.com 可作为 dogfood 与 eval 的模型来源：三种协议在 reins 依赖的特性上与官方一致（流式形状、工具、用量、thinking/reasoning 回放、中途 system/developer），降级层零改动跑通两家真实往返。但网关不校验历史 thinking 签名 / encrypted_content（伪造也 200），回放可能被静默丢弃，**测回放质量的 eval 须直连官方 API 对照**。GPT 仅 5.4+，`gpt-5.4` 当日不稳，默认用 `gpt-5.5`。降级层来源判定改为只比 provider+api，模型 id 不同只留备注 | 探针脚本 `spikes/gateway-check/probe.mjs` 26 项核实，报告见其 README；响应报告的模型 id 常与请求不同（日期后缀、别名、网关改名），按 id 严格比较会误判为别家模型 | 高 |
| 2026-09-08 | **T9** `runLoop` 落地为 539 行异步生成器，yield 每条刚 append 的事件。**续跑统一机制**：每轮先补齐时间线里没有 tool_result 的 tool_call，审批 / 宿主中止 / 进程死亡 / 客户端工具回填全走这一条路，恢复 = 同一 sessionId 再跑一次。Socket 契约相对方案微调：钩子可不返回（无意见）；`beforeTool` 多传 `tool`；`afterTool` 操作尚未 append 的草稿（返回即替换，外溢不必再追加一条）；`onTurnEnd` 增加 `{ pause: { reason: budget \| host } }`，且第一个给意见的定。循环只两处兜底：`needsApproval` 无 Socket 做主时转审批（policyId `tool.needsApproval`）、`maxTurns` 缺省 100。`RunResult` 四态都带 sessionId 与 lastSeq；`Interruption` 增加 `client_tool`。Tool 的 `inputSchema` 用 JSON Schema 对象 + 可选 `validate` 函数，不引 StandardSchema；带类型入参的工具用 `defineTool()` 擦成 `Tool`。beforeModel 期间 emit 的事件本轮即对模型可见（运维类型除外）。降级层异常与 error 停止都记 `core.error` 后返回 error 态，aborted 记 `paused(host)`。`budget_usage` 由循环每轮 append，`remaining` 留给 B8 | 一条续跑路径比"resume 参数 + 三种恢复分支"简单且天然容错（崩溃也能接上）；afterTool 改草稿避免日志里出现"结果 + 替换结果"两条；needsApproval 兜底是安全默认值（§14），否则工具声明了要审批却被静默执行；StandardSchema 的 JSON Schema 输出尚未标准化，JSON Schema 直给模型最省事 | 中（发布前可调） |
| 2026-09-08 | **T10** 恢复 = `runLoop({ resume, decisions, secret })`。状态新增 `pendingDigest`（pending 调用 id/名/入参的 SHA-256）；签名 HMAC-SHA256（Web Crypto，密钥字符串由宿主给）。校验顺序形状 → 会话 → 签名 → 配置摘要 → 日志长度 → pending 对账 → decisions 指向 pending，任何一步不过抛 `RunStateError` 且一条日志不写；配置变了缺省拒绝，`allowConfigDrift` 放行。`decisions` 可不带 `resume` 单独使用（宿主自己保证会话）。不配 `secret` 则不签名不验签 | 客户端只回传 toolCallId + approved，入参留在服务端日志，HMAC 保护的是状态本身不被伪造、pendingDigest 保护日志与状态对得上；拒绝在写日志前发生才算 fail-closed；配置漂移默认拒绝是因为暂停时模型看到的工具集就是它做决定的依据 | 中 |
| 2026-09-08 | **T11** fork 不做任何"修补"：切在 tool_call 与 tool_result 之间，新会话首轮就把该调用当 pending 重新执行；有副作用的工具请切在轮边界 | 分叉的定义就是"从这里起独立演进"，静默补一个假结果或禁止切点都会让语义变复杂；行为已在测试里钉死并写进注释 | 高 |
| 2026-09-08 | **T12** `@reins/server` 的 SSE 缺省推**原始时间线事件**（`id:` = seq、不带 event 名），AG-UI 只是可插的编码器（`encode: StreamItem → SseFrame[]`，T13 提供）。POST 先补发缺口再起 run（天然不重复）；GET 重连先订阅再补发，按 seq 去重、丢弃补发期间攒下的 delta。同会话同时只允许一个 run：进程内登记表给 409，跨进程靠 EventLog 的 seq_conflict 兜底。发起者断开缺省 run 继续跑完（`onDisconnect: "continue"`），Workers 靠 `ctx.waitUntil`。resume / decisions 在开流前用 core 的 `validateResume` 预校验一遍给 409（循环内仍再校验，fail-closed 不靠这里）。handler 不做路由、CORS、鉴权实现，只暴露 `principal(request)` 钩子（抛 Response 即原样返回）。工程：miniflare pin `4.20260730.0`（5.x 仍 alpha），根 `pnpm.onlyBuiltDependencies` 放行 esbuild 与 workerd | 宪法二：时间线是唯一真源，前端协议只是翻译，server 不该绑死一种；seq 天然是 SSE 的 id，重连补发零状态、不要 Redis；模型的工作不应因用户关掉标签页而丢；4xx 比 200 + error 帧对客户端友好；鉴权与 CORS 是宿主框架的事 | 中（编码器与请求体形状发布前可调） |
| 2026-09-08 | **T13** AG-UI 只做翻译不发明：文本 / 推理 / 工具调用与结果 / run 生命周期用原生事件，reins 特有的（system_note、compaction、审批、预算、暂停恢复、ext.*）一律 `CUSTOM`，name = 事件 type、value = 事件本身。每条翻出的事件带 `metadata.reins = { seq, eventId, type, dropped? }`，图片等进不了正文的内容在 dropped 里声明。流式：文本 / 思考增量先开消息（临时 id），完整事件到达只补 END；tool_args 增量丢弃（缺 id 与工具名）。SSE `id:` 只挂在一个事件翻出的最后一帧。run 四态：done / handoff → `RUN_FINISHED(success)`，paused → `RUN_FINISHED(interrupt)` 且 interrupts 一一对应 Interruption、完整 RunResult（含 state）放 result 供前端回传，error → `RUN_ERROR`。AG-UI 类型手抄为本地类型、`@ag-ui/core` 只作 devDependency 在测试里逐条校验（运行时零依赖，不把 zod 带给用户）。server 的 `encode` 改为按流工厂 `StreamEncoderFactory`；lastSeq 超日志末尾时钳到末尾 | 前端协议是翻译层（宪法二），不该反过来定义事件；有状态编码器在多流并发下必须按流隔离；metadata.reins 让任何 AG-UI 客户端都能对回时间线；客户端记错 lastSeq 不应导致静默丢事件 | 中 |
| 2026-09-08 | **T14** 总包 `reins` 只做 `createAgent` 并原样再导出 core / server / ui-agui，**不**再导出 lowering-pi（带 pi-ai 依赖，按 PRD 单独 import）。`createAgent({ model: BoundModel, store: Stores, ...AgentDefinition, handler? })` 返回 `{ definition, handler, run }`，handler 缺省 AG-UI 编码（PRD：AG-UI 一等输出）。`BoundModel = { model, lowering }` 放 core；`anthropic(id, { apiKey, baseUrl? })` / `openai(...)` 工厂在 lowering-pi，**apiKey 必填不读环境变量**（沿用 T7 决策，由宿主决定来源；PRD §5.1 示例同步改为显式传 key）。`Stores = { log, blobs?, memory? }` + `memoryStore()` 放 core。node:http 适配放 `@reins/server/node` 子路径（唯一允许出现 `node:*` 的位置，主入口不变）；server 包 tsconfig 加 `types: ["node"]` 供 tsup 出声明。示例用 Node 22 原生类型剥离直接跑 `.ts`，不引 tsx | 一个包装完即用，但把 pi-ai 留在可选包才能换降级层；显式 key 让 Workers / Deno 等无 process.env 的运行时同一份代码；SQLite 要等 B9，PRD 示例先用 memoryStore 保证"复制即用"为真 | 中（发布前可调） |
| 2026-09-08 | **T15** 回放做成 core 的纯函数 `replayTurns`（模型轮边界 = model 事件紧跟非 model 事件；第 n 轮可见 = project(该轮首条模型输出之前的全部事件)），不新增任何事件类型、不让循环多写"请求开始"标记；重算若新造事件则标 `diverged`，如实告知"这是按现在的规则算的，不是当时的事实"。录制与回放只做示例脚本 + 静态 HTML，JSONL 一行一事件，不进 core。Socket 用 `patch.events` 整体替换投影的部分声明为不可重算 | 循环本来就在请求前把模型将看到的一切先写进日志，边界已隐含在数据里，再加标记事件就是第二份真源；eval（M2 E1）逐轮对比要的正是这个函数；JSONL 与 HTML 是宿主层的事，core 不带序列化格式与界面 | 高 |
| 2026-09-08 | **T16** 读时 schema 升级统一放在 core store 层的 `readTimeline` / `readEvents`，而不是塞进每个 EventLog 实现或让调用方各自 `registry.read`；循环、server、回放一律经它读，`log.read` 视为"原始字节层"。循环起步整条过一遍注册表，读不出来先抛再说（一条日志不写）。M0 复盘结论：里程碑验收"Node/Bun"中 Bun 未实测，不假装通过，挪到 M2 门槛 3 的四环境验证一并做 | 存储后端会有 SQLite / pg / DO 多个实现，升级逻辑只该有一份；读时升级是 P9 的落地点，M0 里被漏掉说明"接口层写了、调用点没接"这类问题要靠复盘对照抓，不能只看单测绿。Bun 这类环境验证放到能一次验四个的时候做，省得零碎 | 高 |
| 2026-09-08 | **prompt cache 共识**（Boss 提出）：动态信息（上下文余量、预算档位等）绝不放对话前部或系统提示，只作为 `system_note` 追加在时间线末尾；旧说明不删不隐藏；系统提示与工具表每轮稳定；缓存断点复用 pi-ai 现有做法不自造；只有 compaction 允许打掉缓存且须在轮边界由模型自决。细则写入技术方案 §9.1，B1 验收加 cacheRead 占比不下降 | 录制的真实会话里 cacheRead 从 100 涨到 878，说明前缀缓存今天已在生效，任何每轮变化的前缀内容都会把它清零、成本翻倍；把"放哪"写死比只说"少变"更能防止实现时走偏 | 高 |
| 2026-09-08 | **B1** 感知做成 `@reins/brain` 的 **Socket**（`perception()`），不是投影策略：`beforeModel` 只 `emit` 一条 `system_note`、永不返回补丁；core 默认投影链删掉 `perception` 插槽。判重按**渲染后的文字**（模型当前可见的最后一条感知说明相同就不追加），不按读数；折叠盖掉旧说明后重新注入。所有数值先分档再给模型（档位边界可配，非升序构造期即拒绝）；说明文字用英文、只陈述不下指令，规则提示留给 B2 放稳定的系统提示。新增 `SystemNotePayload.meta`（可选，不进模型上下文，给回放 / eval）与 `TurnContext.budget.targetTokens`。**降级层断点处置**：`PiAiLowering.midSystemCacheBreakpoint` 缺省 `automatic`（说明殿后时去掉 pi-ai 打在它上面的块级 `cache_control`，请求顶层补一个 → Anthropic 自动缓存，块级已满 4 个则放弃）；`previous-user` / `drop` 供对照与特殊上游 | 感知的价值在"追加在末尾、不改前缀"，投影是纯函数、每轮重算，做成策略要么每轮新造事件、要么得在链里回看日志，两条路都比 Socket 绕；按文字判重是因为宿主换了 render 后，模型看到的没变就没必要多一条。断点：官方文档明确"最后一个断点之后的内容不缓存"，丢弃会让说明殿后的请求整段历史不缓存；三种处置经网关实测（`spikes/b1-perception-cache`，2~3 次重复）：`automatic` / `drop` 与不注入持平（92.9% / 93.9% vs 91.8%~93.3%），`previous-user` 低 3~6 个点，留在 system 消息上崩到 18.6%。`drop` 在网关上表现好只因网关替我们加了顶层自动缓存，直连官方 API 就不成立，所以缺省 `automatic` 而不是 `drop` | 中（档位边界与文案发布前可调；断点缺省若直连官方 API 实测不同再改） |
| 2026-09-08 | **B1 附带** `ModelDefinition.midConversationSystem?: boolean`：第三方 Anthropic 协议上游是否接受中途 `role:"system"` 由宿主声明，缺省仍按不支持（说明以标签包住走 user）。DeepSeek 的 Anthropic 兼容端口（Boss 提供密钥）实测接受中途 system、块级与顶层 `cache_control`、thinking + 工具，并把自动缓存翻译成 `cache_read_input_tokens`；用它跑 B1 实测，两种落点下注入都不降命中（94.3% → 93.9% / 93.1%；94.5% → 94.2% / 94.1%）。**但它不能替代官方 Anthropic 对照**：缓存全自动、不认断点位置，三种断点处置在它那里无差别，"缺省 automatic 直连官方是否成立"仍是未实测项 | 能力按 id 正则推断只覆盖内置 Anthropic 模型，第三方上游必须有声明入口，否则能用的 exact 落点被迫降成 lossy；缺省不支持是因为发错了是 400、发保守只是有损；DeepSeek 顺带成为 eval / dogfood 的便宜模型来源 | 高 |
| 2026-09-08 | **B2** compact 做成 `@reins/brain` 的 Socket，工具与规则提示走新增的 **Socket 静态贡献** `Socket.tools` / `Socket.systemPrompt`（循环起步并入工具表与系统提示，整个 run 不变、计入 configHash、续跑补齐 pending 时在场；宿主同名工具优先）；`beforeModel` 补丁改为顺序合并（后一个 Socket 在 ctx 里看到前一个改过的）。工具入参用 `keepRecentTurns` 替代方案里的 `coverUntilSeq`（模型看不到 seq）。真正的工作在 `afterTool` 里做（要视图与时间线），工具 execute 只占位。切点：模型轮边界、发起调用所在轮永不折、seq 封闭（旧 compaction 不能吸收就留着可见）。**被折叠范围内最近的一条用户消息缺省幸存**（`keepLatestUserMessage`）。阈值兜底复用 core 的 `budgetTruncate`，不重复实现；连续上限缺省 3，模型自决 + 阈值合计，按"轮"分段计数（开轮时追加的兜底 compaction / 系统说明归入其后那一轮），达到上限且本轮仍有工具调用才 `pause(budget)` | 静态贡献：模块工具若走 beforeModel 补丁，补齐 pending 时（beforeModel 之前）工具不在场会报"未知工具"，且不进 configHash 恢复时察觉不到模块拆装；放 Socket 上又不破坏"Socket 是唯一契约"。最近用户消息幸存：真模型实测（`spikes/b2-compact-live`）模型把"先整理，然后做 X"折进摘要后反问 X 是什么——它复述不了还没处理的指令，这是库该兜的底，不是替模型做决定。分段口径：阈值 compaction 在日志里紧贴下一轮模型输出之前，按模型轮起点分段会把它算进上一轮，连续三轮兜底就永远数不到 3（写测试时撞到）。收尾轮不拦：模型已作答、循环本来就停，再暂停只会把一次正常结束变成 paused | 中（`keepRecentTurns` 语义、缺省 3、幸存规则发布前可调） |
| 2026-09-08 | **B2 附带待核实**：(1) 感知的上下文使用率按视图正文粗估，真实 input（含系统提示、工具表、thinking）可高出一倍（3k 窗口时估 50%–70%、实测 4.4k）→ 感知应改用上一请求 `budget_usage.tokens` 校准，放 B8 一并做；(2) 经网关、请求以中途 system 收尾时，模型 thinking 里出现"用户只发了一个句号"——Boss 提议用 DeepSeek 直连对照，**已确认是 aireiter 所为**（`spikes/aireiter-gateway-check`）：其 Claude 端点会改写请求，最后一条 user 正文之后的一切都丢（末尾中途 system、末尾额外 user、同一条 user 里问题之后的文字），中途 system 在中段被换成 user "Continue"，末尾 system 被换成 Assistant "OK" + Human "."。DeepSeek 直连七种落点全部到达。**决策**：经该网关跑 Claude 时 perception 说明当轮不可见，B1 的"落为中途 system、exact"在该网关不成立，B1/B2 其余结论不变；DeepSeek 直连列为当前唯一可信的 Anthropic 协议上游；**Boss 决定不为网关打补丁**：aireiter 只是测试用中转，不改降级层；经它跑 Claude 时接受感知说明晚一轮可见。后续联网核实以 DeepSeek 直连为主对照 | 都是真模型实测才看得到的问题；不阻塞 B2，但会影响感知的可信度与说明落点的选择；网关是 Boss 的资源，去留与付费属 Boss 决策 | 高 |
| 2026-09-08 | **B3** pins 做成 `@reins/brain` 的 Socket `pins()`：宿主 `PinSpec[]`（静态文本 / 抽取函数）在 `beforeModel` 与视图里可见的同名 pin 按文字判重、只追加不改前缀；模型 `pin({ text, replaces? })` 工具在 `afterTool` 里 emit `system_note(kind=pin, actor=model)`。**取代机制**：core `SystemNotePayload` 新增可选 `supersedes?: string[]`（同 `meta` 先例，不升版本），fold / truncate / compact 规划三处一致：被取代的事件一旦被覆盖就不幸存，不论是否在 `pinsKept` 或为 kind=pin。**模型只能替换自己钉的**，宿主 pin 报错。**不做 unpin**：撤销 = 钉一条当前有效的说法替换旧的。抽取函数返回 undefined = 上一条继续生效，不撤销。幸存与重注入本身沿用 T6 的投影链，模块不重复实现；模块的"折叠后重注入"只是兜底（自动幸存被关掉或第三方 compaction 没列 pin 时补一条到末尾） | 不加取代机制，抽取式 pin 每变一次就永久多一条、模型改口也留两条互相矛盾的 pin，pins 会变成第二个失控的上下文来源；append-only 下唯一的撤销表达就是"被后来者取代"。纯 unpin 需要一条本身不幸存却能让别人不幸存的事件——要么新事件类型（15 种事件 × 有损矩阵 × AG-UI 映射全得动），要么让 kind=pin 的撤销说明自己永久幸存，两者都比"替换"重；先看 eval 里是否真需要。宿主 pin 不许模型动是治理边界：宿主声明的是宿主的约束 | 中（`replaces` 语义、500 字上限、是否补 unpin 发布前可调） |
| 2026-09-08 | **B4** spill 做成 `@reins/brain` 的 Socket `spill()`，外溢在 `afterTool` 而非包装工具的 execute；上限取 `Tool.resultPolicy.maxTokens`，缺省 8000；只度量与外溢文本片段，图片原样保留；`resultPolicy.overflow="truncate"` 才截断且向模型明说不可恢复，缺省一律存 blob；没有 BlobStore 时按 §5 自动关闭 + 告警一次（不退化成截断）；`fetch_blob` 用**字符**偏移 `start / end`（不用 `BlobStore.slice` 的字节切片），单次按同一上限裁，只读本会话的文本类 blob，越权当不存在 | afterTool 让宿主 / MCP / OpenAPI 来源的工具都不必知道 reins（P3），日志只一条 tool_result；缺省存而不截是宪法一（不替模型决定"后面的不重要"）；无 BlobStore 静默截断会在宿主不知情时丢数据，宁可原样通过让感知与预算兜底并告警；字节切片会切坏 UTF-8 多字节字符而模型说的是字符；按会话隔离是 §14 fail-closed 的安全默认值 | 中（发布前可调） |
| 2026-09-08 | **B5** handoff 做成 `@reins/brain` 的 Socket `handoff()`，交接机械部分留在 core 循环（T9）；core 只加 `HandoffIntent.opening`（新会话开场草稿，循环不解释内容）。模型工具 `handoff({ summary, nextSteps, triggerMessage?, reason? })`，意图记在 afterTool、onTurnEnd 才执行（交接只在轮次边界，同轮其他工具照常跑）；开场说明由模块排版且与 `handoff.summary` 同文；可见且未被取代的 pin 复制成新会话的 pin 事件而不是并进摘要；触发消息缺省最近一条用户消息；新会话续跑仍由宿主再起 runLoop（不自动续接）。外溢 blob 不跨会话，记入 §17 待 B9 定 | 循环已有机制，模块只补"模型能用"这一层（宪法一）；pin 作为独立事件才能在新会话继续穿越折叠、也让 pins() 判重生效；`opening` 比让模块直接写新会话日志干净（新会话 id 与 seq 由循环分配）；自动续接需要宿主先重绑锚点（onHandoff），由宿主决定何时起下一轮更稳 | 中（发布前可调） |
| 2026-09-08 | **B6** memory 做成 `@reins/brain` 的 Socket `memory()`：只有静态贡献、没有钩子。工具是**自定义工具**、形状对齐 `memory_20250818`（六个 command 与字段同名同义、回执文案对齐官方参考实现），不用 Anthropic 原生工具类型。`create` 对已存在文件**覆盖**并在回执说明（官方参考实现报错，但工具说明写 "creates or overwrites"，模型按后者行事）。路径先规范化再碰存储（拒 `..` / `.` / 反斜杠 / 百分号编码 / 控制字符 / 段首尾空白，折叠重复斜杠），存储键 = `namespace(ctx)` 前缀 + 规范路径；目录在 KV 里隐含。`memory_op` 只记成功操作，新增可选 `toPath`（rename 目标，不升版本）。**core 顺带**：`Socket.tools` / `systemPrompt` 可为按 `SocketSetup` 算一次的函数（`StaticContribution<T>`），落地 §5"缺 MemoryStore 则不注册"；解析抽成 `resolveSocketContributions` 导出，**修了 server 恢复预校验只用宿主工具算 configHash 的 B2 遗留问题**（装任何带静态工具的模块后续跑都会 409 误判漂移） | pi-ai 不支持 provider-defined 工具、reins 要跨模型族，同名同义 schema 已足够让模型认出熟悉的工具；注册一个一用就报错的工具会让模型（被规则要求先查记忆）每轮撞墙，函数形态静态贡献比 beforeModel 补丁干净且不碰缓存前缀；预校验与循环必须同一份算法否则 hash 必然分叉 | 可逆：原生工具类型可在降级层支持后按模型族切换；`create` 覆盖策略一行可改 |
| 2026-09-08 | **B7** approval 做成 `@reins/brain` 的 Socket `approval()`，管线在 `beforeTool` 里跑：未知工具 → deny 段 → ask 段（规则 + 工具自己的 `needsApproval`）→ allow 段 → `unmatched`（缺省按 `risk`：low 放行，其余先问人）。deny 先 emit `approval_decision(by=规则 id)` 再 `block`；ask 走 `defer`；allow 不留事件。fail-closed 覆盖规则与 `needsApproval` 函数。**位置改为建议放 sockets 末尾**（方案原写"在所有钩子之前"），配套 core 两条语义：`rewrite` 后的入参传给后续钩子；宿主已批准的调用遇到 `defer` 不短路。另修每条结果路径先 flush 留痕。HMAC 不另做，沿用 T10 状态签名 | 复用 Socket 契约（`defer` / `block` 本来就是为此设计），比在 core 加 `LoopConfig.policy` 少一套接口、适配器（B10）也不用额外 plumbing；"放首位"会让后面的 rewrite 绕过按入参写的规则，"放末尾"则前面的钩子只能收紧，配合"已批准的 defer 不短路"，deny 在任何顺序下都拦得住；allow 每次记一条会让日志翻倍而信息量为零；再加一套审批 HMAC 与状态签名保护的是同一件事 | 中（位置建议与 unmatched 缺省都是一行可改） |
| 2026-09-08 | **B8** budget 做成 `@reins/brain` 的 Socket `budget({ limits })`，`onTurnEnd` 查五维、用量 ≥ 上限即 pause(budget)，**只拦模型还要继续的轮**；上限按**每次 run** 计，续跑即再批一份；会话级配额留给宿主聚合 `budget_usage`。contextTokens 按真实用量 `input + cacheRead + cacheWrite`。perception 上下文使用率改为**加法校准**：`budget_usage` 新增 `contextEstimate`，开销 = 上次真实上下文 − 当时估算。core 加 `TurnContext.budget.lastUsage` 与模型调用后更新 `wallMs`；`budget_usage.remaining` 循环不填、预留宿主 | 与 approval 同构（边界都是 brain Socket，核心只记账）；收尾作答的轮再暂停会让宿主续跑一个无事可做的会话；per-run 语义让"暂停 = 找宿主要更多预算"最直白，会话级需求可由宿主减去已用量后传入；加法而非比例校准是因为估算漏掉的是系统提示 / 工具表这类整轮不变的常量，比例会随历史变长把误差放大；估算存进事件才能离线回放重算读数 | 中（校准方式与 per-run 语义都可换，字段是可选的） |
| 2026-09-08 | **B9** `@reins/store-sqlite`（认 `{exec,prepare}`：node:sqlite / bun:sqlite）与 `@reins/store-pg`（认 `query(text,params)→{rows}`：pg / PGlite）。事件整条存 `data`、壳字段抽列、主键 `(session_id,seq)` 做并发闸。pg 的 `append` / `fork` 都是单条语句、不开事务、传连接池即对；`data` 用 **json 不是 jsonb**。测试缺省 PGlite、`REINS_PG_URL` 对真库再跑，三份一致性套件全绿 | 一份 SQL 多个驱动（S3）；单条语句原子性省掉借连接的复杂度，连接池友好；jsonb 会重排键序，让 T10 按 JSON.stringify 算的 pendingDigest / configHash 续跑时误报篡改，json 文本原样存取才与内存实现逐字节一致 | 高（接口稳定，驱动可换） |
| 2026-09-08 | **B9 附**：外溢 blob 的 `fetch_blob` 授权从"meta.sessionId 等于当前会话"改为"**本会话时间线引用过**（tool_result.spilled.blobId）"，只读本会话日志判定 | fork 复制了含 blobId 的 tool_result 却读不回是 B5 留下的洞；按"引用过"授权既让 fork 会话能取、又不复制字节、也不泄露未引用的 id（当不存在）；handoff 不复制结果故仍读不到，与"把要紧的写进摘要"的规则一致；比"fork 时把 blob 归属写进新会话"少一次全量复制 | 中 |
| 2026-09-08 | **B10** TanStack AI 适配器：循环归 TanStack，**日志仍是唯一真源** —— 每轮 `onConfig` 用日志投影产出 `providerMessages`，TanStack 自己的 `messages` 只给客户端 UI 与它的内部对账（pending 调用、审批状态）；客户端历史只在日志为空时整段接管，否则只导入末尾连续的新用户消息 | 宪法二；S2 核实 `onConfig` 可同时改 providerMessages / systemPrompts / tools；两份历史并存时必须有且只有一个真源，否则 compact / spill / pin 的效果会被客户端重发的全文覆盖 | 中（宿主想让客户端历史为真源就不该用本适配器） |
| 2026-09-08 | **B10** reins 动态审批（Socket.beforeTool 返回 defer）在 TanStack 里落成**通用中断** `reinsApprovalInterrupt`（`defineInterrupt`，schema 手写 Standard Schema 不引 zod），在 `beforeTools` 边界发出、`onInterruptResolution` 记 `approval_decision`；宿主须登记到 `chat({ interrupts })`。TanStack 原生 `needsApproval`（静态）保留其自身审批流，只把请求 / 结论镜像进日志（policyId `tanstack.needsApproval`，答复者 `tanstack`） | TanStack 审批只认工具上的静态布尔，动态判定只能在边界以通用中断表达；两套审批并存但日志形状同一，前端与回放不分路径 | 中 |
| 2026-09-08 | **B10** TanStack 路径与默认循环的已知差异（有损声明）：① ModelMessage 无 system 角色，system_note 以 `<system_note>` 标签走 user，compaction 走 user 文本；② thinking 无同源签名不下发；③ defer 让整轮工具都等审批（TanStack 在边界暂停不执行任何调用），默认循环会先执行不需审批的；④ onTurnEnd 的 continue 只在 TanStack 自己也想继续时生效，pause 表现为 run 正常结束 + `run_paused` 事件；⑤ handoff 只做记录与新会话开头，宿主下一次请求改用新 sessionId；⑥ 落在 tool_call 与 tool_result 之间的 user 角色说明后移到同批结果之后 | 这些都是 TanStack 引擎的形状决定的，不在适配器里绕；⑥ 是 Anthropic 硬规则（tool_result 必须紧跟调用），lowering-pi 靠 pi-ai 归位而 TanStack 路径由我们翻译时后移 | 中（③④ 若 dogfood 反馈明显可改为在 afterTools 边界发 `reins.pause` 中断） |
| 2026-09-08 | **B11** 投放工具（AdRate）接入走官方 CLI `@adrate/cli`，不读服务层：工具从服务端 `schema` 生成（`examples/adrate/tools.ts`），幂等键 = `reins-<toolCallId>`，带幂等键的写操作 risk=high 交 approval 按风险问人，`feedback` 不给模型，两份官方 Agent Skill 全文作系统提示；子进程只在示例里，core / brain 零 node:* 不变 | CLI 本身按 Agent 设计（信封 / 退出码 / 幂等 / 自描述），与 reins 的审批、时间线、幂等语义天然对齐；reins 的 toolCallId 在审批暂停前后不变，正好满足"一个键一次不可变的写"；schema 生成比手抄 CLI 帮助准且可重同步 | 高 |
| 2026-09-08 | **B11** dogfood 的模型来源改为 DeepSeek 直连（`REINS_PROVIDER=deepseek`），aireiter 网关只留给单轮探测 | 网关对带工具结果的多轮请求两次掐断（第一次 15 s 后 Connection error，第二次返回了用量却在正文前 terminated），而 20k / 60k 字符的单轮大请求都能过；DeepSeek 小请求 3 s、大请求 5 s 稳定且接受中途 system、缓存命中 85% | 中（网关修好可切回） |
| 2026-09-08 | **B11 附** tsup 配置 `removeNodeProtocol: false`（store-sqlite），并把"跑一次 dist 产物"列入每个含 `node:*` 子路径包的验收 | tsup 8 缺省剥掉 `node:` 前缀，`node:sqlite` 不在 esbuild 内置清单里就成了裸的 `sqlite` 包名，只在真跑 dist 时才暴露；vitest 与 tsc 走源码路径全绿掩盖了它 | 高 |
| 2026-09-08 | **B11 附** lowering-pi 工厂 `BoundModelOptions.midConversationSystem?` 透传到 ModelDefinition；示例对 DeepSeek 置 true | B1 附加的字段只有直接构造 PiAiLowering 才能用，走 `anthropic()` 工厂的第三方上游被迫 lossy；透传后 dogfood 后半程全部 exact | 高 |
| 2026-09-09 | **Boss 决定**：取消 PRD §7 门槛 1 的"两周 dogfood 观察期"，改为"跑通真实长任务并进入生产接入"；机制已在测试环境验证，直接推进 M2 | 流程与工具都跑通说明机制成立，两周日历观察不再提供新信息；生产接入本身就是持续 dogfood | 高（Boss 决策） |
| 2026-09-09 | **B11 附** AdRate 契约漂移复核：ads 状态命令属实（服务端 registry 声明 `--status`，CLI 只认 `--set`，守门测试把错误值当预期冻结了），已修复发布测试与生产，两端统一小写；GMV Max 不属漂移（inputSchema 枚举大写是 HTTP 线上格式，CLI 内部小写→大写映射且有三方对齐测试）。示例 `CLI_OVERRIDES` 删掉 flag 改名、只保留两处"HTTP 大写枚举 → CLI 小写"的层间映射 | 工具站在 CLI 层，模型按 schema 给大写值是合理的，映射属于适配层职责而非漂移；重新 sync 即验证服务端修复 | 高 |
| 2026-09-09 | **E1** eval 做成独立包 `@reins/eval`，只依赖 core；臂（arm）就是 LoopConfig 片段（sockets / projection / 追加系统提示），模型自决臂由调用方用 @reins/brain 组，不让 eval 包依赖 brain；三个对照臂用同一个 `runLoop`，不写第二套循环 | §3 包表本就写"仅 core"；eval 要能评第三方脑子模块与用户自己改的循环配置，绑死 brain 就评不了别人；同一循环保证差异只来自臂 | 高 |
| 2026-09-09 | **E1** fixture 的"事件日志"落成两件事：`task.seed`（任务开始前的历史，换 sessionId 原样 append）与 `recordedTools(recording)`（录像里 tool_call → tool_result 配对成确定性工具）。匹配严格：同名 + 键排序后入参逐字相同；没匹配上缺省给 isError 说明而不是随便给一条，`fallback` / `sequence` 要显式开 | 三个臂必须面对同一个世界，差异才只来自脑子；不碰真服务（AdRate 写操作有副作用）；宁可让模型知道"这里没数据"，也不给一条错的答案让它接着推理 | 中（匹配策略可加） |
| 2026-09-09 | **E2** 脱敏口径：广告主 id、计划 id、请求 id、Command / 凭证 id、人名 / 团队名 / 广告主名 / 授权账号名一律换成等长别名（在事件 JSON 文本层逐字替换，同一个值处处一起变）；测试广告主下的计划名、时间戳、用量数字、模型思考正文保留；构建脚本自查"原值一个不剩、每条规则都命中" | 别名等长等形，模型在回放里看到的仍像真 id，token 数与真实接近；计划名是 Boss 在测试广告主上造的数据，不含业务信息，留着让任务读起来自然；源录像本身仍含真实 id 与人名，**发布前须把 `examples/adrate/recordings/patrol-disable.jsonl` 换成脱敏版或移出历史**（待 E4 与 Boss 定） | 中 |
| 2026-09-09 | **E2** fixture 的世界 = 去外溢后的完整录像 + 从录像数据里长出来的补位（重分页、合成 get / status / commands 查询）；不是纯逐字回放，也不是完整模拟器 | 模型换一种问法（页大小、先 get 再 list、按键对账）世界仍自洽，三个臂与不同模型族比较才公平；只回答这个世界里真有的东西，别的广告主 / 计划 / 日期窗口照样报错，不凭空造数据 | 中（补位规则可加） |
| 2026-09-09 | **E2** 带种子的 fixture，指标只算本次新追加的事件（`fresh`），种子历史不计 token / 轮 / 动作 / 违规；完成判定与探针仍看全链 | 种子是上一次真实运行的账；不剔除的话三个臂共同背一段固定开销，比例被摊平 | 高 |
| 2026-09-09 | **E3** 脑子模块缺省开关按对照数据定（DeepSeek v4 flash，3 fixture × 3 重复 × 4 轮，见 `examples/eval/results/2026-09-09-deepseek-v4-flash/`）：`spill` 缺省阈值 8k → 16k；`compact` / `memory` / `handoff` 不默认开；`perception` / `pins` / `budget` / `approval` 推荐默认；core 阈值折叠继续兜底 | 外溢 6k 把模型必须整读的列表外溢再分段取回，只读任务 token 143k → 85k、全流程 255k → 135k；模型自决整理触发太少（接续版 ≤1/3 格）不足以证明收益，且整理机制的说明诱发模型"以为旧细节被折叠"的拒答（召回 −3~6 点）；memory 多花 1~4 轮召回没涨；handoff 全程未触发。P8：模型自决类机制无 eval 达标不默认开 | 中（换模型族重测可翻） |
| 2026-09-09 | **E3** 门槛 2 状态：**未达成**。brain-lean 对照 threshold：token 两轮不多于、一轮多 10%；完成度三轮不低于；召回四轮都低 3–6 点；违规率持平 0。发布前须解决"以为被折叠"并换模型族复测 | 诚实记录；失分的机理已定位（模型看到"compactions so far: 1"就不回看上文），不是整理丢了数据 | — |
| 2026-09-09 | **E3 附** 报告同时给"总 token"（缓存读按 1×，门禁用）与"计费等价"（缓存读按 0.1×）；探针回答正文为空时退回最后一段 thinking（`answerFrom` 标明） | PRD 原文是 token 不多于，不改门禁口径；但 brain 多出的 token 九成是缓存读，账单差别小得多，两个数都给才不误导。DeepSeek 短答案 9% 整个落在 thinking 里，判成"不记得"是误伤 | 高 |
| 2026-09-09 | **E3 附** lowering-pi：并行工具时落在两条 tool_result 之间的 system_note / compaction 后移到同批结果之后（与 TanStack 适配器 B10 同规则），落点备注说明后移 | pin / memory 留痕的说明排在自己的结果之前，把 tool_result 拆成两段，Anthropic 协议要求同批 tool_result 紧跟 tool_use，DeepSeek 直接 400。dogfood 没撞上是因为模型没并行调两个脑子工具 | 高 |
| 2026-09-09 | **E3 附** eval 世界"写后可见"：fixture 的列表 / 报表 / get / status / commands 全部由从录像长出来的世界供给并按会话记账，不逐字回放这几个工具 | 录像是写操作之前的快照，模型停投后再拉列表仍显示 ENABLE，第一轮有一格被这个矛盾带偏；世界自洽比逐字更重要 | 中 |
| 2026-09-09 | **E3b** 换模型族复测（claude-sonnet-5 经 Boss 中转，直通官方 API）：compact 不默认开的结论**加固** —— Sonnet 每格真的整理了，摘要只留结论不留字段值，同一条事实 6/6 丢；approval **默认开**再添一条理由：没装它时两个模型族都出现"列完候选停下等 Owner 批准"的停摆，装了就直接提交（完成度 100% vs 93%） | 两个模型族一起指向机制本身：模型自决整理按自己的重要性判断丢细粒度字段；审批模块是"让模型敢动"的机制不只是安全网 | 中 |
| 2026-09-09 | **E3b 附** 中转 `http://95.216.117.43:8356`（Boss 提供）经暗号法体检为直通官方 Anthropic API（`spikes/relay-check`）：内容不改、用量字段官方、强制隐藏 thinking；官方规则"中途 system 必须紧跟 user"，reins 归位规则合规，`midConversationSystem: true` 可用。取代 aireiter 作为 Claude 侧对照上游 | aireiter 丢中途 system，跑出来的数字不可信；这条中转虽是 http 明文只作测试，但内容忠实 | 中 |
| 2026-09-09 | **E1** 关键信息召回用**探针问答**：跑完后从主会话末尾 `fork` 出探针会话，去掉宿主工具、保留脑子工具，追加"只凭已知作答"的系统提示，一问一答按 `expect`（包含 / 正则 / 函数）或 LLM judge 打分；探针用量另记不进任务 token | fork 让每条探针独立、也不污染主会话（主会话日志仍是那次任务的真实记录）；去掉宿主工具是防止模型重新查一遍来"作弊"，保留 fetch_blob 是因为取回外溢结果正是脑子的本事、该算它的分；judge 与执行分离是 §13 原则，eval 包只定义 `Judge` 接口不绑模型 | 中 |
| 2026-09-09 | **E1** 指标全部是时间线纯函数，"轮"按模型输出类型切（thinking / text / tool_call），第一轮前的事件归第一轮；治理衰减按第一条 compaction 的数组位置分前后两窗；缩窗口靠包一层 Lowering 改 `capabilities.contextWindow`（`withContextWindow`），不改 core | 录像可以不重跑直接算分、三个月后算出来一样；按 actor 切轮会把模型自决 compaction / 模型 pin / memory_op（actor 都是 model）在并行工具时误切成一轮 —— **core `replayTurns` 就有这个缺陷，E1 一并改成按输出类型**（导出 `isModelOutput`）；窗口是能力声明，循环与脑子本来就从 capabilities 读，包一层最省 | 高 |
| 2026-09-09 | **E1** 门禁 `checkGate` 直接照 PRD §7 门槛 2 落四条硬规则（token ≤ 基线 × 1、完成度 ≥、召回 ≥、候选整理后违规率 ≤ 整理前），只给结论与数字；CI 红绿与默认开关由 E3 的脚本决定 | P8"无 eval 不默认开"要有一个机械可查的判据；把规则写成代码而不是文档，E3 跑数时就不会临场放水 | 高 |
| 2026-09-10 | **E3c** 整理丢细节的对策选 ②"让模型知道什么还能拿回来"，落成两件：compact 摘要之后自动附**被折叠工具结果清单**（`seq N tool(入参) — 大小`，core 纯函数 `foldedToolResults` / `renderFoldedToolResults`，阈值兜底摘要同样列），加 **`recall({ seq })` 工具**按 seq 从本会话日志逐字取回一条原件（随 compact Socket 带出，可关）。清单范围含被吸收旧摘要盖住的原件（从完整时间线找）。① "keep 引导逐字保留"不再单做（E3 已加过"Keep exact values"一句，Sonnet 照样只留结论，说明靠文案改不了模型的取舍）；③ pin 上限不放宽，只把 500 字符写进工具说明与 schema `maxLength` 并说明分条 | 宪法一：模块只让模型看见、给能力，不替它决定留什么；宪法二：原件本来就在日志里，缺的只是一条读回去的路，与 spill 的 fetch_blob 同一思路。清单是"事实上还在的东西"，摘要是模型的取舍，两者分开各司其职。pin 是每轮都占的永久成本，放宽上限是把成本转嫁给所有后续轮 | 中（清单条数上限 80、措辞、是否让阈值摘要也列均可调） |
| 2026-09-10 | **E3c 附** 清单与取回引用用 tool_result 的 **seq**，不用 toolCallId 或事件 id | seq 短、模型能抄；fork（eval 探针、宿主分叉会话）三种存储实现都保留 seq，子会话里照样有效；toolCallId 是厂商格式且长，事件 id 36 位 | 高 |
| 2026-09-10 | **E3c 附** pin 字数校验加**两成容差**（声明 500，601 起才拒；`overshootTolerance`，可设 0 严格），报错仍报声明值 | 上限写进工具说明与 schema 后，Sonnet 5 在 E3c 的 3/3 格仍写出 502 / 545 / 558 字符 —— 模型瞄着上限写、数不准自己的字数，差几个字就拒掉只是白费一轮；声明值不变，模型照旧往 500 以内凑，容差只吃掉数错的那一点。不直接放宽到 600 是因为那样模型会瞄着 600 写、又数错 | 高 |
| 2026-09-10 | **E3c 结论** `compact`（含被折叠清单 + `recall`）从"不默认开"改为**推荐默认**，取代 E3 / E3b 的结论；memory / handoff 仍不默认；spill 16k、perception / pins / budget / approval 推荐默认不变。**PRD §7 门槛 2 达成** | E3 / E3b 不默认的理由是"模型自决整理丢细粒度字段、召回低 5–10 点"；E3c 让丢掉的东西有路可回后，两族召回 100%（Sonnet 12/12、DeepSeek 9/9 格答对上一轮必丢的事实），候选 token 比阈值基线少 7%（DeepSeek）/ 22%（Sonnet 第二轮），只装整理 + 取回的 compact-only 臂少 29%。Sonnet 第一轮 token +1.8% 未过，差在审批分批 14 次暂停与 pin 重试而非整理，如实记录。P8 的判据是 `checkGate` 四条机械规则，两族都过了才改缺省 | 中（DeepSeek 在这批 fixture 里从不自己整理，recall 在它身上未被真正用过；换 fixture 或模型族要再看） |
| 2026-09-10 | runLoop 模型调用的瞬断重试**只在本次尝试尚未落任何模型输出时**进行；落了半截再失败照旧记 error 交宿主（`retryable` 如实标注、`detail.partialOutput`）。每次将要重试的失败也记一条 `core.error(retryable=true, detail.willRetry)`，模型不可见。判不出的错误一律当非瞬断 | 时间线只追加：半截后重试等于让模型重说一遍，日志里就有两份半截，UI / eval / 回放都得自己猜哪份算，违背"日志即真相"；E3 实测的 3 次掐断都发生在输出 0 token 时，保守规则已覆盖真实场景。重试留痕是为了宿主与 eval 能看见发生过什么（宪法二），不给模型看是因为它对模型的决策没有信息量。判不出当非瞬断：重试一个 400 只会再挨一次 400 还多等几秒 | 中（半截重试可后续做成"记一条 retracted 事件再重说"，需新事件类型；先看真实需求） |
| 2026-09-09 | **上线前审查** `input` 事件草稿加**两道白名单**：循环层 `inputDraft` 只接受 `core.user_message` / `core.tool_result` / `core.system_note` / `ext.*`，其余在写日志之前抛 RangeError；server 层更严，只放行 `user_message`（只取 content，actor / trust 由服务端定）与**客户端工具** pending 调用的 `tool_result`（toolCallId 须 pending、工具须 `side:"client"` 或无 execute，name 取自 tool_call），其余 400 / 409 | 审查发现网络端点原样接受任何草稿：一条伪造的 `approval_decision(approved=true)` 让 pending 调用免审批执行（探针实测），伪造的 system_note 带 system 信任、compaction 能藏历史。审批结论只能走 `decisions`（T10 校验）。分两层是因为循环在进程内也可能被宿主拿不可信输入直接调用 | 高 |
| 2026-09-09 | **上线前审查** 用户在工具结果回来之前插话（续跑带新 input、进程死亡后再发消息）：**日志顺序不动**（user_message 如实排在 tool_result 之前），由**降级层**把它后移到同批结果之后并记 `lossy(user)`（lowering-pi 与 TanStack 适配器同一规则，矩阵新增条目）；不改 runLoop 让 input 等 pending 补齐再落 | 宪法二：时间线是唯一真源，用户插话就是再追加一个事件，什么时候说的就记在什么位置；"tool_result 必须紧跟 tool_use"是厂商线协议的约束，属降级层职责（与说明后移同一机制）。若改成循环先补齐再落 input，pending 暂停时 input 要么丢、要么仍落在结果之前，问题只是换了地方。此前实测模型第二次请求看到 `[user, tool_call, user, tool_result]`，Anthropic 会 400 | 高 |
| 2026-09-09 | **上线前审查** runLoop 的宿主中止检查**前移到"补齐 pending"之前**；工具批中途中止后余下调用留作 pending，下一轮开头直接 paused(host) | 此前检查在补齐之后：中止只跳出当前批，余下调用被当成 pending 在下一轮一个个继续执行，直到跑完才暂停（探针实测两个并行工具、第一个执行中 abort，第二个照样跑）。中止的语义是"没执行的别执行"，留作 pending 恢复时再补 | 高 |
| 2026-09-09 | **上线前审查** Anthropic 中途 system 归位改为"先算每条说明在原始消息列表里的落点、按落点分组、再一次拼出"，不再边算边 splice | 边 splice 让同一落点的多条说明前后颠倒（探针实测感知 + pin 同轮注入变成 pin 在前、感知在后；中段与末尾都反）。现有测试只断言角色序列没查文本顺序，已补 | 高 |
| 2026-09-09 | **R1** 工具入参 `validate` 前移到审批判定之前（runLoop、approval 模块、TanStack 适配器三处同序）；校验不过的调用**不问人**，直接以"入参不合法"拒掉（approval 模块记 `approval.invalid_args` 放行给循环拒） | 审批人批的必须是将要执行的那份入参，`needsApproval(input: TInput)` 才不是谎话；让人批一个随后必定被 validate 拒掉的调用只是浪费一次暂停。`rewrite` 仍在 validate 之前（钩子改写原始入参，改完再校验） | 高 |
| 2026-09-09 | **R2** 被打断（审批 / 中止）的那一轮在续跑补齐 pending 之后调 `onTurnEnd` 收尾（循环抽出 `endTurn`，缺省 continue）；handoff 模块的意图**按日志重建**（`unfinishedHandoffArgs`），不再只靠 WeakMap | 根因是被打断的轮永远没有 onTurnEnd，模块在那一轮记下的决定全丢；修循环比在每个模块里各自补救干净。意图从日志重建是宪法二的直接推论（模型的回执与入参都在日志里），换进程续跑也成立。ctx.timeline 仍是轮开始时的快照（既有语义不变） | 高 |
| 2026-09-09 | **发布前核实** PRD"跑在任何 Web 标准运行时"的卖点经 `spikes/edge-runtime-check` 实证成立：`@reins/lowering-pi` 的 **dist** 在 Cloudflare workerd 上四层探测 × 三档配置 12 格全过，**不需要 `nodejs_compat`**，两条协议路径（Anthropic Messages 走 `@anthropic-ai/sdk`、OpenAI Responses 走 `openai`，代码路径互不相干）各自真打了一次线上 API。判据取**最严档**（`compatibility_date=2023-01-01` 无 flag，`process` / `Buffer` 均不存在、`node:*` 一律 import 失败），不取当前推荐 date | 两个 SDK 的 `exports` 没有 worker / edge 条件导出，靠运行时探测打 shim，"静态 import 链零 node: 内置"只能证明静态干净、不能证明能跑。必须打 dist 而非源码：B11 的 `node:sqlite` 被 tsup 剥成 `sqlite` 就是只跑过源码路径。发现 Workers 较新的 compat date 默认已带部分 Node 兼容（2026-09-09 那档不加 flag 也能 `import("node:fs")`），只跑新 date 会高估结论，故补最严档兜底 | 高（结论随 pi-ai / 两个 SDK 升级可能变，脚本留下可一键复核） |
| 2026-09-09 | **发布前核实** 依赖与许可证盘查：96 个生产依赖全为宽松证（MIT 42 / Apache-2.0 41 / BSD-3-Clause 11 / Unlicense 1 / 0BSD 1），**零 copyleft、零未知、零商业限制**，与我们的 MIT 兼容。`@reins/core` 与 `@reins/brain` 确认零外部依赖。pi-ai 的传递依赖体积代价（安装约 65 M，其中 `@google/genai` 14 M + aws-sdk 全家桶 15 M 在可达链之外纯属死重）**0.1 只在 README 明示，不做处置**；自研零依赖降级层 `@reins/lowering-fetch` 列为 0.1 后可选项 | pi-ai 把 10 个 dependencies 全列在 `dependencies`（非 optional / peer），npm 安装不看可达性，用户装 lowering-pi 就得全下载。实测我们只子路径深导入两个 api 子模块，递归 27 个模块、零 node: 内置、只可达 `@anthropic-ai/sdk` / `openai` / `partial-json`，所以是**装机盘与冷启动**代价而非运行时代价，不阻塞 0.1。另记：pi-ai 的 npm 包内无 LICENSE 文件（只在 package.json 声明 MIT），我们作为普通 npm 依赖无再分发义务，但将来若 vendor 或打单文件 bundle 须自行附上上游许可证正文 | 中（降级层薄，lowering-fetch 是加法不是改法） |
| 2026-09-09 | **R6** server 鉴权拆成两层：`principal(request)` 只解析"谁在问"，新增 `authorizeSession({ sessionId, principal, request, method, isNew })` 决定"他能碰哪条会话" —— 在 GET 解析出 query、POST 解析出 body 的 sessionId **之后**、读写该会话任何日志**之前**调用；**只有显式 `true` 放行**，`false` 与 `undefined`（漏写 return）一律回 **404**（非 403），抛 Response 原样返回。**不设它时 handler 不做任何会话归属检查**，根 README 新增 Security notes 明示 | 原缺陷比记录的更具体：分发处 `handleGet` 压根没收到 `principal`（只传给了 POST），而 POST 的 sessionId 在**请求体**里 —— 宿主想在 `principal` 里判归属得 `clone()` 读 body、handler 随后还要再读一次，所以对 POST 来说宿主**做不到**，光写 README 挡不住。由 handler 统一解析好再回调，两条路一个关口。拒绝用 404 因为 403 等于确认"这条会话存在只是你不能看"，与外溢 blob"未被引用的 id 一律当不存在"同一规则。缺省不检查而非默认拒绝：单租户与本地开发是主流用法，默认拒绝会让所有人都得先写一个恒真钩子 | 高（钩子是加法，缺省行为不变） |
| 2026-09-09 | **R6 顺带** `sessionId` 入口校验为"非空、可打印 ASCII、不含空格"（`/^[\x21-\x7e]+$/`），GET 的 query 与 POST 的请求体两条路各校验一次，越界回 400 `bad_request`。**取比运行时真实边界（latin1）更严**：`é` 实测能进 header 并正常回写，仍然拒 | Boss 判断"ID 没人会用中文"对，但依据要换：sessionId 是**客户端可控输入**（GET 取 query、POST 取请求体），不取决于有没有人愿意那样命名 —— 任何前端 bug 或恶意请求都能传。实测七种越界值（中文 / emoji / CRLF / 裸 LF / NUL / 空格 / é 中前六种）全部抛未捕获 TypeError 冒出 handler，把一个该 400 的请求变成 500；**CRLF 被 Response 构造器挡住，不构成注入漏洞**。比 latin1 更严的理由：一句话说得清、各处一致，且 header 值首尾空格会被 trim，使回写的 `X-Reins-Session` 与客户端传入的不是同一字符串 —— 那种静默不一致比直接拒绝更难查。校验点就放在已有的 `parseBody`（原本只查"非空字符串"）旁边，属补齐而非新增关注点 | 高（放宽只需改一条正则；0.1 未发布，现在收紧代价最小） |
| 2026-09-10 | **记忆隔离不加"角色"字段**：隔离分三层 —— 存储实例（表 / 库，store 构造期定、表名做成可配置）、`namespace` 前缀（`memory()` 的函数选项）、路径（模型自决）；"平台共用一套记忆"与"按场景各自一套"都是前缀函数的写法，角色用闭包常量、用户用 `ctx.principal`；挂载表（共享只读 + 私有可写）列为待需求项不做 | 角色由"哪个 createAgent"已经决定，再进存储或事件就是第二真源（宪法二）；平台分法（租户 / 用户 / 角色 / 项目）没有统一形状，字段必定定错，函数任意组合；MemoryStore 保持 4 个方法，宿主自实现仍只有几十行 | 高（选项层面，随时可加） |
| 2026-09-10 | **子代理 = 工具**（Boss 提出、评估成立）：角色 B 以 `Tool.execute → agentB.run()` 挂在角色 A 的工具表上，库不做编排器；0.1 只给范式与示例（`examples/team/`），0.2 才做 `asTool` 助手，且助手只解决手写做不对的两件事 —— 审批冒泡（新 `Interruption.kind="subagent"`）与预算合算；**联停与否由使用者决定**（Boss 补充）：范式缺省传 signal，接力式编排（财务做完交法务）可不传 | 叫谁、叫它做什么、信不信结果都在 A 的模型判断里，正是宪法一；OpenAI Agents SDK 与 Claude Agent SDK 同形；手写版今天已能跑，先让 dogfood 暴露真问题再抽助手，避免造没人用的接口 | 高 |
| 2026-09-10 | **MCP 提前到 0.1**（Boss 定）：新包 `@reins/tools-mcp`，用官方 `@modelcontextprotocol/client@2.0.0`（npm 核实最新、MIT；`@modelcontextprotocol/sdk` 1.30.0 是旧线，不用），`mcpTools(): Socket` 静态贡献形态、run 内工具表不变、`listChanged` 只影响下次 run；annotations → risk / needsApproval 缺省；主入口只 Streamable HTTP，stdio 在 `/node` 子路径；0.1 不做 sampling / elicitation / resources / prompts | 自用环节要一个能力完整的库；静态贡献让 MCP 工具与进程内工具走同一条路进循环，spill / approval / budget 自动生效（P3）；工具表 run 内稳定是 §9.1 缓存约束，也是 PRD"不做动态注册"的落点 | 中（包边界定了，内部实现可调） |
| 2026-09-10 | **MCP 生效粒度**（Boss 介入定）：库只做"工具表按 run 绑定"一个原语，不做按会话快照、不做 run 中热加载；平台策略推荐"下一条消息生效"，"新会话才生效"由平台自己存配置快照实现；**工具表变化默认告知模型**：新增模型不可见的 `core.tools_bound` 每 run 一条，增删时追加模型可见 `system_note(kind=host)` 列出工具名 | 按 run 是最小原语，平台在其上可收紧不能放宽；用户加工具是为把眼前的事做完，重开对话丢上下文反直觉；宪法一要求让模型看见环境变化，且 tools_bound 让日志有了每次 run 的工具表快照（宪法二）；暂停中漂移拒绝与 `allowConfigDrift` 放行沿用既有机制 | 中（事件加了就要保留 upcast；平台策略随时可调） |
| 2026-09-10 | **文档换代进入维护阶段**（Boss 提出，借鉴其 TanStack 项目启动资产的体系）：CLAUDE.md 改为正式版（身份历程、宪法、维护阶段工作流、索引、包全景、硬约束、技术要点与风险，红线 300 行）；新增 `docs/README.md`（体系规范 + 写入路由表）、`docs/系统全景图.md`、`docs/模块盘点/`（每包一份，四段式）、`docs/踩坑记录.md`（四段式，首批 18 条回填）；`TASKS.md` 只留未完成项，已完成记录整体迁 `归档/2026-09-10-任务记录-M0-M2.md`；技术方案加阅读指南与 §19"与代码的已知出入"，不回改原文 | 代码 3.1 万行、任务板 47 KB、技术方案 88 KB 后，每次开工整读成本过高；按"阅读时机"分层、活文档少快照多，是 AdRate 两年验证过的做法；任务行不再堆实现记录，细节归模块盘点 | 高（纯文档） |
| 2026-09-10 | **核实 `@modelcontextprotocol/client@2.0.0` 导出面**（P1 ①）：主入口导出 `Client`、`StreamableHTTPClientTransport`、`InMemoryTransport` 等，dist 实测**零 `node:*`**，外部依赖只有 `eventsource` / `eventsource-parser` / `pkce-challenge` 与 `./_shims` 条件导出（workerd / browser → cf-worker JSON Schema 校验器，node → Ajv）；`./stdio` 子路径才带 `node:process` / `node:stream` / `cross-spawn`；`@modelcontextprotocol/core` 只依赖 zod。`listTools` 自动翻分页且**带结果缓存**（要 `cacheMode: "bypass"` 才取服务器此刻的表）；服务端抛错与入参不合法都回 `isError` 结果，只有"工具不存在"抛 `ProtocolError -32602`；断连后调用抛 `Not connected`。测试与示例用 `@modelcontextprotocol/server@2.0.0`（`McpServer` / `createMcpHandler` / `InMemoryTransport`） | 实测（/tmp 探针 + `spikes/edge-runtime-check` 最严档 `/mcp` 通过）而非读文档；决定主入口只 Streamable HTTP、stdio 走 `/node` 与规划一致 | 低（上游升级复跑 spike） |
| 2026-09-10 | **静态贡献可异步**（P1 core 小改）：`StaticContribution<T>` 函数形态可返回 Promise，`resolveSocketContributions` 变 async，各 Socket **依次** await 不并发；三处调用点（循环、server 预校验、TanStack initRun）改 `await`。**`core.tools_bound` 落地**：每次 run 起步（校验通过后、新输入前）一条模型不可见快照 `{ toolNames 排序, configHash }`，与上一条比对有增删且 `LoopConfig.announceToolChanges !== false`（缺省开，Boss 定）再一条模型可见 `system_note(kind=host, meta.toolsChanged)`，首次 run 不出；纯函数在 `loop/tools-bound.ts`，TanStack 适配器共用 | MCP 的 `tools/list` 是网络调用，同步解析不可能成立；替代方案"宿主先 await 建好 Socket 再传"会把工具表冻结在进程启动时，违背"listChanged 只影响下次 run"与免重启承诺。依次而非并发：同名去重以先到者为准，顺序变 configHash 就变。快照放在校验之后是"校验不过一条日志不写"，放在输入之前是模型读到问题时已知道手里的工具变了 | 中（事件加了就要保留 upcast；announce 缺省可调） |
| 2026-09-10 | **`@reins/tools-mcp` 形状**（P1 ②③）：`mcpTools(options): McpToolsSocket`（Socket + `close()`），只用异步静态贡献 `tools`，不挂钩子；传输是**配方** `McpTransport { kind, label, create(): unknown }`——`httpTransport()` 在主入口、`stdioTransport()` 在 `/node`，SDK 类型不出本包；连接懒建、跨 run 复用、`onclose` 后下一次需要时按配方重建一次，**不做退避重试**；run 起步 list 失败缺省抛（fail-closed），`optional: true` 才空表 + 告警一次；注解**只定缺省**：readOnly → low、destructive → high + `needsApproval: true`、其余 medium，刻意不把"没写 destructiveHint"当破坏性（spec 缺省为 true）；模型侧工具名按 `^[A-Za-z0-9_-]{1,64}$` 改写、调用按原名；`override(tool, info)` 返回 Tool 或 false（排除）；0.1 不做 sampling / elicitation / resources / prompts / MCP Apps / OAuth | 与进程内工具走同一条路进循环，spill / approval / budget 自动生效（P3）；配方而非连接才能"断了重建"且不漏 SDK 类型；一次调用失败让模型看到 isError 自己定是宪法一，跨 run 重建只是清掉"上次断了"的状态不是重试；"没写注解就当破坏性"会让每个 MCP 工具都要审批，要严的宿主用 `override` 或 `approval({ unmatched: "ask" })` | 中（选项可加，形状定了） |
| 2026-09-10 | **平台策略不需要"删工具新会话生效"的不对称规则**（P1 ⑧ 实测，`spikes/mcp-removed-tool-history`）：历史含已移除工具的 tool_use / tool_result 时，DeepSeek（Anthropic 协议直连）与 aireiter gpt-5.5（OpenAI Responses）在"工具表里有别的工具"与"工具表为空"两个变体下都接受且模型正确作答、只列当前工具；aireiter 的 Claude 端点两变体都回 "Anthropic stream ended without a stop reason"（网关改写截断，与 2026-09-08 已知问题同源，非协议拒绝）；**官方 Anthropic 直连未测**（无 key），列 TASKS 0.1 之后。示例服务器踩坑：一个 `WebStandardStreamableHTTPServerTransport` 只服务一个会话，第二个客户端 initialize 400，服务器要用 `createMcpHandler(factory)` 且业务状态放工厂外（踩坑记录 2026-09-10） | 判据不是状态码而是模型真的作答并列出当前工具；两条协议、两个变体都过，加工具与删工具可以同一规则"下一条消息生效"；官方端点没测就写明没测 | 中（官方 Anthropic 实测若拒绝再加不对称规则） |
| 2026-09-10 | **P2 记忆表名可配，且只配记忆表**：`sqliteStores` / `pgStores` 新增 `memoryTable`（缺省 `reins_memory`），同时喂给建表语句与 MemoryStore；`SqliteMemoryStore` / `PgMemoryStore` 构造器第二参从 `now` 改为选项对象 `{ table?, now? }`；表名过白名单 `^[A-Za-z_][A-Za-z0-9_]{0,62}$`（两包各一份同一正则），不合规抛 `StoreError("invalid_argument")` 且不发任何语句；不加引号 | 隔离需求只在记忆（§9.6 第一层"哪张表"），事件与 blob 按 session_id 隔离已够，事件表也开选项等于给"一个库多套 reins"这个未出现的场景提前定型；表名是标识符绑不了参数，字面拼接就必须白名单，白名单比引号转义更容易一眼看出安全；选项对象比追加位置参数可扩展；不加引号是因为 Postgres 未加引号标识符 DDL 与查询同一折叠规则、SQLite 不区分大小写，两边一致 | 高（加选项不破签名；真要整套隔离走不同库 / schema） |
| 2026-09-10 | **P3 子代理范式的三个形状选择**：① 子会话关联只走一条路 —— 子 `sessionId` 连同状态、答案、用量一起以 JSON 写进父 `tool_result`（模型可见），不另 `ctx.emit` system_note；② 子 paused（审批 / 预算 / 中止）与 error 一律 `isError` + 一句英文说明交父模型，工具里不循环、不替批；③ 专家工具缺省 `risk: "low"`（专家只有只读工具），专家有写工具时由使用者提高 risk 让父侧 approval 问人。范式住在示例（`examples/team/subagent-tool.ts`），不进库 | 一条路够用且回放 / eval 只需解析一处，再发 system_note 就是第二份真源且会夹在并行工具之间触发后移；paused 当 isError 是 0.1 仅有的两种安全选择之一（另一种是子不装 approval，示例两者都做），替批等于把审批权从人挪到代码；risk 缺省 low 是因为示例专家全只读，而 approval 缺省 byRisk 会把 medium 当要问人，把每次委派都变成审批会让范式没法演示 | 高（0.2 asTool 助手落地时审批冒泡替代 isError，见 §10.1） |
| 2026-09-10 | **R9 trust 标注落地**：`trust === "untrusted"` 的事件内容在降级层翻译时包成 `<untrusted source="tool:<name>">…</untrusted>`（来源退路 `provenance.source` → actor），核心逻辑放 `@reins/core` `lowering/trust.ts` 一份纯函数，lowering-pi 与 TanStack 适配器都调它；只包文本，图片不包（首尾是图片就各插一段文本标记）；内容里的 `</untrusted`（大小写不敏感）转义成 `<\/untrusted`，发生时落点记 lossy 并说明；**缺省开**，三处选项 `trustMarkers: false` 可关；事件 payload 与日志不动 | 放 core 是因为"同一件事两处做"迟早文案分叉（configHash 教训），两条协议只是调同一函数；用 XML 式标签而非 `[untrusted …]` 方括号，与 `<system_note>` 同一族、Anthropic 官方也推荐用标签划界；转义提前闭合是防注入内容自己"关掉"标记，这是唯一会改内容字面的地方所以必须记 lossy；不加"treat as data"之类说明文字是省每条工具结果的 token，标签名本身已说明，宿主要强调可写进 systemPrompt；缺省开是安全默认值（§14 fail-closed），不是模型自决机制，不受"无 eval 不默认开"约束，但用 DeepSeek 复跑 examples/team 验了行为不受影响 | 中（标记形状与转义策略集中在一个文件；若有厂商原生"untrusted content block"可按 api 分支） |
| 2026-09-10 | **发包 = 冻结公开接口，发前清单据此筛选并封口**（Boss 定，我建议筛选规则）：会改公开类型或默认行为、规划清晰且无外部依赖的先做完再一起发 0.1——R5、R7、R3、R4、R8 五个小项 + `asTool` 助手（原 0.2，因要改 `Interruption` / `RunResult` 公开类型而提前）+ E4；纯新增的（lowering-fetch、四环境验证、挂载表、tools-mcp 后续）发后做。清单封口，不再往里加；Boss 的商业化投放工具等正式包再接入，不走本地 file: 链接 | 第一次发布后再改公开类型就是破坏性变更，0.x 虽允许但会让首批使用者立刻返工；小缺陷（R5 重复消息、R7 审批检查缺失）发出去就是别人踩的坑；代价是真实反馈往后推，用"封口 + 约两个工作日会话量"控住 | 中（Boss 可随时改为先发再补） |
| 2026-09-10 | **R5 TanStack 导入幂等键**：客户端消息自带 `id`（TanStack 持久化会保留）优先，否则用消息在客户端完整数组里的位置；键存 `provenance.ref`（`import:id:<id>` / `import:<index>`），去重还要求内容逐字相同，只对 `core.user_message` 且来源为 `tanstack-ai` 的生效；被跳过时 `warn` 一句 | 不用内容哈希：客户端数组只增不减，"同位置 + 同内容"已能唯一标识且零碰撞，哈希反而在裁剪历史时会误删用户消息；不只认 id：TanStack 客户端缺省不一定带 id；不比较"整段历史"：客户端裁剪就失效。同键不同内容放行是 fail-safe——宁多记一条不丢用户的话 | 键规则在 `importRef` 一处；改键形状要同时改 `dedupeImportedUserMessages` 的匹配，且旧日志里的键按旧规则匹配不上，只退化成不去重 |
| 2026-09-10 | **R7 审批中断漏登记：实现运行时检查而不是改注释**。init 经 `@tanstack/ai/adapter-internals` 的 `GenericInterruptDefinitionRegistryCapability` 读引擎登记表，按引用同一性判定（与引擎 `!==` 判据一致）；没登记 `warn` 一次，需审批的调用留 `approval_request` + `approval_decision(false, by: "reins", reason)` 再 `block`，不发中断 | 只改注释等于承认"JS 宿主漏一行配置，run 就死在等不到答复的 run_paused 上"，违背 fail-closed 默认；不选"没登记就直接执行"（那是 fail-open）；不选"抛错"（引擎已经在抛，问题恰是抛得晚、日志留下误导状态）。用 adapter-internals 而非主入口是因为登记表只从那里导出，主包已 pin 精确版本，风险可控 | 检查在 `approvalInterruptRegistered` 一处；上游若改登记表的暴露方式，只改这个函数，其余降级路径不动 |
| 2026-09-10 | **R3 瞬断判定改为状态码优先**：顺序 = 永久错误（LoweringError / AbortError / APIUserAbortError）→ SDK 连接层类名（APIConnectionError / APIConnectionTimeoutError）→ 响应头 `x-should-retry` → HTTP 状态码（对象 `status` 字段，或文案开头三位数字 / "status 503" 写法；有则只按它定，408 / 409 / 429 / 5xx 算瞬断）→ `code` 精确匹配网络码 → 关键词兜底；**删掉对裸数字的正则** | 两家 SDK 的 message 固定 "<status> <body>"、pi-ai 原样转 errorMessage，状态码几乎总在手，不用它而对全文匹配 "timeout" / "429" 就是把 400 参数错当瞬断重试。状态码集合对齐 SDK 与 pi-ai `provider-retry`（含 409）而不自创，避免三层重试策略互相打架；不把 `LoweringOutcome` 加 `status` 字段是因为 pi-ai 的 error 事件本就没有结构化状态，加了也只能从文案解析，不如把解析放在判定函数一处 | `TRANSIENT_STATUS` / `statusFromMessage` 各一处；宿主可用 `retry.isTransient` 整体覆盖 |
| 2026-09-10 | **R4 投影新造事件 seq 冲突：不重跑投影，改注释如实写"抛 StoreError 交宿主"**（`projection/types.ts`、技术方案 §6 追加实现说明） | run 进行中还有人往同一会话 append，是会话级并发违规，正确的闸是 server 的 409 一会话一 run（已有）；投影层重跑等于替宿主吞掉并发写入，模型会看到一条别人正在写的时间线，且与 runLoop 其余 append 点"冲突即抛"的语义不一致（CLAUDE.md：循环外 append 漏同步 lastSeq 就整轮 seq_conflict，靠的正是它会抛） | 要改成重跑只动 runLoop / 适配器 buildTurn 两处 append，但先得回答"谁在并发写" |
| 2026-09-10 | **`asTool(agent, opts)` 的形状（发前，改公开类型）**：① `Interruption` 新增 `{ kind: "subagent", toolCallId, call, childSessionId, reason, interruptions, state }`——子 run 任何 paused（approval / budget / host）都整体冒泡，父 `RunResult.paused.reason` 按 approval > budget > host 综合，`RunResult` 形状不变；② 工具让 run 暂停的通道是 **返回值标记** `subagentPause(detail)`（core `loop/subagent.ts`，`Symbol.for` 品牌），循环见到它不落 tool_result、把调用留作 pending；③ 宿主给子会话的审批结论走同一个 `decisions`，`ApprovalDecisionInput` 新增可选 `sessionId`：缺省 / 等于本会话的照旧校验并记事件，指向别的会话的**不校验、不记事件**，经新增的 `ToolContext.decisions` 原样转发给工具，工具再交给子 run（多层嵌套逐层下传）；④ 预算合算走新增的 `ToolContext.spend?(usage)`：只加本 run 的 `ctx.budget.tokensSpent`，**不**追加父 `budget_usage`（那条是感知校准上下文大小的依据，掺入子用量会污染）；⑤ 子会话 id 缺省 `${parentSessionId}:${toolCallId}`，续跑时工具凭它找回子会话，子日志末条是 `run_paused` 即续跑（不带 input），否则当同一专家的新一轮；子 run 续跑不带 `resume` 状态，子的 state 只给宿主看 | 返回值标记而非抛错：暂停不是异常，抛错会被 `execute` 的 catch 吞成 isError；而非 `ctx.pause()` 回调：回调后 execute 还得返回个东西，两条路容易打架。决策走 `decisions.sessionId` 而非新配置项：宿主对"批一个子调用"与"批一个调用"的动作一致，server 请求体不加字段；子 state 不回传是因为它从未离开服务端工具、签名与 pendingDigest 防的正是不可信客户端与并发写者，而子会话只有这个工具在写。子 id 确定性派生让父日志不必新增事件类型也能跨进程找回子会话（回放仍可从最终 tool_result 的 JSON 里读 childSessionId） | 中：类型是公开的，发后再改即破坏性；`spend` / `decisions` 都是可选成员，TanStack 路径只实现 `spend`，其 `subagentPause` 降级为 isError（TanStack 边界不能只暂停一个工具） |
| 2026-09-10 | **0.1.0 发布准备（E4）**：11 个包同号 0.1.0、一条 minor changeset 生成全部 CHANGELOG 首条；examples 设为 `privatePackages: { version: false, tag: false }` 不参与版本；每包各放一份 LICENSE（npm 只打包包目录内的文件）；新增 `scripts/check-dist.mjs`（`pnpm check:dist`）作为发布前必跑的产物自检；每包英文 README，设计文档保持中文；根 README 状态改 0.1.0。`repository` / `homepage` 字段等 GitHub 组织建好再补 | 同号发版让"哪套包互相兼容"一眼可见，0.x 阶段没必要各包独立节奏；CHANGELOG 由 changeset 生成而不手写，避免两处真源；产物自检是因为 store-sqlite 的 `node:sqlite` 被剥前缀只有 dist 才炸的旧坑 | 低：都是元数据与脚本 |
| 2026-09-10 | **发前双审查（cursor / Grok）处置：handler 的 `decisions` 预校验改为与 runLoop 同口径分流**（只有本会话的结论对照本会话 pending，带别的 sessionId 的原样下传），`decisions[].sessionId` 与请求 sessionId 走同一字符集校验 | 两份报告独立抓到同一条：asTool 已文档化的 HTTP 续跑路径实际 409。取"预校验对齐循环"而不是"循环对齐预校验"，因为循环层的分流是 §10.1 定的语义，server 只是入口；不做"先对子会话另起 POST"的绕法——asTool 靠子日志末条是否 `run_paused` 判续跑还是新一轮，子先跑完父会把同一任务再跑一遍 | 改 `handler.ts` 一处 + reins 包两条跨包用例；语义与 runLoop 绑定，改一处必改另一处 |
| 2026-09-10 | **`@reins/adapter-tanstack-ai` 把 `@tanstack/ai` 从 dependencies 改为 peerDependencies（仍 pin 0.53.0）** | R7 的漏登记判定与 TanStack 引擎对 `ChatMiddleware` 的识别都靠引用同一性；作为 dependency 时宿主自己装的 `@tanstack/ai` 若版本不同会出现两份实例，审批全被判成"没登记"而降级为拒绝——功能上安全但整条审批路径失效。peer 让包管理器强制单实例，版本不符在装机时就报 | 改回 dependency 只动 package.json；README 安装命令本来就写了 `@tanstack/ai@0.53.0` |
| 2026-09-10 | **`REINS_VERSION` 单源：core 常量手改，`pnpm check:dist` 核对它与 package.json 一致；tools-mcp 的客户端身份直接引用它** | core 零 Node 不能读 package.json；构建期注入（tsup define）多一层机制只为一个字符串。发前审查抓到 0.0.0 说明缺的是"忘了改会被拦"，产物自检正是发布前必跑的闸 | changesets bump 版本后要手改一行，不改 check:dist 会红 |
| 2026-09-10 | **0.1 书面豁免：`@reins/lowering-pi` 的进阶导出（`draftsOf` / `consumeStream` / `eventsToContext` 等）签名含 pi-ai 类型，d.ts 顶部 import pi-ai** | "pi-ai 类型不出 lowering-pi"的约束边界定为**其它 `@reins/*` 包不依赖 pi-ai 类型**（core / brain / server / reins 等已达标）；主路径 `anthropic()` / `openai()` 与 `PiLoweredPayload` 已 opaque。pi-ai 是本包 dependency，消费者装机必带，类型解析不会失败。改成 `unknown` 或挪 `/internal` 是 breaking，留给 0.2 与 `@reins/lowering-fetch` 一起定 | 0.2 收进阶 API 时再定；届时 CLAUDE.md 那条约束改写为两层表述 |
| 2026-09-13 | **发前清单解封一次，追加 S1 Skill 支持进 0.1，随即重新封口**（Boss 以产品所有者身份决定，走变更程序：先改任务板 / 决策 / 规格 / 状态文档，实施在下个会话，不夹带其它功能） | 09-10 封口时 Boss 不知道 0.1 不含 Skill；Boss 的真实场景第一版就要用，Skill 已是 agent 的通用能力，缺它 0.1 不符合唯一 dogfood 宿主的需求。不选"0.2 首任务"的原因：0.1 的定义就是"Boss 能用"，而不是"技术侧封版"；筛选规则"纯新增发后做"是效率规则，让位于产品定义 | 高（只是排期与范围）；代价是版本再晚约三天，brain 多一个 opt-in 模块与一个 `/node` 入口要一并冻结 |
| 2026-09-13 | **S1 Skill 设计取向：渐进式披露** —— 菜单（name + description）走 Socket 静态 `systemPrompt` 贡献、run 内不变；翻书是 `skill_read({ name, path? })` 工具，正文以 `tool_result` 进时间线而不塞 system；**载体接口不新造**，`SkillSource = Pick<MemoryStore, "list" \| "read">`，任何 MemoryStore（内存 / sqlite / pg）天然就是数据库载体，文件系统载体 `fsSkillSource(dir)` 放 `@reins/brain/node`；只支持说明书型技能，不执行脚本；opt-in，缺 source 不注册；技能内容视同宿主配置可信，`skill_read` 结果不套 untrusted 标记；生命周期（模型提案 / 审核 / 试用 / 回滚 / 变更即刷新）仍留第三期 | 加载哪个技能由模型判断是宪法一（AdRate 示例"全文塞系统提示"是反面做法，改造成第一个真实样本）；正文进日志是宪法二，compact 折叠 / recall 取回 / spill 外溢零改动自动适用；不新造 store 接口是"复用现有不造接口"——宿主已有的记忆表直接当技能库，Workers 上把 SKILL.md bundle 成字符串预填内存实现即可，"存哪"由宿主定、"载体可插拔"由我们保证；不做脚本执行因为子进程越硬约束，技能需要的动作宿主用普通 Tool 暴露；不单独成包 `@reins/skills`，因为它与 memory 同形（只有静态贡献 + 一个工具），单独成包只多一层版本管理 | 中：接口只增不改（core 可能加 Tool 级 trust 声明，实施时按现有字段定形）；brain 首次出现 `/node` 子路径，构建须 `removeNodeProtocol: false` 并跑 dist；trust 豁免若第三期允许模型写技能必须收回 |
| 2026-09-13 | **S1 实施定形（四点）**：① trust 口子 = core `Tool.resultTrust?: Trust`，循环与 TanStack 适配器只在**成功**结果上按它落 `tool_result.trust`，isError / 执行抛错仍缺省 untrusted；② 提供 `inlineSkills({...}, { root? })` 助手；③ 没给 `source` **或** `root/` 下无一份合规 SKILL.md 都不注册、告警一次，不合规单份跳过各告警一次；④ memory 的路径规范化与 view 截断逻辑抽成 brain `shared/`，memory 成薄包装、文案逐字不变 | ① `Tool` 没有 trust 字段而 `ToolResultDraft`（EventDraft）已有 `trust?`，口子开在 Tool 上两条降级路线才都认；开在模块 afterTool 里会受 Socket 顺序影响（后面的 afterTool 重建草稿就丢），且适配器路径认不到。只用于成功结果：错误文案里可能夹工具抛出的内容。② 实施前核实：`adrate skills install` 落盘的 SKILL.md 是"请运行 `adrate skills read`"的存根，正文只在 CLI 里，`fsSkillSource` 读它模型会拿到一句做不到的指令——字符串预填是唯一能接 AdRate 的路，也正是 Workers 的用法。③ 与 memory"缺则不注册"同理：空菜单 + 一用就"不存在"的工具会让被规则要求先读技能的模型撞墙。④ "同一个值在两处比对必须同一个纯函数"（踩坑教训），memory 52 个用例就是重构的锁 | 中：`resultTrust` 是 core 公开类型新增可选字段，只增不改；其余在 brain 内部 |
| 2026-09-13 | **`skill_read` 缺省 `maxReadChars` 从规格的 16000 改为 40000，并声明 `resultPolicy: { maxTokens: maxReadChars, overflow: "spill" }`** | 真跑发现：AdRate `adrate-ads` 24.5k 字符，16k 上限下被截掉 110 行（含"Keep Campaign writes server-owned"），DeepSeek 与 Claude **都没有**按截断提示 `range` 续读就开工。"先读再动手"的说明书被截等于没读全，与 memory 文件（模型自己分页读）性质不同；Agent Skills 规范建议 SKILL.md ≤ 500 行 ≈ 40k 字符，缺省要让规范内的技能一次读完。resultPolicy 同值是因为 token 数 ≤ 字符数，spill 的按工具限额永远放行——否则 6k 阈值的 AdRate 示例会把技能正文换成预览 + fetch_blob，让模型再翻一次 | 高：两个数字，宿主可覆盖；再改先看真跑录像 |
| 2026-09-13 | **0.1 未发布，S1 直接改写各包 CHANGELOG 的 0.1.0 条目，不再加 changeset** | 09-10 已 `changeset version` 生成 0.1.0；再加一条 changeset 会在下次 version 时把版本推到 0.2.0，而 0.1.0 尚未 publish，公开面上 S1 就是 0.1.0 的一部分 | 高 |

| 2026-09-13 | **S1 发前双审查处置**（两个只读子代理：安全 / 正确性、接口 / 文档 / 发布就绪；逐条核实后全部属实）：① `Tool.resultTrust` 收窄为 `"system" \| "untrusted"`，落法收成 core 一份纯函数 `toolResultTrust(tool, isError)`，四处都调它（runLoop 执行结果、runLoop 接受宿主回填的客户端工具结果 input、TanStack afterToolCall、TanStack toolPhaseComplete）；② `skill_read` 视图改硬上限（带 range 也截、超长单行切开），`resultPolicy.maxTokens = 2 × maxReadChars + 256`（可证明的 token 上界）；③ `skills({ root })` 拒绝与 `/memories` 相同或互为前缀、拒绝 `.`/`..` 段；④ 宿主工具表已有同名 `skill_read` 时整个不注册 + 告警；⑤ `inlineSkills` 键与文件路径不合规直接抛错；⑥ fsSkillSource 把 ENAMETOOLONG / ELOOP / EACCES 等当"不存在"，skills 的 execute 兜住载体异常只告警、模型看"读不到"；⑦ frontmatter 对齐 YAML：BOM、`>` / `\|` 块标量、` #` 注释、重复键后者优先、空头部；⑧ 改名 `DEFAULT_MAX_READ_CHARS → DEFAULT_SKILL_READ_CHARS`、`DEFAULT_SKILL_FILE → SKILL_FILE_NAME`，`"/skills"` 收成 `skills/constants.ts` 一份；⑨ `SkillsOptions.source` 类型加 `\| undefined`；⑩ 11 个包 `files` 补 `CHANGELOG.md`；⑪ brain 加一条扫源码的用例把"主入口零 node:*"的闸补回来 | ① 审查发现 `resultTrust` 只落了两处，客户端工具经 HTTP 回填与 TanStack 兜底路径静默降级；"同一判定同一纯函数"是本仓库三次踩坑的教训。收窄类型：工具输出冒充 principal / model 都不对。② 审查复现：40k 字符 500 行中文技能（规范内）按"token ≤ 字符"取上界会被 spill 外溢成预览，且取回来是 untrusted——core 粗估非 ASCII 一字一 token，行号又加 8%；软上限还能被 `range: [1, -1]` 与单行绕过。③ 审查复现：`root: "/memories"` 被接受，模型 `memory create` 一份 SKILL.md 下一 run 就进系统提示、读出来 system 信任。④⑤ 都是"静默失效"：菜单指向宿主另一个同名工具；`"a/b"` / `""` / `"../x"` 键既不进菜单也不进 rejected。⑥ 审查复现：300 字符单段让 realpath 抛 ENAMETOOLONG，宿主技能目录绝对路径进了模型上下文。⑦ `description: >` 被解析成一个字符、BOM 让整份技能消失，都无告警。⑧ 发布即冻结，改名只在发前免费。⑩ `npm pack` 实测 CHANGELOG 不在包里，而 S1 刻意用 CHANGELOG 记变更。⑪ tsconfig 为 node.ts 开了 node 类型后，主入口误写 `node:` 导入不再编译失败，只剩 check:dist 一道闸 | 中：①③ 是行为收紧（原本可接受的输入现在拒绝），发前改无成本；其余内部 |
| 2026-09-13 | **运行时告警与构造期错误文案 0.1 保持中文**，与全包既有约定一致；英文化列 0.1 之后 | 审查指出英文 README + 中文告警对非中文用户是死路，但这是全包十几处字符串的产品决策，不该夹在 S1 审查修复里单独改 skills 一处造成风格分裂；0.1 的定义是"Boss 能用" | 高：纯文案 |
| 2026-09-14 | **0.1 保持 MIT，不改限制性许可证**（Boss 曾提议"年营收 100 万美元以下免联系、超出需申请"的有限开源；我分析后 Boss 决定直接 MIT）。"注明出处"由 MIT 自带的"保留版权声明与许可证全文"条款满足，版权行统一写 `Copyright (c) 2026 NewRate Limited`（12 份 LICENSE），11 个 package.json 补 `author` / `repository` / `homepage` / `bugs`，各 README 尾行改 `MIT © 2026 NewRate Limited` | 现成的对应许可证是 PolyForm Small Business 1.0.0（<100 人且上年营收 <100 万美元 2019 年美元随 CPI 调整），法律可行、上游零障碍（三处外部依赖全 MIT / Apache，且都是 import 外链未打进 dist，无再分发义务）；但它不是 OSI 开源，reins 是被嵌进别人代码的**库**而非独立部署产品，企业依赖扫描会把非 OSI 的 npm 包标红、别人的 MIT 项目无法引用，会切断 0.1 唯一目标"有人用"的传播路径；收入应来自产品层而非库授权。版权持有人写真实法律主体而非 `reins contributors`，将来维权 / 转让干净 | 低：MIT 发出的版本永远 MIT，收紧只能对将来版本生效 |
| 2026-09-14 | **仓库落 `github.com/AdRate-io/reins`，npm 用已发过 `@adrate/cli` 的 `adrate-io` 账号发布**；包名仍 `reins` + `@reins/*`（`reins` 组织建不了则 `@reinsjs`）。更正 09-08 记录：GitHub 用户名 `reins` 并非空闲，是 2013 年注册的零仓库个人账号（当时只查了组织页）；`reins-ai` 亦被占，`reins-dev` / `reinsjs` / `reins-agent` 空闲 | Boss 定：modelflare.ai 尚未运营，AdRate-io 的 GitHub / npm 发布链路现成（已有公开仓库 `cli` 与 npm 包），版权主体 NewRate Limited 与之关联；仓库以后迁到 ModelFlare 下 GitHub 自动跳转，只需下个版本改 `repository` 字段 | 高：都是元数据 |
| 2026-09-14 | **npm scope 改为 `@reinsjs/*`，总包仍 `reins`，GitHub 仓库仍 `AdRate-io/reins`**。Boss 建 npm 组织 `reins` 时提示不可用，按 09-08 备选建了 `reinsjs`；全仓库 201 个文件 711 处 `@reins/` → `@reinsjs/`（源码、示例、spikes、README、CHANGELOG、check-dist 表、活文档），`docs/DECISIONS.md` 与 `docs/归档/` 作为历史记录不改 | 组织名与裸包名是两套命名空间：`reins` 裸包仍空闲，总包名不必动；GitHub 仓库名与 npm scope 无关，保持产品名 `reins`。历史文档里的旧名保留，否则 09-08 那条"备选 @reinsjs"会被改得自相矛盾 | 低：包名发出即冻结，改名等于新包 |
| 2026-09-14 | **录像脱敏处置（E4，Boss 批准方案 A 后按实情调整）**：不删历史文件，而是用 `git filter-repo --replace-text` 把 E2 口径的别名表（`examples/adrate/scrub.ts` 生成，patrol 部分与 fixture 的 build.ts 同规则同顺序，S1 新值接着编号，共 236 条）在全部历史的全部文件里逐字替换；原始录像挪到 gitignore 的 `recordings/raw/`，公开版 `recordings/*.jsonl` / `.html` 由 scrub.ts 生成；S1 三份录像同口径脱敏后入库；测试广告主 id 等本地专用值移入 gitignore 的密钥文件由 `local.ts` 读取；单测改用编造值 / 按别名形态断言。**S1 的计划名不打码**（核过全是测试广告主的，与 E2 一致）；产品团队名 `AdRate 团队` 在录像里照样别名化但不进历史替换表 | 按值扫全历史发现真实 id 散落在 10 个文件（README、示例源码、单测、文档），只删录像挡不住；同一张表在录像、fixture、历史里同值同别名，回放与 eval 互相对得上；备份 bundle 在仓库外 `../reins-pre-rewrite-2026-09-14.bundle`，仍含真实值，Boss 自行保管 | 低：历史已改写且尚未推远程，推送后不可再改 |
| 2026-09-14 | **docs/ 随仓库公开，走"留在仓库 + 一次体检 + 一条公开性规则"**（Boss 定，四选一：原样公开 / 本方案 / 拆私有文档仓库 / 过滤镜像）。体检三项：① 全历史作者邮箱换成 GitHub noreply（filter-repo mailmap，本仓库 `git config` 同步）；② 对第三方网关的表述从"是某产品的代理"改为只记现象（"会改写请求：最后一条 user 之后的内容丢、中途 system 被换"），历史提交用同一张短语表一起改，`spikes/aireiter-kiro-check` 改名 `aireiter-gateway-check`；③ `docs/README.md` 开头加公开说明（Boss = 产品所有者，文档是维护者工作记忆）、路由表末行加公开性规则。"Boss" 称呼、归档审查报告、运维细节保留 | 文档与代码同一个提交才不漂移，拆仓库让路由表与几十处相对链接全断；文档是产品论点（驾驭经验 + 用数字说话）而非负担；对第三方内部实现的推断公开有风险，观察到的现象才是我们能负责的部分；邮箱推送后不可再改 | 低：规则一行，后续按路由表执行零额外成本 |
| 2026-09-14 | **提交作者邮箱定为 Boss 个人 GitHub 账号 `choodie-chen` 的 noreply**（`153592099+choodie-chen@users.noreply.github.com`），本仓库 `git config user.email` 同步；GitHub 显示名保留 `chenshuli`。首次改写误用了 GitHub 上另一个同名用户 `chenshuli` 的 id，推送前经 `gh auth` 登录结果发现并改正 | GitHub 按邮箱归属提交，用错 id 会把 76 个提交算到无关账号头上；账号身份只能以登录结果为准，不能按显示名猜——本次教训 | 低：推送前改正，远程只有正确版本 |
| 2026-09-14 | **总包改名 `@reinsjs/agent`**（目录 `packages/agent`，盘点 `模块盘点/agent.md`）。npm 以"与 `redis` 过于相似"拒绝裸名 `reins`（防拼写劫持的自动规则，申诉按周计）；Boss 在 `@reinsjs/agent` / `@reinsjs/reins` / `reins-agent` 三选一中定前者。MCP 客户端身份、TanStack 中间件名、中断类型名里的 `"reins"` 是产品标识，不随包名改 | 总包职责就是"整车 agent"，名字说清角色；11 个包统一在 `@reinsjs` 前缀下一眼看出同源；裸名以后还可能撞别的规则 | 低：其余 10 个包已发布不受影响；总包尚未发布，改名零成本 |
| 2026-09-14 | **0.1.1：只改文档也 11 个包同号一起发**（Boss 定 npm 页面现在就要一致）。0.1.0 tarball 里的 README / CHANGELOG 仍把总包写作 `reins`；改 CHANGELOG 0.1.0 条目的文案（asTool 所在包名、补 skills），README 已在仓库改过；`REINS_VERSION` 同步 0.1.1 | 沿用 09-10"同号发版"：哪套包互相兼容一眼可见，比只发 4 个改了 README 的包省解释；docs-only patch 的代价是 Boss 在终端确认 11 次 | 低 |
| 2026-09-14 | **AdRate 接入六条评估 → 0.2 候选 D1～D5**（AdRate 项目侧的评估提出六条，逐条对照代码核实后归置）：① 工具懒发现 → 做，brain 模块，形态照 skills，`Tool.lazy` 至此有消费者，eval 门禁；② 落库前脱敏钩子 → 不加接口，先写配方（afterTool 草稿 + 包 log.append），必要时再加 `redactingLog` 助手；③ 跨进程 run 登记 → `RunRegistry` 接口化 + store-pg advisory lock 实现，README 红线；④ 规范化序列化放开 jsonb → 不做，等真约束；⑤ 可观测 → handler 加 `onEvent` 旁路钩子（`agent.run()` 已是生成器，评估漏看了这点）；⑥ 审批过期 → `approval({ ttl })`。两条"暂不动"（lowering-fetch、告警英文化）同意。**0.2 开发不等 AdRate 对 0.1 的实测**（Boss 定）：五项全是加法不改已发布形状，0.1.x 补丁可并行 | 事实修正两处：双实例并发只浪费一次模型调用、第二个 run 撞 seq_conflict 进 error 态，不是"两条回复"；进程内 run 本就 yield 每条事件。工具表"run 内固定"约束的是绑定表，不限制逐轮暴露的子集，与宪法一（模型自己决定看哪些工具）一致。digest 算法一改 0.1 的暂停状态全失效，属状态格式变更，收益不抵 | 低：全为加法 |

| 2026-09-14 | **D1 工具懒发现设计定形**：brain 第十个模块 `lazyTools(opts): Socket`，`Tool.lazy` 至此有消费者。形态照 skills 的渐进式披露：① 静态 `systemPrompt` = `LAZY_TOOL_RULES` + 菜单（每个标 `lazy: true` 的**宿主**工具一行 `name: 摘要`，摘要缺省取 description 首行截 160 字符，`summarize` 可换；按 name 排序），run 内不变、进 configHash；② 静态 `tools` = `tool_find({ names })`（`risk: low`、`resultTrust: system`——工具说明是宿主配置，与 skills 同理），结果 = 每件的完整 description + inputSchema JSON，一次最多 `maxPerCall`（缺省 20）件，不在菜单的名字在结果尾部点名，一件都没找到才 isError；③ `beforeModel` 把本轮工具表过滤成"非 lazy + 已取回的 lazy"，**已取回集合从时间线重建**：本会话所有 `tool_find` 的 tool_call 与其非 isError 的 tool_result 按 toolCallId 配对，取 `names ∩ 当前菜单`——不加新事件类型、不留内存状态，暂停 / 续跑 / 跨 run / 换进程一律成立；④ `beforeTool` 对"菜单里有、尚未取回"的直接调用回 `block`，文案指向 `tool_find`（否则循环回"未知工具"，模型会以为工具不存在）。绑定表 `tools_bound` 仍含全部工具、run 内固定，只是每轮请求暴露不同子集。**只对宿主工具生效**：菜单用对象同一性记住哪些是"宿主的 lazy 工具"，Socket 贡献的工具（MCP `override` 标了 lazy）不进菜单也不藏——藏了却没菜单等于工具消失。缺一件 lazy 宿主工具、或宿主已有同名 `tool_find` → 不注册 + 告警一次。`resultPolicy.maxTokens` 在 setup 时按"最大的 maxPerCall 件条目估算之和 + 256"算出，spill 永不把 schema 换成 untrusted 预览。**不做**：关键词搜索（菜单就是搜索空间，读菜单挑工具是模型的判断）、按 provider 原生 deferred tools 下发（降级层优化，另立任务）。**门禁**：eval fixture 用 AdRate 29 个真实操作 + 邻近领域合成干扰项凑 200 件（本地 capabilities.json 与服务器此刻都只有 16 能力 / 29 操作，任务行的"200"是 AdRate 侧的规划规模）；候选臂 `lazy` 对照 `eager`（同一 fixture、全部工具标 lazy、只差装不装模块），完成度不低、token 不高才进 README 推荐；prompt cache 影响用 spike 实测 | 菜单 + 翻书与 skills 同形，模型已被证明会用（S1 两族都先读技能）；已取回集合放时间线重建是宪法二的直接推论，也是唯一能让审批暂停后换进程续跑仍对得上的做法；block 而不是放行隐藏工具，因为请求里没给 schema 的调用入参多半不对，与其让模型撞"入参不合法"不如一句话指路；resultTrust=system 与 skills 同一条理由（内容等同宿主配置）；Anthropic 文档写明工具表变动使 tools / system / messages 三段缓存全部失效，所以规则文案要求一次把要用的都取回，spike 量这个代价 | 中：模块内部；`Tool.lazy` 是既有字段；若 spike 证明缓存代价不可接受则改为"取回后只在结果里给 schema、不改请求工具表"，接口不变 |

| 2026-09-14 | **D1 结论：`lazyTools()` 落地，opt-in，README 写明适用边界，不进任何缺省装配** | 机制两族都会用（先 `tool_find` 再动手，靶工具全找对、零走错门）；eval 门禁两族第二轮都过（完成度持平 100%，总 token −59%～−62%，计费等价 −13%～−44%）。但 spike 显示代价有边界：Anthropic 官方 API 上每次取回后第一个请求缓存整段重写，一个会话里频繁换任务（22 个请求 7 次取回）按价目加权反而贵 1.7 倍，粗算一次取回要约 7 个后续请求回本。所以适用于"工具表大、单任务用得少、任务长"，不是人人该开的缺省；首个真实消费者是 AdRate 规划中的 200 件表。第一轮 eval 的 fixture 缺陷（authId 类型）与处置见踩坑记录。后续：provider 原生 deferred tools（Anthropic tool search 把取回的定义注入 messages、不改 tools 块）是降级层优化，接口不变，列 0.1 之后 | 高：opt-in 模块 |

| 2026-09-14 | **D2 handler 旁路观测钩子定形**：`HandlerOptions.onEvent(event, { sessionId, principal, request })`，加 `warn` 出口。三条边界：① **只 live 不 replay**——补发的事件从日志重读，再调就是同一事件观测两次；② **不挡 run**——先 yield 给 SSE 订阅者再调钩子，异步返回值按事件顺序串链但不 await 就拉下一条，只有 run 收尾等链（否则 Workers `waitUntil(run.done)` 覆盖不到观测者最后一次写入）；③ **出错只告警不上抛**，一次 run 一次。实现是 handler 里包住 `runLoop` 生成器的 `observed`，**不进 core、不进 `agent.run()`** | 为什么不进 runLoop：它是可整个复制的普通生成器，"观测"就是消费者拿到每条 yield，进程内宿主 `for await agent.run()` 已经是观测，只有 HTTP 路径的消费者是 handler 自己才缺口子（AdRate 评估漏看了这点）。为什么不是 EventLog 装饰器（包 `log.append`）：那会连子代理会话与补发一起看到，且拿不到 request / principal——traceId 正是 HTTP 请求才有的东西；包 log 仍是可行的宿主方案（D5 脱敏配方走的就是它）。为什么 await 放收尾而不是每条：每条 await 让观测者的时延进模型循环；完全不 await 则 Workers 会在响应结束后回收未完成的写入。为什么不给 result / error 一个钩子：`run_paused` 已是事件，done 的尾巴是最后一条事件，需要终态的宿主看 SSE `result` 帧或 `ActiveRun.done` | 低：纯加法，`observed` 独立函数，删掉即回到 0.1 形状 |

| 2026-09-14 | **D3 批准有效期定形**：`approval({ ttlMs })`，缺省不限期。判定挂在 `beforeTool` 管线的 **ask 落点**：`findExpiredApproval(ctx.timeline, toolCallId, ttlMs)` 取最后一条 `approval_request` 与其后最后一条 approved=true 的 `approval_decision`，两者 `at` 之差 > ttl → emit `approval_decision(approved=false, by="approval.expired")` + `block`，模型拿到"可重新发起"的说明；`rules` 缺省时追加一句过期可重试。`by` 用策略 id `approval.expired` 而不是任务行写的 `"reins"` | 为什么挂 ask 落点不挂入口：只有"本该问人、放行靠宿主批准"时过期才有意义，策略已 allow 的调用不需要批准。为什么 deny + block 不重新 defer：循环对已批准调用的 defer 直接略过，block 永不略过。为什么比事件 `at` 不读墙钟：宿主批准事件是续跑时循环按 `cfg.now` 写的，它的 `at` 就是批准到达时刻；日志自足，回放结论与机器无关，测试也不用 fake timer。为什么 `by` 不是 `"reins"`：本模块 `by` 一律是策略 id（`approval.unknown_tool` / `approval.risk.*`…），审计按前缀过滤；`"reins"` 是 TanStack 适配器对"宿主漏登记中断"这类框架代答的写法，语义不同。为什么不让未批的 pending 过期：那是"人还没回答"，不是"回答太晚"，等多久都该等；要清理靠宿主 | 低：纯加法，选项不设即旧行为 |

| 2026-09-14 | **D4 跨进程 run 登记设计定形（下次会话实施）**。① **形状直接改，不留兼容**：`RunRegistry` 从类改成接口 `{ get(sessionId): ActiveRun \| undefined; create(sessionId, controller?): Promise<ActiveRun> }`，现有类改名 `InMemoryRunRegistry`，`create` 变异步，handler 的 POST 路径随之 await；0.2 minor。② **机制用租约表，不用 advisory lock**：core 新增存储接口 `RunLease { acquire(sessionId, owner, ttlMs): Promise<boolean>; renew(sessionId, owner, ttlMs): Promise<boolean>; release(sessionId, owner): Promise<void> }`（与 EventLog / BlobStore / MemoryStore 并列，`Stores.runLease?` 可选），store-pg 用一张 `reins_runs(session_id PK, owner, expires_at bigint)` 实现，三条都是单条语句：acquire = `INSERT … ON CONFLICT DO UPDATE … WHERE expires_at < 库时间 RETURNING owner`，renew = 条件 UPDATE，release = 按 owner DELETE；**过期判定用数据库时间**（`extract(epoch from now())`），不用各实例本机时钟。③ **租约语义放 server 一份**：`leasedRunRegistry(lease, { ttlMs = 30_000, owner = uuidv7() })`，起跑 acquire、每 ttl/3 心跳 renew、`run.done` 时 release；renew 返回 false（本实例被冻结超过 ttl、别人已接手）→ `controller.abort()` 让本 run 以 paused(host) 收场，seq_conflict 仍是最后一道闸；release 失败只告警，靠到期自然释放。④ **`get` 只认本进程**：跨实例的 GET 重连只补发不接实时流（`live: false`），README 写明用 sticky session 或接受补发即止。⑤ `createAgent({ store })` 见 `store.runLease` 就自动装 `leasedRunRegistry`，宿主不用记这一步。⑥ README 红线：多实例部署必须共享登记表，否则两实例可同时对一条会话起 run，第二个浪费一次模型调用后撞 seq_conflict | 不留兼容：0.1 今天刚发、零用户，兼容层只会给未来的读者留一个"为什么有两个名字"的谜。不用 advisory lock：会话级锁绑连接，`PgClient` 就是 `query(text, params)`、连接池每条语句可能走不同连接，解锁找不到上锁那条连接；事务级锁要 BEGIN/COMMIT，违反"pg 侧不开事务、每个写是单条语句"。用库时间：租约的意义是各实例对"谁持有"达成一致，判定时钟必须唯一，本机时钟偏几秒就会同时两个持有者。接口进 core 而不是 server：store-pg 依赖方向只能指向 core（硬约束）。语义放 server 而不是每个 store 各写一遍：心跳、丢租约中止、失败告警是登记表的行为，与用哪种数据库无关，store 只做三条 SQL。`get` 不跨进程：别的实例的 run 事件流只在那个进程的内存里，要跨实例转发就得引入消息通道，不值；补发本就从日志读、零状态。ttl 30 s / 心跳 10 s：进程崩了会话最多锁 30 s，心跳三次机会才判丢 | 中：core 加一个接口、server 改 `RunRegistry` 形状、store-pg 加一张表与三条语句；`agent.run()` 进程内路径不受影响 |

> 待 Boss 本人操作的事项已移到 `docs/TASKS.md`「待 Boss 本人操作」（2026-09-10）。
| 2026-09-14 | **D4 实施定形（对「设计定形」的三处补充）**：① `RunLease.acquire` 对**同一 owner 幂等**（等于续期），pg 的 WHERE 是 `已过期 OR owner 相同`；② `ActiveRun` 新增 `onFinish(fn)` 收尾钩子，结束那一刻**同步**启动、`done` 等异步部分落定，登记表删项与租约释放都走它；③ pg 侧类名 `PgRunLease`（不是任务行草拟的 `pgRunLease(client)` 函数），`pgStores()` 缺省带上；acquire 时存储不可用照常从 handler 抛出，不翻成 409 | ① 同进程两个请求并发 `create`：不幂等就得"先查后占"两步，与单条语句约束冲突；幂等后先到者登记、后到者拿 `RunConflictError`，租约仍归先到者、不需要回滚。② 删项若挂在 `done.then` 上，`done` 一旦要等 release 的数据库往返，同会话紧接着的 POST 会在这几毫秒里误撞 409；反过来 release 若不接到 `done` 上，Workers 的 `waitUntil(run.done)` 覆盖不到它。同步启动 + 异步落定两头都顾到。③ 与 `PgEventLog` / `PgBlobStore` / `PgMemoryStore` 同形；缺省带上是让"用 pg 就自动多实例安全"成立，单实例代价只是每个在跑的 run 每 10 s 一条 UPDATE。存储不可用不翻 409：409 的语义是"别人在跑"，不知道能不能跑时该让宿主看到真错误 | 低：① 改 SQL 一个 OR；② `onFinish` 是加法；③ 类名与缺省项可改 |
| 2026-09-14 | **D5 脱敏走配方不走接口**：根 README 给两段代码——`afterTool` 草稿脱敏的 Socket（放 sockets 首位）+ 包住 `EventLog.append` 的 `redactingLog`；配方与 `packages/brain/src/redaction.recipe.test.ts` 逐字一致，用例锁住"顺序错了 blob 漏原文"与"包装管日志与模型视图（循环每轮从日志重读）、管不到 yield 出去的对象（SSE / onEvent 见原文）"两条边界。核心不加 `redactingLog(log, fn)` 助手 | 脱敏规则只有宿主知道，库给选项等于替宿主猜正则；两处入口（afterTool、append）本就存在，配方 30 行说得清。为什么写成用例：README 里的代码没人跑过就会随接口漂移，用例让它跟着 `pnpm check` 一起绿。为什么不做"流也看不到"：yield 出去的是循环自己造的对象，要洗得在 input 进循环前或在编码器 / onEvent 里做，是宿主输入输出层的事；README 把边界说破而不是含糊 | 低：纯文档 + 一个用例；若 AdRate 接入时包 store 别扭，加助手是加法 |
| 2026-09-14 | **`@reinsjs/lowering-fetch` 立项（与 Boss 讨论定）**：`Lowering` 接口的第二份实现，只用 `fetch` + 自写 SSE 解析，零依赖；**三个协议**：OpenAI Chat Completions、Anthropic Messages、OpenAI Responses，按此顺序实施。**不是替换 pi 版**：两包并存，宿主 import 谁用谁，总包照旧不带降级层；将来是否把 fetch 版设为推荐，等它跑过同一套损失矩阵与真模型后凭数据再定。**Chat 方言范围**（Boss 定）：严格按 OpenAI 官方规范实现，扩展只收 DeepSeek 已验证的（如读侧 `reasoning_content` 当 thinking），其余厂商方言等真有人用再加。Chat 线两处天然有损进矩阵：thinking 不可回放（无签名、无加密项）、无显式缓存断点（断点处置为空操作）。模型目录只放一张最小表（上下文窗口、能力位、价目），`ModelRef` 可覆盖，表过期不阻塞使用。**验证靶子**：DeepSeek 直连（Chat 主场）；Anthropic Messages 与 OpenAI Responses 的官方端点经 **Cloudflare AI Gateway 统一计费**（Cloudflare 持有厂商凭证、按原价 + 5% 计费，我们无需厂商账号，Boss 定：官方账号有封号风险不申请），用前先做暗号法忠实度体检，过了才算官方靶子；aireiter 网关已知改写请求，只作辅助 | 为什么现在做：AdRate 升 Node 22 观察期一两天，是无外部依赖的整段窗口；pi 版三个代价——装机 65 M 一半不走、无 system 角色靠 `onPayload` 外挂改写、请求整形改不了（provider 原生 tool search / deferred tools 做不了）；第二份实现是对降级契约最好的体检。为什么加 Chat：它是 OpenAI 兼容生态的正门（DeepSeek / Qwen / 网关 / vLLM 都是它），pi 版恰好不做，加了这个包才不只是"pi 的瘦身版"。为什么 Chat 先做：DeepSeek 直连是手里最可靠的靶子，先在最简单的协议上立起三线共用的骨架（fetch、SSE、重试判据、用量成本），Anthropic 规矩最多放第二当压力测试，Responses 最后与 pi 版逐格对照。为什么方言收紧：Chat 是方言家族，不收紧会变兼容性泥潭 | 中：新包不动已有接口；自己扛协议代码（估 1.5～2k 行）与模型小表的长期维护；估三到四个完整会话 |
| 2026-09-14 | **F0 靶子体检通过，Cloudflare AI Gateway 透传路径定为 lowering-fetch 的官方 Anthropic / OpenAI 靶子；aireiter 降为辅助**：`spikes/cf-gateway-fidelity` 暗号法 43/43（Anthropic 25 / Responses 13 / Chat 5），正向项全部到达（Opus 5 中途 system 末尾与中段、块级与顶层 `cache_control` 用量、`anthropic-beta` 头、图片、六轮工具往返、并发无串扰、缺省不缓存），反向项全部拿到厂商原文 400（伪造签名、伪造 `encrypted_content`、假 beta 头、未知参数）。顺带实证 F2 要用的摆放规则原文：中途 system 必须紧跟 user（或以服务端工具结果结尾的 assistant），system 后接 assistant 合法 | 判据是产出内容与厂商原文错误，不是状态码：aireiter 正是在"伪造签名 200 放行、末尾 system 丢失"上露馅，CF 两处都对。为什么不再等更多验证：缓存用量、签名校验、密钥注入这三样是网关最可能改写的地方，已逐项过；剩下的"极端负载下的 429"属运行期问题，按瞬断重试处理。三处非网关发现（Anthropic `reasoning_extraction` 分类器对中途 system 里的密语敏感且 `mid-conversation-output-config` beta 抬高触发率、Haiku 4.5 最小可缓存 4096、强制 tool_choice 官方 finish=stop）记踩坑，F2 的 beta 头策略改为"用到才带" | 低：靶子换回直连只改基址与头；F2 / F3 真模型验证据此进行 |
| 2026-09-14 | **F1 定形：lowering-fetch 的共用层是 IR 不是协议消息**——`eventsToIr` 只做分组、后移、trust 标注并记事实（deferred / escaped / 来源），三条协议各自从 IR 编码请求体，没有第二跳改写；`LoweredRequest.payload.body` 就是 POST 的 JSON；encoder 记完落点后 `orderLandings` 按输入事件顺序排回（core 契约"顺序与输入一致"，pi 版后移项乱序未守）| pi 版把 pi-ai 三角色 Message 当中间层，协议缺口（无 system 角色、缓存断点）只能在 `onPayload` 外挂补；IR 把"协议无关的事"与"协议的事"切开，F2 / F3 只加目录不改共用层。排回顺序是因为契约写着，而且 eval 逐条对照落点要靠位置 | 低：IR 是包内类型，不进公开承诺 |
| 2026-09-14 | **Chat 方言只收 DeepSeek 的 `reasoning_content`，且写侧"字段必在"**：`FetchModel.chat.reasoningContent` 开关，`deepseek()` 缺省开；开着时每条历史 assistant 消息都带 `reasoning_content`——同家 thinking 拼进去、没有就空串、别家的记 dropped；关着（`openaiChat()`）不带字段、thinking 记 dropped。立项时只打算读侧 | 2026-09-14 直连实测：DeepSeek 缺省开 thinking，带 `tools` 的请求里 assistant 缺该字段直接 400（"must be passed back"），空串可过，不带 tools 时忽略——不做写侧就跑不了多轮工具。为什么不"有 thinking 才带"：非 thinking 模式产的历史轮也要带（厂商文档明说"即使该轮没调工具"），空串是最简单的合规形 | 低：方言开关按模型声明，Boss 定的"扩展只收已验证的"仍成立 |
| 2026-09-14 | **Chat 线四处有损的表达方式**：同轮多段正文合并成一个字符串（lossy merged-text；DeepSeek 只接受 string \| null）；tool 消息只收文本、图片换占位（lossy tool-text-only）；`isError` 以 `[tool error]\n` 前缀表达（lossy；协议没有错误位）；无显式缓存断点，什么都不做、`cached_tokens` 记 cacheRead。用户消息带图而模型不收图时换占位文本（lossy）| 宁可在矩阵里声明有损，不静默丢：模型区分不了"工具报错"与"工具回了一句 Error"，前缀至少把位信息落到文本里；不用 content parts 发 assistant 多段是因为 DeepSeek 不接受、而"最兼容"是 Chat 线的存在理由 | 低：都是落点文案，改了补矩阵即可 |
| 2026-09-14 | **`FetchModel.auth: "bearer" \| "x-api-key" \| "none"`**，缺省按协议；`"none"` 时不问 `apiKey` 回调、凭证由 `headers` 自带 | CF AI Gateway 的凭证是 `cf-aig-authorization` 头，F0 实测再带 `Authorization: Bearer` 会失败；pi 版没有这个开关，走 CF 得靶子专用 hack。模型级而非降级层级：同一实例可混用直连与网关 | 低 |
| 2026-09-14 | **HttpError 文案对齐官方 SDK 的 `"<status> <body>"`、对象带 `status` / `headers`，不另造重试约定**；超时用 `AbortSignal.timeout`（TimeoutError，core 判可重试），宿主中止 AbortError 不重试；流中途断开先交出已拼草稿再记 error（`stream ended before finish_reason` / 错误文案 + cause.code）| core 的瞬断判据（R3）已经按这套形状写好，两家 SDK 与 pi-ai 都是这个格式，第三份实现再造一套等于让 core 多认一种；错误体不假定形状（CF 429 是网关信封） | 低 |
| 2026-09-15 | **F2 定形：Anthropic Messages 线的五条规矩**——① **中途 system 归位**：说明先攒着，放在"下一条 assistant 之前"或收尾；此刻前一条不是 user / system（是 assistant，或说明是首条）就退成 `<system_note>` 框住的 user 文本记 lossy(user-role)；位置比时间线晚一条 user 的仍算 exact 并备注（说明只换位置）。② **tool_result 紧跟**：同批结果与被 IR 后移的用户消息 / 摘要并进同一条 user（F0 A5b 实测同条 user 里 tool_result 后接文本可达）。③ **thinking 只回放带 signature 的同家块**，`redacted_thinking` 以 data 回放；无签名（流中断）与别家的 dropped 声明，**不像 pi-ai 那样降成正文**。④ **缓存断点自己打三处**（system 末块 / tools 末项 / 最后一条 user 末块），说明殿后时按 `anthropic.midSystemCacheBreakpoint` 处置，缺省 `automatic` = 顶层 `cache_control`；块级 + 顶层总数封顶 4，宿主 requestOptions 里的顶层 `cache_control` 不覆盖。⑤ **`anthropic-version` 固定带，`anthropic-beta` 只在模型声明 `anthropic.betas` 时带**；`thinking` 参数不缺省设置，`max_tokens` 缺省取模型声明 | ① 是厂商规则原文（F0 A2d：system 跟在 assistant 后 400）；pi 版的 `placeOf` 同一算法，这里没有第二跳所以直接在 encoder 里做。③ 私下推理不该变成模型"说过的话"进历史；且 Fable 5.1 缺省 display omitted，正文空、签名在，回放靠签名不靠正文——所以判据是签名不是正文。④ pi-ai 也是这三处，B1 实测 `automatic` 与不注入持平、`previous-user` 低 3～6 点、留在 system 上几乎零命中；F2 spike 在官方上实证第二个请求起 cacheRead > 0。⑤ F0 A2c 实测中途 system 不需要 beta，且 `mid-conversation-output-config` beta 会抬高 reasoning_extraction 拒答率；Opus 5 起缺省就是 adaptive thinking、Fable 5.1 对 `type:"disabled"` 400，开不开由宿主按代次定 | 低：都是 encoder 内部规则；断点处置一行配置可换；与 pi 版矩阵只差三格（无签名 thinking dropped 而非 text-or-drop、redacted 单列、tool_result 图片按能力处置） |
| 2026-09-15 | **宿主 `requestOptions` 里的 `system` / `tools`（Chat 线是 `tools`）一律剥掉不透传**，其余字段先铺、我们的字段后盖 | 这两个字段只由事件与工具表决定；之前是"有工具时后盖、没工具时不写"，宿主传一份就漏进请求体——F2 单测抓到，Chat 线同病同修 | 低 |
| 2026-09-15 | **F3 定形：OpenAI Responses 线的五条规矩**——① **服务端不留状态**：`store: false` 强制、宿主 `requestOptions` 里的 `previous_response_id` 剥掉，每个请求把历史全量放进 `input`。② **reasoning 回放判据是 `encrypted_content`**：推理模型缺省带 `include: ["reasoning.encrypted_content"]`（`responses.encryptedReasoning: false` 可关，关了 `thinkingReplay` 如实报 false），流侧把整个 reasoning 项 JSON 存 `replay.thinkingSignature`（与 pi 版同字段、事件可互换），写侧整项原样放回；没有加密项（未开 include / 流中断）或别家的 dropped 声明，不降成正文。③ **中途 system 不归位**：developer / system 消息在 `input` 任意位置合法，推理模型用 developer、其它 system（`responses.systemRole` 可钉死），宿主声明 `midConversationSystem: false` 才退成 `<system_note>` user 文本。④ **工具调用两个 id 分开存**：`toolCallId = call_id`，`fc_` 项 id 存 `replay.itemId` 且只在同一模型回放时带回；正文项 id 存 `replay.textSignature`（裸 msg_ id，pi 版的 `{"v":1,"id"}` JSON 也认），没有就补 `msg_reins_<n>`。⑤ `reasoning` / `max_output_tokens` 不缺省设置，tools 一律 `strict: false`。另：**内置表同 id 两协议各一份，`findBuiltin(provider, id, api?)` 带协议精确取；OpenAI 官方 id 无协议解析缺省走 Responses**（F1 时只有 Chat 条目，`openaiChat()` 与 Chat 矩阵测试改为显式取 Chat 条目） | ① 时间线是唯一真源（宪法二），OpenAI 侧的会话状态不能成为依赖；也让三条线请求形状一致。② pi 版只在请求带 effort 时才带 include、`thinkingReplay` 随 requestOptions 变——但 gpt-5 缺省就开推理，不带 include 就会产出无法回放的 reasoning 项；F0 R3 / R3b 实证整项回放接受、伪造 400，所以判据必须是加密项本身。整项存而不是只存 encrypted_content，是因为 summary 段的拆分与 id 都要原样回去，且与 pi 版互换。③ F0 R3 中途 developer 实测到达，Responses 没有 Anthropic 那套摆放规则。④ core 各处（pending 配对、asTool 子会话 id）用的是短 id，pi 版把两个 id 用 `|` 拼进 toolCallId 会漏到 core；fc 项 id 只同模型带回是 pi-ai 的经验（OpenAI 校验 fc 与 rs 项配对）。⑤ 与 F2 ⑤ 同一原则：开不开推理由宿主按型号定，gpt-5.1 起缺省 none；strict 需要 schema 满足额外约束，我们的 JSON Schema 不保证。表的缺省改成 Responses 是因为它是 OpenAI 的主协议、推理系列只有它 | 低：都是 encoder 内部规则；两个方言开关一行配置；与 pi 版矩阵只差三格（无加密项 dropped 而非 text-or-drop、非对象入参 exact 而非 wrapped-args、tool_result 多一格 lossy 表达 isError 前缀 / 图片）。`spikes/f3-responses-live` 经 CF 网关 gpt-5-mini 9/9、gpt-5.4 9/9（第一遍 8/9：自动缓存第二请求未命中，重跑命中——OpenAI 自动缓存是尽力而为，不是我们的换算问题） |
