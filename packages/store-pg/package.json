{
  "name": "@reinsjs/store-pg",
  "version": "0.3.0",
  "description": "reins — PostgreSQL-backed EventLog / BlobStore / MemoryStore. Works with any client exposing query(text, params) → { rows } (pg, PGlite, …)",
  "keywords": [
    "agent",
    "event-log",
    "postgres",
    "postgresql",
    "pglite"
  ],
  "license": "MIT",
  "author": "NewRate Limited (https://www.adrate.io)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AdRate-io/reins.git",
    "directory": "packages/store-pg"
  },
  "homepage": "https://github.com/AdRate-io/reins/tree/main/packages/store-pg#readme",
  "bugs": "https://github.com/AdRate-io/reins/issues",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsup"
  },
  "dependencies": {
    "@reinsjs/core": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@reinsjs/server": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.23.0",
    "pg": "^8.23.0"
  },
  "engines": {
    "node": ">=22"
  }
}
