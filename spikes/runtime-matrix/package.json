{
  "name": "spike-runtime-matrix",
  "private": true,
  "type": "module",
  "description": "四环境验证 spike：Bun / Deno / Vercel Edge（edge-runtime）托管 edge-runtime-check 的同一个 fetch 探针，打各包 dist",
  "scripts": {
    "start": "node run.mjs"
  },
  "devDependencies": {
    "bun": "1.4.2",
    "deno": "2.9.6",
    "edge-runtime": "4.0.1",
    "esbuild": "0.28.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bun",
      "deno",
      "esbuild"
    ]
  }
}
