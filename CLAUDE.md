# reins — 会话入口

> 新会话先读本文件，再按"开工流程"读任务板。全部用中文思考、对话与注释。

## 这是什么

reins（缰绳）是一个可嵌入的 Agent 库：装上、配自己的模型，就得到一辆有驾驭经验的车。底盘 pi 大小、循环归用户、脑子预装、部件可拆、跑在任何 Web 标准运行时、闲时零占用。已有底盘（TanStack AI、pi）的人只装脑子也行。它**不是**框架、不是运行时竞品、不是独立应用、不是平台。

## 两条宪法（一切设计的依据）

1. **决策权默认在模型。** 模型是有判断力的同事。库只做四件事：让它看见（感知）、给它能力（工具）、给它边界（预算、权限、安全网）、给它记录（时间线）。任何"框架替模型决定"的逻辑必须同时提供模型自决路径且后者为默认，框架动作只是兜底。
2. **时间线是唯一真源，角色只是翻译。** 内部只有一种数据：带发起者（user / model / tool / system / host）的事件。用户打断、插话只是再追加一个事件。各家 API 的角色格式只在降级层出现，有损必须声明，禁止静默丢弃。

压缩、记忆、切会话等只是宪法下的可拆实例，不是产品本身。模型换代淘汰某个实例就删那个模块，宪法不变。

## 文档在哪（仓库内相对路径）

| 文件 | 用途 |
| --- | --- |
| `docs/PRD.md` | 做什么、给谁、成功标准 |
| `docs/技术方案.md` | 架构原则 P1~P9、包结构、事件模型、接口、脑子模块规格、里程碑 |
| `docs/TASKS.md` | **任务板**，当前进度与下一步 |
| `docs/DECISIONS.md` | 决策日志（日期、决策、理由、可逆性） |
| `docs/归档/` | 旧稿与调研报告，只作证据查阅，**不要引用其中过期的定位** |

## 协作方式

- Boss 是产品所有者，提供背景与设想；**技术与产品决策由技术合伙人（我）直接做**，写入 DECISIONS.md，Boss 可随时否决。只有需要 Boss 本人账号、资金、业务背景的事才请示。
- 向 Boss 汇报用大白话与比喻，不堆术语；进展用能看的东西和数字证明。
- 遵守全局 CLAUDE.md 的价值观：认真查阅不暗猜接口、复用现有不造轮子、主动测试不跳过验证、诚实坦述。

## 开工流程（每个会话）

1. 读 `docs/TASKS.md`，找到第一个未勾选任务；若前一阶段的 spike（S1~S4）未完成，先做 spike。
2. 读技术方案中对应章节，确认接口与约束；有疑问先查上游源码或文档，不猜。
3. 实现 + 单测；`pnpm check` 全绿。
4. 若做了技术方案未覆盖的决定，追加一行到 docs/DECISIONS.md。
5. 在 docs/TASKS.md 打勾并写一行结果（日期 + 关键结论）；若接口与技术方案有出入，同步修订技术方案。
6. 提交：`type: 摘要`，正文说明对应任务号；结尾 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。不推远程（发布前仓库只在本地）。

## 工程硬约束（摘自技术方案 §1，违反即返工）

- `@reins/core` 与 `@reins/brain` **零 Node 内置依赖**，只用 Web 标准 API；`node:*`、子进程、真实文件系统只能出现在可选包。
- EventLog **只 append**。压缩、外溢、交接一律以追加事件表达；模型可见的一切都在日志里。
- run 状态可序列化；暂停是 `runLoop` 的显式返回值，不是阻塞的回调。
- 循环 `runLoop` 是导出的普通异步生成器，几百行，无私有状态，用户可整个复制。
- 每个事件 type 带 `schemaVersion`，读时 upcast，查不到升级函数即拒绝。
- 每个脑子模块可单独关闭、按模型族配置；模型自决类机制无 eval 不默认开。
- 降级层复用 pi-ai，pin 精确版本，pi-ai 类型不出 `@reins/lowering-pi`。
- 不自造前端协议：AG-UI 为一等输出。不做 MCP 动态注册。多智能体只做 asTool 与 handoff。

## 仓库

- pnpm monorepo，Node ≥ 22，TypeScript 严格模式，vitest，tsup，changesets，MIT。
- `packages/core`、`packages/brain`、`packages/lowering-pi` 起步；其余包按 TASKS 需要再建。
- 命令：`pnpm check`（typecheck + lint + test）、`pnpm format`、`pnpm build`。提交约定见 `CONTRIBUTING.md`。
- 代码注释写清设计意图，见名知义；三个月后再看也能懂。
